import variable as v;
import func.trig as trg;

function main(playerID)
{
   if (v.P_WaitMain[playerID] == 0)
   {
      if (v.P_CountMain[playerID] == 0)
      {
         if (v.P_LoopMain[playerID] == 0)
         {
            trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50);
            Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere");

            epic.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 5, 100, 1);
            trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50);
            KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID);
         }
         if (v.P_LoopMain[playerID] == 4)
         {
            KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
         }

         var i = v.P_LoopMain[playerID] / 2;
         var x = 50 - 50 * i;
         var y = 100;

         if (v.P_LoopMain[playerID] < 8 && v.P_LoopMain[playerID] % 2 == 0)
         {
            RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID);

            if (i % 2 == 0)
            {
               epic.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y, 1);
               trg.Shape_Square(playerID, 1, "Protoss Dark Templar", x, y);
               KillUnitAt(All, "Protoss Dark Templar", "Anywhere", playerID);
            }
            else
            {
               trg.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y);
               trg.Shape_Square(playerID, 1, "60 + 1n High Templar", x, y);
               KillUnitAt(All, "60 + 1n High Templar", "Anywhere", playerID);
            }

            MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
            Order("80 + 1n Artanis", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
         }

         trg.Main_Wait(80);

         v.P_LoopMain[playerID] += 1;

         if (v.P_LoopMain[playerID] == 8)
         {
            v.P_CountMain[playerID] += 1;
            v.P_LoopMain[playerID] = 0;         
         }
      }
      else if (v.P_CountMain[playerID] == 1)
      {
         if (v.P_LoopMain[playerID] == 0)
         {
            RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID);

            trg.Shape_Edge(playerID, 1, "40 + 1n Mojo", 45, 5, 100);
            KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
         }
         if (v.P_LoopMain[playerID] == 2)
         {
            trg.Shape_Edge(playerID, 1, "Kakaru (Twilight)", 45, 5, 100);
            KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID);

            trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50);
            Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere");

            trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50);
            KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID);
         }

         trg.Main_Wait(80);

         v.P_LoopMain[playerID] += 1;

         if (v.P_LoopMain[playerID] == 6)
         {
            v.P_CountMain[playerID] += 1;
            v.P_LoopMain[playerID] = 0;         
         }
      }
      else if (v.P_CountMain[playerID] == 2)
      {
         KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);

         trg.SkillEnd();
      }
   }
}