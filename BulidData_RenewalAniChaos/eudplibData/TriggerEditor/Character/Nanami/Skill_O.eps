import PluginVariables as msqcvar;
import func.shape as s;

const s = StringBuffer();

function main(playerID)
{
   if (v.P_Delay[playerID] == 0)
   {
      if (v.P_CountMain[playerID] == 0)
      {
         if (v.P_LoopMain[playerID] == 0)
         {         
            if (cp < 3) SetSwitch("Unique - Nanami1", Set);
            else SetSwitch("Unique - Nanami2", Set);

            s.Shape_Dot(playerID, 1, "50 + 1n Battlecruiser", 0, 0);
            KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);

            SetDeaths(playerID, SetTo, 2160, " `UniqueCoolTime");
            SetDeaths(playerID, SetTo, 720, " `UniqueSkill");

            s.SkillWait(80);

            v.P_CountMain[playerID] += 1;
            v.P_LoopMain[playerID] = 0;
         }
      }
      else if (v.P_CountMain[playerID] == 1)
      {
         s.SkillEnd();
      }
   }
}