import PluginVariables as msqcvar;
import func.shape as s;

function main(playerID)
{
   if (v.P_Delay[playerID] == 0)
   {
      if (v.P_CountMain[playerID] == 0)
      {
         if (v.P_LoopMain[playerID] < 8)
         {                        
            s.LineShape(playerID, 1, "Kakaru (Twilight)", 45 * 3 * v.P_LoopMain[playerID], 7, 50, 50);
            s.LineShape(playerID, 1, "Protoss Dark Templar", 45 * 3 * v.P_LoopMain[playerID], 7, 50, 50);
            KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID);
            KillUnitAt(All, "Protoss Dark Templar", "Anywhere", playerID);
            s.SkillWait(50);
            v.P_LoopMain[playerID] += 1;
         }
         else if (v.P_LoopMain[playerID] == 8)
         {
            s.SkillWait(0);

            v.P_CountMain[playerID] += 1;
            v.P_LoopMain[playerID] = 0;
         }
      }
      if (v.P_CountMain[playerID] == 1)
      {
         if (v.P_LoopMain[playerID] < 2)
         {    
            RemoveUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);
            s.Shape_NxNSquare(playerID, 1, "50 + 1n Battlecruiser", 3, 75);
            Order("50 + 1n Battlecruiser", playerID, "Anywhere", Attack, "Anywhere");

            s.SkillWait(50);
            v.P_LoopMain[playerID] += 1;
         }
         else if (v.P_LoopMain[playerID] == 2)
         {
            RemoveUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);
            s.Shape_NxNSquare(playerID, 1, " Unit. Hoffnung 25000", 3, 75);
            KillUnitAt(All, " Unit. Hoffnung 25000", "Anywhere", playerID);

            s.SkillWait(0);

            v.P_CountMain[playerID] += 1;
            v.P_LoopMain[playerID] = 0;
         }
      }

      else if (v.P_CountMain[playerID] == 2)
      {
         s.SkillEnd();
      }
   }
}