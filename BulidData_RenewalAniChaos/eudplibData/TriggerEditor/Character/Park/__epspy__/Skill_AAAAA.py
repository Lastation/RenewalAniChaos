## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import Function as f;
import Function as f
# (Line 4) function EdgeShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y);
# (Line 6) function main(cp)
# (Line 7) {
@EUDFunc
def f_main(cp):
    # (Line 8) f.BanReturn(cp);
    f.BanReturn(cp)
    # (Line 10) if (f.delay[cp] == 0)
    if EUDIf()(f.delay[cp] == 0):
        # (Line 11) {
        # (Line 12) if (f.count[cp] == 0)
        if EUDIf()(f.count[cp] == 0):
            # (Line 13) {
            # (Line 14) if (f.loop[cp] == 0)
            if EUDIf()(f.loop[cp] == 0):
                # (Line 15) {
                # (Line 16) f.ShieldFix(cp, 1);
                f.ShieldFix(cp, 1)
                # (Line 17) SetSwitch("ComputerAlliy", Set);
                # (Line 18) SetAllianceStatus(P1, Ally);
                DoActions(SetSwitch("ComputerAlliy", Set))
                # (Line 19) SetAllianceStatus(P2, Ally);
                DoActions(SetAllianceStatus(P1, Ally))
                # (Line 20) SetAllianceStatus(P3, Ally);
                DoActions(SetAllianceStatus(P2, Ally))
                # (Line 21) SetAllianceStatus(P4, Ally);
                DoActions(SetAllianceStatus(P3, Ally))
                # (Line 22) SetAllianceStatus(P5, Ally);
                DoActions(SetAllianceStatus(P4, Ally))
                # (Line 23) SetAllianceStatus(P6, Ally);
                DoActions(SetAllianceStatus(P5, Ally))
                # (Line 24) }
                DoActions(SetAllianceStatus(P6, Ally))
                # (Line 26) if (f.loop[cp] < 10)
            EUDEndIf()
            if EUDIf()(f.loop[cp] >= 10, neg=True):
                # (Line 27) {
                # (Line 28) f.SquareShape(cp, 1, "50 + 1n Battlecruiser", 100, 100);
                f.SquareShape(cp, 1, "50 + 1n Battlecruiser", 100, 100)
                # (Line 29) f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100);
                f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100)
                # (Line 30) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp);
                # (Line 31) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp))
                # (Line 32) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 34) f.SkillWait(cp, 80);
            EUDEndIf()
            f.SkillWait(cp, 80)
            # (Line 36) f.loop[cp] += 1;
            _ARRW(f.loop, cp).__iadd__(1)
            # (Line 38) if (f.loop[cp] == 10)
            if EUDIf()(f.loop[cp] == 10):
                # (Line 39) {
                # (Line 40) f.count[cp] += 1;
                _ARRW(f.count, cp).__iadd__(1)
                # (Line 41) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 42) }
                # (Line 43) }
            EUDEndIf()
            # (Line 44) else if (f.count[cp] == 1)
        if EUDElseIf()(f.count[cp] == 1):
            # (Line 45) {
            # (Line 46) if (f.loop[cp] == 0)
            if EUDIf()(f.loop[cp] == 0):
                # (Line 47) {
                # (Line 48) EdgeShapeAt(cp, 1, "Target", 22, 3, 32, 100, 100);
                EdgeShapeAt(cp, 1, "Target", 22, 3, 32, 100, 100)
                # (Line 49) f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100);
                f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100)
                # (Line 50) KillUnitAt(All, "Target", "Anywhere", cp);
                # (Line 51) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "Target", "Anywhere", cp))
                # (Line 52) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 53) if (f.loop[cp] == 1)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 1):
                # (Line 54) {
                # (Line 55) EdgeShapeAt(cp, 1, "Terran Science Vessel", 22, 3, 32, 100, 100);
                EdgeShapeAt(cp, 1, "Terran Science Vessel", 22, 3, 32, 100, 100)
                # (Line 56) f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100);
                f.SquareShape(cp, 1, "Protoss Dark Archon", 100, 100)
                # (Line 57) KillUnitAt(All, "Terran Science Vessel", "Anywhere", cp);
                # (Line 58) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "Terran Science Vessel", "Anywhere", cp))
                # (Line 59) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 61) f.SkillWait(cp, 80);
            EUDEndIf()
            f.SkillWait(cp, 80)
            # (Line 63) f.loop[cp] += 1;
            _ARRW(f.loop, cp).__iadd__(1)
            # (Line 65) if (f.loop[cp] == 5)
            if EUDIf()(f.loop[cp] == 5):
                # (Line 66) {
                # (Line 67) f.count[cp] += 1;
                _ARRW(f.count, cp).__iadd__(1)
                # (Line 68) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 69) }
                # (Line 70) }
            EUDEndIf()
            # (Line 71) else if (f.count[cp] == 2)
        if EUDElseIf()(f.count[cp] == 2):
            # (Line 72) {
            # (Line 73) if (f.loop[cp] < 6)
            if EUDIf()(f.loop[cp] >= 6, neg=True):
                # (Line 74) {
                # (Line 75) f.EdgeShape(cp, 1, "50 + 1n Battlecruiser", 0, 1 + 2 * f.loop[cp], 50 * f.loop[cp]);
                f.EdgeShape(cp, 1, "50 + 1n Battlecruiser", 0, 1 + 2 * f.loop[cp], 50 * f.loop[cp])
                # (Line 76) f.EdgeShape(cp, 1, "Protoss Dark Archon", 0, 1 + 2 * f.loop[cp], 50 * f.loop[cp]);
                f.EdgeShape(cp, 1, "Protoss Dark Archon", 0, 1 + 2 * f.loop[cp], 50 * f.loop[cp])
                # (Line 77) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp);
                # (Line 78) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp))
                # (Line 79) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 80) if (f.loop[cp] == 5)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 5):
                # (Line 81) {
                # (Line 82) for (var i = 0; i < 3; i++)
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t14():
                        i.__iadd__(1)
                    # (Line 83) {
                    # (Line 84) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 85) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 87) EdgeShapeAt(cp, 1, "40 + 1n Firebat", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "40 + 1n Firebat", 0, 3, 50, x, y)
                    # (Line 88) }
                    # (Line 89) }
                    EUDSetContinuePoint()
                    _t14()
                EUDEndWhile()
                # (Line 90) f.SkillWait(cp, 80);
            EUDEndIf()
            f.SkillWait(cp, 80)
            # (Line 92) f.loop[cp] += 1;
            _ARRW(f.loop, cp).__iadd__(1)
            # (Line 94) if (f.loop[cp] == 15)
            if EUDIf()(f.loop[cp] == 15):
                # (Line 95) {
                # (Line 96) f.count[cp] += 1;
                _ARRW(f.count, cp).__iadd__(1)
                # (Line 97) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 98) }
                # (Line 99) }
            EUDEndIf()
            # (Line 100) else if (f.count[cp] == 3)
        if EUDElseIf()(f.count[cp] == 3):
            # (Line 101) {
            # (Line 102) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp);
            # (Line 104) if (f.loop[cp] < 6)
            DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp))
            if EUDIf()(f.loop[cp] >= 6, neg=True):
                # (Line 105) {
                # (Line 106) var x = -50 - 50 * f.loop[cp];
                x = EUDVariable()
                x << (-50 - 50 * f.loop[cp])
                # (Line 107) var y = 250 - 50 * f.loop[cp];
                y = EUDVariable()
                y << (250 - 50 * f.loop[cp])
                # (Line 109) EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y);
                EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y)
                # (Line 110) EdgeShapeAt(cp, 1, "50 + 1n Tank", 0, 3, 50, x, y);
                EdgeShapeAt(cp, 1, "50 + 1n Tank", 0, 3, 50, x, y)
                # (Line 112) KillUnitAt(All, "50 + 1n Tank", "Anywhere", cp);
                # (Line 114) MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere");
                DoActions(KillUnitAt(All, "50 + 1n Tank", "Anywhere", cp))
                # (Line 115) Order("50 + 1n Battlecruiser", cp, "Anywhere", Attack, f.location[cp]);
                DoActions(MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere"))
                # (Line 116) }
                DoActions(Order("50 + 1n Battlecruiser", cp, "Anywhere", Attack, f.location[cp]))
                # (Line 118) f.SkillWait(cp, 80);
            EUDEndIf()
            f.SkillWait(cp, 80)
            # (Line 120) f.loop[cp] += 1;
            _ARRW(f.loop, cp).__iadd__(1)
            # (Line 122) if (f.loop[cp] == 11)
            if EUDIf()(f.loop[cp] == 11):
                # (Line 123) {
                # (Line 124) f.count[cp] += 1;
                _ARRW(f.count, cp).__iadd__(1)
                # (Line 125) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 126) }
                # (Line 127) }
            EUDEndIf()
            # (Line 128) else if (f.count[cp] == 4)
        if EUDElseIf()(f.count[cp] == 4):
            # (Line 129) {
            # (Line 130) if (f.loop[cp] == 0)
            if EUDIf()(f.loop[cp] == 0):
                # (Line 131) {
                # (Line 132) for (var i = 0; i < 3; i++)
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t22():
                        i.__iadd__(1)
                    # (Line 133) {
                    # (Line 134) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 135) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 137) EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y)
                    # (Line 138) EdgeShapeAt(cp, 1, "40 + 1n Wraith", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "40 + 1n Wraith", 0, 3, 50, x, y)
                    # (Line 139) EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y)
                    # (Line 140) }
                    # (Line 142) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp);
                    EUDSetContinuePoint()
                    _t22()
                EUDEndWhile()
                # (Line 143) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp))
                # (Line 145) MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere");
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 146) MoveUnit(All, "40 + 1n Wraith", cp, "[Skill]Unit_Wait_ALL", f.location[cp]);
                DoActions(MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere"))
                # (Line 147) Order("40 + 1n Wraith", cp, "Anywhere", Attack, f.location[cp]);
                DoActions(MoveUnit(All, "40 + 1n Wraith", cp, "[Skill]Unit_Wait_ALL", f.location[cp]))
                # (Line 148) MoveUnit(All, "40 + 1n Firebat", cp, "[Skill]Unit_Wait_ALL", f.location[cp]);
                DoActions(Order("40 + 1n Wraith", cp, "Anywhere", Attack, f.location[cp]))
                # (Line 149) Order("40 + 1n Firebat", cp, "Anywhere", Attack, f.location[cp]);
                DoActions(MoveUnit(All, "40 + 1n Firebat", cp, "[Skill]Unit_Wait_ALL", f.location[cp]))
                # (Line 150) }
                DoActions(Order("40 + 1n Firebat", cp, "Anywhere", Attack, f.location[cp]))
                # (Line 151) if (f.loop[cp] == 2)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 2):
                # (Line 152) {
                # (Line 153) for (var i = 0; i < 3; i++)
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t25():
                        i.__iadd__(1)
                    # (Line 154) {
                    # (Line 155) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 156) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 158) EdgeShapeAt(cp, 1, "80 + 1n Tom Kazansky", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "80 + 1n Tom Kazansky", 0, 3, 50, x, y)
                    # (Line 159) EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y)
                    # (Line 160) }
                    # (Line 161) MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere");
                    EUDSetContinuePoint()
                    _t25()
                EUDEndWhile()
                # (Line 162) Order("80 + 1n Tom Kazansky", cp, "Anywhere", Attack, f.location[cp]);
                DoActions(MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere"))
                # (Line 163) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(Order("80 + 1n Tom Kazansky", cp, "Anywhere", Attack, f.location[cp]))
                # (Line 164) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 165) if (f.loop[cp] == 4)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 4):
                # (Line 166) {
                # (Line 167) RemoveUnitAt(All, "80 + 1n Tom Kazansky", "Anywhere", cp);
                # (Line 168) for (var i = 0; i < 3; i++)
                DoActions(RemoveUnitAt(All, "80 + 1n Tom Kazansky", "Anywhere", cp))
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t28():
                        i.__iadd__(1)
                    # (Line 169) {
                    # (Line 170) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 171) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 173) EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "50 + 1n Battlecruiser", 0, 3, 50, x, y)
                    # (Line 174) EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y)
                    # (Line 175) }
                    # (Line 177) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp);
                    EUDSetContinuePoint()
                    _t28()
                EUDEndWhile()
                # (Line 178) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", cp))
                # (Line 179) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 180) if (f.loop[cp] == 6)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 6):
                # (Line 181) {
                # (Line 182) for (var i = 0; i < 3; i++)
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t31():
                        i.__iadd__(1)
                    # (Line 183) {
                    # (Line 184) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 185) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 187) EdgeShapeAt(cp, 1, "80 + 1n Tom Kazansky", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "80 + 1n Tom Kazansky", 0, 3, 50, x, y)
                    # (Line 188) EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "Protoss Dark Archon", 0, 3, 50, x, y)
                    # (Line 189) }
                    # (Line 190) MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere");
                    EUDSetContinuePoint()
                    _t31()
                EUDEndWhile()
                # (Line 191) Order("80 + 1n Tom Kazansky", cp, "Anywhere", Attack, f.location[cp]);
                DoActions(MoveLocation(f.location[cp], f.heroID[cp], cp, "Anywhere"))
                # (Line 192) KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp);
                DoActions(Order("80 + 1n Tom Kazansky", cp, "Anywhere", Attack, f.location[cp]))
                # (Line 193) }
                DoActions(KillUnitAt(All, "Protoss Dark Archon", "Anywhere", cp))
                # (Line 194) if (f.loop[cp] == 8)
            EUDEndIf()
            if EUDIf()(f.loop[cp] == 8):
                # (Line 195) {
                # (Line 196) RemoveUnitAt(All, "80 + 1n Tom Kazansky", "Anywhere", cp);
                # (Line 197) for (var i = 0; i < 3; i++)
                DoActions(RemoveUnitAt(All, "80 + 1n Tom Kazansky", "Anywhere", cp))
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t34():
                        i.__iadd__(1)
                    # (Line 198) {
                    # (Line 199) var x = -0 - 100 * i;
                    x = EUDVariable()
                    x << (-0 - 100 * i)
                    # (Line 200) var y = 300 - 100 * i;
                    y = EUDVariable()
                    y << (300 - 100 * i)
                    # (Line 202) EdgeShapeAt(cp, 1, "Terran Science Vessel", 0, 3, 50, x, y);
                    EdgeShapeAt(cp, 1, "Terran Science Vessel", 0, 3, 50, x, y)
                    # (Line 203) }
                    # (Line 204) KillUnitAt(All, "Terran Science Vessel", "Anywhere", cp);
                    EUDSetContinuePoint()
                    _t34()
                EUDEndWhile()
                # (Line 205) }
                DoActions(KillUnitAt(All, "Terran Science Vessel", "Anywhere", cp))
                # (Line 208) f.SkillWait(cp, 80);
            EUDEndIf()
            f.SkillWait(cp, 80)
            # (Line 210) f.loop[cp] += 1;
            _ARRW(f.loop, cp).__iadd__(1)
            # (Line 212) if (f.loop[cp] == 100)
            if EUDIf()(f.loop[cp] == 100):
                # (Line 213) {
                # (Line 214) SetSwitch("ComputerAlliy", Clear);
                # (Line 216) if (cp < 3)
                DoActions(SetSwitch("ComputerAlliy", Clear))
                if EUDIf()(cp >= 3, neg=True):
                    # (Line 217) {
                    # (Line 218) SetAllianceStatus(P4, Enemy);
                    # (Line 219) SetAllianceStatus(P5, Enemy);
                    DoActions(SetAllianceStatus(P4, Enemy))
                    # (Line 220) SetAllianceStatus(P6, Enemy);
                    DoActions(SetAllianceStatus(P5, Enemy))
                    # (Line 221) }
                    DoActions(SetAllianceStatus(P6, Enemy))
                    # (Line 222) else if (cp >= 3)
                if EUDElseIf()(cp >= 3):
                    # (Line 223) {
                    # (Line 224) SetAllianceStatus(P1, Enemy);
                    # (Line 225) SetAllianceStatus(P2, Enemy);
                    DoActions(SetAllianceStatus(P1, Enemy))
                    # (Line 226) SetAllianceStatus(P3, Enemy);
                    DoActions(SetAllianceStatus(P2, Enemy))
                    # (Line 227) }
                    DoActions(SetAllianceStatus(P3, Enemy))
                    # (Line 229) f.count[cp] += 1;
                EUDEndIf()
                _ARRW(f.count, cp).__iadd__(1)
                # (Line 230) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 231) }
                # (Line 232) }
            EUDEndIf()
            # (Line 233) else if (f.count[cp] == 5)
        if EUDElseIf()(f.count[cp] == 5):
            # (Line 234) {
            # (Line 235) if (Bring(cp, AtLeast, 20, "Protoss Arbiter", "[Skill]UseSkill")
            _t39 = EUDIf()
            # (Line 236) && Deaths(cp, AtLeast, f.UltimateB[cp], " `UltimateCoolTime"))
            if _t39(EUDSCAnd()(Bring(cp, AtLeast, 20, "Protoss Arbiter", "[Skill]UseSkill"))(Deaths(cp, AtLeast, f.UltimateB[cp], " `UltimateCoolTime"))()):
                # (Line 237) {
                # (Line 238) f.Voice_Routine(cp, 3);
                f.Voice_Routine(cp, 3)
                # (Line 239) f.wait[cp] = 0;
                _ARRW(f.wait, cp) << (0)
                # (Line 240) f.count[cp] = 0;
                _ARRW(f.count, cp) << (0)
                # (Line 241) f.loop[cp] = 0;
                _ARRW(f.loop, cp) << (0)
                # (Line 242) f.step[cp] = 0;
                _ARRW(f.step, cp) << (0)
                # (Line 243) SetDeaths(cp, Subtract, f.UltimateB[cp], " `UltimateCoolTime");
                # (Line 244) KillUnitAt(20, "Protoss Arbiter", "[Skill]UseSkill", cp);
                DoActions(SetDeaths(cp, Subtract, f.UltimateB[cp], " `UltimateCoolTime"))
                # (Line 245) }
                DoActions(KillUnitAt(20, "Protoss Arbiter", "[Skill]UseSkill", cp))
                # (Line 246) else
                # (Line 247) {
            if EUDElse()():
                # (Line 248) KillUnitAt(All, "40 + 1n Firebat", "Anywhere", cp);
                # (Line 249) KillUnitAt(All, "40 + 1n Wraith", "Anywhere", cp);
                DoActions(KillUnitAt(All, "40 + 1n Firebat", "Anywhere", cp))
                # (Line 250) SetSwitch("UiltimateSwitch", Clear);
                DoActions(KillUnitAt(All, "40 + 1n Wraith", "Anywhere", cp))
                # (Line 251) f.SkillEnd(cp);
                DoActions(SetSwitch("UiltimateSwitch", Clear))
                f.SkillEnd(cp)
                # (Line 252) }
                # (Line 253) }
            EUDEndIf()
            # (Line 254) }
        EUDEndIf()
        # (Line 255) }
    EUDEndIf()
    # (Line 257) function EdgeShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y)

# (Line 258) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def EdgeShapeAt(cp, count, Unit, degree, n, interval, x, y):
    # (Line 259) f.EdgeShapeAt(cp, count, Unit, degree, n, interval, x, y);
    f.EdgeShapeAt(cp, count, Unit, degree, n, interval, x, y)
    # (Line 260) f.EdgeShapeAt(cp, count, Unit, degree, n, interval, -x, -y);
    f.EdgeShapeAt(cp, count, Unit, degree, n, interval, -x, -y)
    # (Line 261) f.EdgeShapeAt(cp, count, Unit, degree, n, interval, -y, x);
    f.EdgeShapeAt(cp, count, Unit, degree, n, interval, -y, x)
    # (Line 262) f.EdgeShapeAt(cp, count, Unit, degree, n, interval, y, -x);
    f.EdgeShapeAt(cp, count, Unit, degree, n, interval, y, -x)
    # (Line 264) f.DotShape(cp, count, Unit, x, y);
    f.DotShape(cp, count, Unit, x, y)
    # (Line 265) f.DotShape(cp, count, Unit, -x, -y);
    f.DotShape(cp, count, Unit, -x, -y)
    # (Line 266) f.DotShape(cp, count, Unit, -y, x);
    f.DotShape(cp, count, Unit, -y, x)
    # (Line 267) f.DotShape(cp, count, Unit, y, -x);
    f.DotShape(cp, count, Unit, y, -x)
    # (Line 268) }
