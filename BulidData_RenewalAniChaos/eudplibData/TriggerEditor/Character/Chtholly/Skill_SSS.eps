import PluginVariables as msqcvar;
import func.shape as s;

function main(playerID)
{
	if (v.P_Delay[playerID] == 0)
	{
		if (v.P_CountMain[playerID] < 4)
		{
			KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
		
			s.Shape_Square(playerID, 1, "40 + 1n Mojo", 16 * v.P_CountMain[playerID] + 32, 16 * v.P_CountMain[playerID] + 32);
			s.Shape_Square(playerID, 1, "40 + 1n Zealot", 16 * v.P_CountMain[playerID] + 32, 16 * v.P_CountMain[playerID] + 32);
			MoveLocation(v.P_LocationID[playerID], v.P_CharacterID[playerID], playerID, "Anywhere");
			KillUnitAt(All, "40 + 1n Zealot", "Anywhere", playerID);
			Order("40 + 1n Mojo", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
			s.SkillWait(150);
			v.P_CountMain[playerID] += 1;
		}
		else if (v.P_CountMain[playerID] == 4)
		{
			KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
			s.SkillWait(0);
			v.P_CountMain[playerID] += 1;
		}
		else if (v.P_CountMain[playerID] == 5 || v.P_CountMain[playerID] == 6)
		{
			KillUnitAt(All, "40 + 1n Marine", "Anywhere", playerID);
			
			s.Shape_Square(playerID, 1, "40 + 1n Mojo", 116 - 32 * (v.P_CountMain[playerID] - 5), 0);
			s.Shape_Square(playerID, 1, "40 + 1n Marine", 116 - 32 * (v.P_CountMain[playerID] - 5), 0);
			MoveLocation(v.P_LocationID[playerID], v.P_CharacterID[playerID], playerID, "Anywhere");
			KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
			Order("40 + 1n Marine", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
			s.SkillWait(500);
			v.P_CountMain[playerID] += 1;
		}
		else if (v.P_CountMain[playerID] == 7)
		{
			KillUnitAt(All, "40 + 1n Marine", "Anywhere", playerID);
			
			s.Shape_Square(playerID, 1, "50 + 1n Tank", 90, 90);
			s.Shape_Square(playerID, 1, "50 + 1n Tank", 128, 0);
			MoveLocation(v.P_LocationID[playerID], v.P_CharacterID[playerID], playerID, "Anywhere");
			Order("50 + 1n Tank", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
			s.SkillWait(740);
			v.P_CountMain[playerID] += 1;
		}
		else if (v.P_CountMain[playerID] == 8)
		{
			KillUnitAt(All, "50 + 1n Tank", "Anywhere", playerID);
			s.SkillEnd();
		}
	}
}