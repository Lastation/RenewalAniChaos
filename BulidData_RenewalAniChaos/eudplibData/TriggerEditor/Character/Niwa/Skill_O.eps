import Function as f;

function main(cp)
{
   if (f.delay[cp] == 0)
   {
      if (f.count[cp] == 0)
      {
         if (f.loop[cp] < 11)
         {
            f.DotShape(cp, 1, "40 + 1n Gantrithor", 0, 0);
            KillUnitAt(All, "40 + 1n Gantrithor", "Anywhere", cp);
         }
         if (f.loop[cp] == 11)
         {
            f.DotShape(cp, 1, "Target", 0, 0);
            f.DotShape(cp, 1, "60 + 1n Archon", 0, 0);
            KillUnitAt(All, "Target", "Anywhere", cp);
            KillUnitAt(All, "60 + 1n Archon", "Anywhere", cp);
         }
         if (f.loop[cp] == 12)
         {
            f.DotShape(cp, 1, "Kakaru (Twilight)", 0, 0);
            KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", cp);
         }

         if (f.loop[cp] == 25)
         {
            f.NxNSquareShape(cp, 1, "40 + 1n Gantrithor", 3, 100);
            f.NxNSquareShape(cp, 1, "60 + 1n Archon", 3, 100);
            KillUnitAt(All, "40 + 1n Gantrithor", "Anywhere", cp);
            KillUnitAt(All, "60 + 1n Archon", "Anywhere", cp);
         }


         f.SkillWait(cp, 80);

         f.loop[cp] += 1;

         if (f.loop[cp] == 26)
         {
            f.count[cp] += 1;
            f.loop[cp] = 0;         
         }
      }
      else if (f.count[cp] == 1)
      {
         SetDeaths(cp, SetTo, 1440, " `UniqueCoolTime");
         SetDeaths(cp, SetTo, 240, " `UniqueSkill");
         f.SkillEnd(cp);
      }
   }
}