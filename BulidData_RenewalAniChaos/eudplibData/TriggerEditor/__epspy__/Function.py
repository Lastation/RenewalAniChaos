## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import py_math;
import math
# (Line 2) import Variable as v;
import Variable as v
# (Line 4) const step		= PVariable();			// SkillStep
step = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 5) const count 		= PVariable();			// SkillCount
count = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 6) const loop 		= PVariable();			// SkillLoop
loop = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 7) const delay		= PVariable();			// wait(value)
delay = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 8) const wait		= PVariable();			// SkillWait
wait = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 10) const INSERT_KEY = PVariable();
INSERT_KEY = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 12) const loopB 		= PVariable();			// SkillLoop2
loopB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 13) const loopC 	= PVariable();			// SkillLoop3
loopC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 14) const delayB	= PVariable();			// wait(value)2
delayB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 15) const delayC	= PVariable();			// wait(value)3
delayC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 17) const location 	= PVariable();			// 로케이션 번호
location = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 18) const heroID 	= PVariable();			// 영웅 유닛 번호
heroID = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 20) const UltimateA	= PVariable();			// 궁극기 1번 소모량
UltimateA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 21) const UltimateB	= PVariable();			// 궁극기 2번 소모량
UltimateB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 22) const UltimateC	= PVariable();			// 궁극기 3번 소모량
UltimateC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 24) const UniqueA	= PVariable();			// 유니크 1번 쿨타임
UniqueA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 26) const distance	= PVariable();			// 거리 관련 변수
distance = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 27) const SinAngle 	= PVariable();			// Sin 변수
SinAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 28) const CosAngle 	= PVariable();			// Cos 변수
CosAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 30) const stb		= StringBuffer();
stb = _CGFW(lambda: [StringBuffer()], 1)[0]
# (Line 32) const HoldLocation = [97, 119, 122, 126, 134, 143, 0, 0];
HoldLocation = _CGFW(lambda: [_ARR(FlattenList([97, 119, 122, 126, 134, 143, 0, 0]))], 1)[0]
# (Line 34) const CosTableArray = EUDArray(py_eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"));
CosTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 35) const SinTableArray 	= EUDArray(py_eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"));
SinTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 37) const Rusaruka_Voice 	= PVariable();	// 1
Rusaruka_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 38) const Chtholly_Voice 	= PVariable();	// 2
Chtholly_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 39) const Kiana_Voice 		= PVariable();	// 17
Kiana_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 40) const Yuuna_Voice 		= PVariable();	// 22
Yuuna_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 41) const Mayuri_Voice 		= PVariable();	// 23
Mayuri_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 42) const Nanami_Voice 		= PVariable();	// 24
Nanami_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 43) const Milim_Voice 		= PVariable();	// 25
Milim_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 44) const Yume_Voice 		= PVariable();	// 26
Yume_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 45) const Magane_Voice 	= PVariable();	// 27
Magane_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 46) const Oda_Voice 		= PVariable();	// 28
Oda_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 47) const Ekidona_Voice 	= PVariable();	// 29
Ekidona_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 48) const Niwa_Voice 		= PVariable();	// 30
Niwa_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 49) const Park_Voice 		= PVariable();	// 31
Park_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 51) const p_burrowed = UnitProperty(burrowed = true);
p_burrowed = _CGFW(lambda: [UnitProperty(burrowed=True)], 1)[0]
# (Line 52) const p_hallucinated = UnitProperty(hallucinated = true);
p_hallucinated = _CGFW(lambda: [UnitProperty(hallucinated=True)], 1)[0]
# (Line 54) const ally				= PVariable();
ally = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 56) function Table_Sin(cp, degree, distance);
# (Line 57) function Table_Cos(cp, degree, distance);
# (Line 59) function Voice_Routine(cp, num)
# (Line 60) {
@EUDFunc
def Voice_Routine(cp, num):
    # (Line 61) switch(v.Hero_Num[cp])
    EUDSwitch(v.Hero_Num[cp])
    # (Line 62) {
    # (Line 63) case 1:
    _t1 = EUDSwitchCase()
    # (Line 64) Rusaruka_Voice[0] = num;
    if _t1(1):
        _ARRW(Rusaruka_Voice, 0) << (num)
        # (Line 65) Rusaruka_Voice[1] = num;
        _ARRW(Rusaruka_Voice, 1) << (num)
        # (Line 66) Rusaruka_Voice[2] = num;
        _ARRW(Rusaruka_Voice, 2) << (num)
        # (Line 67) Rusaruka_Voice[3] = num;
        _ARRW(Rusaruka_Voice, 3) << (num)
        # (Line 68) Rusaruka_Voice[4] = num;
        _ARRW(Rusaruka_Voice, 4) << (num)
        # (Line 69) Rusaruka_Voice[5] = num;
        _ARRW(Rusaruka_Voice, 5) << (num)
        # (Line 70) break;
        EUDBreak()
        # (Line 71) case 2:
    _t2 = EUDSwitchCase()
    # (Line 72) Chtholly_Voice[0] = num;
    if _t2(2):
        _ARRW(Chtholly_Voice, 0) << (num)
        # (Line 73) Chtholly_Voice[1] = num;
        _ARRW(Chtholly_Voice, 1) << (num)
        # (Line 74) Chtholly_Voice[2] = num;
        _ARRW(Chtholly_Voice, 2) << (num)
        # (Line 75) Chtholly_Voice[3] = num;
        _ARRW(Chtholly_Voice, 3) << (num)
        # (Line 76) Chtholly_Voice[4] = num;
        _ARRW(Chtholly_Voice, 4) << (num)
        # (Line 77) Chtholly_Voice[5] = num;
        _ARRW(Chtholly_Voice, 5) << (num)
        # (Line 78) break;
        EUDBreak()
        # (Line 79) case 17:
    _t3 = EUDSwitchCase()
    # (Line 80) Kiana_Voice[0] = num;
    if _t3(17):
        _ARRW(Kiana_Voice, 0) << (num)
        # (Line 81) Kiana_Voice[1] = num;
        _ARRW(Kiana_Voice, 1) << (num)
        # (Line 82) Kiana_Voice[2] = num;
        _ARRW(Kiana_Voice, 2) << (num)
        # (Line 83) Kiana_Voice[3] = num;
        _ARRW(Kiana_Voice, 3) << (num)
        # (Line 84) Kiana_Voice[4] = num;
        _ARRW(Kiana_Voice, 4) << (num)
        # (Line 85) Kiana_Voice[5] = num;
        _ARRW(Kiana_Voice, 5) << (num)
        # (Line 86) break;
        EUDBreak()
        # (Line 87) case 22:
    _t4 = EUDSwitchCase()
    # (Line 88) Yuuna_Voice[0] = num;
    if _t4(22):
        _ARRW(Yuuna_Voice, 0) << (num)
        # (Line 89) Yuuna_Voice[1] = num;
        _ARRW(Yuuna_Voice, 1) << (num)
        # (Line 90) Yuuna_Voice[2] = num;
        _ARRW(Yuuna_Voice, 2) << (num)
        # (Line 91) Yuuna_Voice[3] = num;
        _ARRW(Yuuna_Voice, 3) << (num)
        # (Line 92) Yuuna_Voice[4] = num;
        _ARRW(Yuuna_Voice, 4) << (num)
        # (Line 93) Yuuna_Voice[5] = num;
        _ARRW(Yuuna_Voice, 5) << (num)
        # (Line 94) break;
        EUDBreak()
        # (Line 95) case 23:
    _t5 = EUDSwitchCase()
    # (Line 96) Mayuri_Voice[0] = num;
    if _t5(23):
        _ARRW(Mayuri_Voice, 0) << (num)
        # (Line 97) Mayuri_Voice[1] = num;
        _ARRW(Mayuri_Voice, 1) << (num)
        # (Line 98) Mayuri_Voice[2] = num;
        _ARRW(Mayuri_Voice, 2) << (num)
        # (Line 99) Mayuri_Voice[3] = num;
        _ARRW(Mayuri_Voice, 3) << (num)
        # (Line 100) Mayuri_Voice[4] = num;
        _ARRW(Mayuri_Voice, 4) << (num)
        # (Line 101) Mayuri_Voice[5] = num;
        _ARRW(Mayuri_Voice, 5) << (num)
        # (Line 102) break;
        EUDBreak()
        # (Line 103) case 24:
    _t6 = EUDSwitchCase()
    # (Line 104) Nanami_Voice[0] = num;
    if _t6(24):
        _ARRW(Nanami_Voice, 0) << (num)
        # (Line 105) Nanami_Voice[1] = num;
        _ARRW(Nanami_Voice, 1) << (num)
        # (Line 106) Nanami_Voice[2] = num;
        _ARRW(Nanami_Voice, 2) << (num)
        # (Line 107) Nanami_Voice[3] = num;
        _ARRW(Nanami_Voice, 3) << (num)
        # (Line 108) Nanami_Voice[4] = num;
        _ARRW(Nanami_Voice, 4) << (num)
        # (Line 109) Nanami_Voice[5] = num;
        _ARRW(Nanami_Voice, 5) << (num)
        # (Line 110) break;
        EUDBreak()
        # (Line 111) case 25:
    _t7 = EUDSwitchCase()
    # (Line 112) Milim_Voice[0] = num;
    if _t7(25):
        _ARRW(Milim_Voice, 0) << (num)
        # (Line 113) Milim_Voice[1] = num;
        _ARRW(Milim_Voice, 1) << (num)
        # (Line 114) Milim_Voice[2] = num;
        _ARRW(Milim_Voice, 2) << (num)
        # (Line 115) Milim_Voice[3] = num;
        _ARRW(Milim_Voice, 3) << (num)
        # (Line 116) Milim_Voice[4] = num;
        _ARRW(Milim_Voice, 4) << (num)
        # (Line 117) Milim_Voice[5] = num;
        _ARRW(Milim_Voice, 5) << (num)
        # (Line 118) break;
        EUDBreak()
        # (Line 119) case 26:
    _t8 = EUDSwitchCase()
    # (Line 120) Yume_Voice[0] = num;
    if _t8(26):
        _ARRW(Yume_Voice, 0) << (num)
        # (Line 121) Yume_Voice[1] = num;
        _ARRW(Yume_Voice, 1) << (num)
        # (Line 122) Yume_Voice[2] = num;
        _ARRW(Yume_Voice, 2) << (num)
        # (Line 123) Yume_Voice[3] = num;
        _ARRW(Yume_Voice, 3) << (num)
        # (Line 124) Yume_Voice[4] = num;
        _ARRW(Yume_Voice, 4) << (num)
        # (Line 125) Yume_Voice[5] = num;
        _ARRW(Yume_Voice, 5) << (num)
        # (Line 126) break;
        EUDBreak()
        # (Line 127) case 27:
    _t9 = EUDSwitchCase()
    # (Line 128) Magane_Voice[0] = num;
    if _t9(27):
        _ARRW(Magane_Voice, 0) << (num)
        # (Line 129) Magane_Voice[1] = num;
        _ARRW(Magane_Voice, 1) << (num)
        # (Line 130) Magane_Voice[2] = num;
        _ARRW(Magane_Voice, 2) << (num)
        # (Line 131) Magane_Voice[3] = num;
        _ARRW(Magane_Voice, 3) << (num)
        # (Line 132) Magane_Voice[4] = num;
        _ARRW(Magane_Voice, 4) << (num)
        # (Line 133) Magane_Voice[5] = num;
        _ARRW(Magane_Voice, 5) << (num)
        # (Line 134) break;
        EUDBreak()
        # (Line 135) case 28:
    _t10 = EUDSwitchCase()
    # (Line 136) Oda_Voice[0] = num;
    if _t10(28):
        _ARRW(Oda_Voice, 0) << (num)
        # (Line 137) Oda_Voice[1] = num;
        _ARRW(Oda_Voice, 1) << (num)
        # (Line 138) Oda_Voice[2] = num;
        _ARRW(Oda_Voice, 2) << (num)
        # (Line 139) Oda_Voice[3] = num;
        _ARRW(Oda_Voice, 3) << (num)
        # (Line 140) Oda_Voice[4] = num;
        _ARRW(Oda_Voice, 4) << (num)
        # (Line 141) Oda_Voice[5] = num;
        _ARRW(Oda_Voice, 5) << (num)
        # (Line 142) break;
        EUDBreak()
        # (Line 143) case 29:
    _t11 = EUDSwitchCase()
    # (Line 144) Ekidona_Voice[0] = num;
    if _t11(29):
        _ARRW(Ekidona_Voice, 0) << (num)
        # (Line 145) Ekidona_Voice[1] = num;
        _ARRW(Ekidona_Voice, 1) << (num)
        # (Line 146) Ekidona_Voice[2] = num;
        _ARRW(Ekidona_Voice, 2) << (num)
        # (Line 147) Ekidona_Voice[3] = num;
        _ARRW(Ekidona_Voice, 3) << (num)
        # (Line 148) Ekidona_Voice[4] = num;
        _ARRW(Ekidona_Voice, 4) << (num)
        # (Line 149) Ekidona_Voice[5] = num;
        _ARRW(Ekidona_Voice, 5) << (num)
        # (Line 150) break;
        EUDBreak()
        # (Line 151) case 30:
    _t12 = EUDSwitchCase()
    # (Line 152) Niwa_Voice[0] = num;
    if _t12(30):
        _ARRW(Niwa_Voice, 0) << (num)
        # (Line 153) Niwa_Voice[1] = num;
        _ARRW(Niwa_Voice, 1) << (num)
        # (Line 154) Niwa_Voice[2] = num;
        _ARRW(Niwa_Voice, 2) << (num)
        # (Line 155) Niwa_Voice[3] = num;
        _ARRW(Niwa_Voice, 3) << (num)
        # (Line 156) Niwa_Voice[4] = num;
        _ARRW(Niwa_Voice, 4) << (num)
        # (Line 157) Niwa_Voice[5] = num;
        _ARRW(Niwa_Voice, 5) << (num)
        # (Line 158) break;
        EUDBreak()
        # (Line 159) case 31:
    _t13 = EUDSwitchCase()
    # (Line 160) Park_Voice[0] = num;
    if _t13(31):
        _ARRW(Park_Voice, 0) << (num)
        # (Line 161) Park_Voice[1] = num;
        _ARRW(Park_Voice, 1) << (num)
        # (Line 162) Park_Voice[2] = num;
        _ARRW(Park_Voice, 2) << (num)
        # (Line 163) Park_Voice[3] = num;
        _ARRW(Park_Voice, 3) << (num)
        # (Line 164) Park_Voice[4] = num;
        _ARRW(Park_Voice, 4) << (num)
        # (Line 165) Park_Voice[5] = num;
        _ARRW(Park_Voice, 5) << (num)
        # (Line 166) break;
        EUDBreak()
        # (Line 168) }
    # (Line 169) }
    EUDEndSwitch()
    # (Line 171) function SetHeroID(cp)

# (Line 172) {
@EUDFunc
def SetHeroID(cp):
    # (Line 173) switch(v.Hero_Num[cp])
    EUDSwitch(v.Hero_Num[cp])
    # (Line 174) {
    # (Line 175) case 3:
    _t1 = EUDSwitchCase()
    # (Line 176) heroID[cp] = 74;
    if _t1(3):
        _ARRW(heroID, cp) << (74)
        # (Line 177) break;
        EUDBreak()
        # (Line 178) case 4:
    _t2 = EUDSwitchCase()
    # (Line 179) heroID[cp] = 74;
    if _t2(4):
        _ARRW(heroID, cp) << (74)
        # (Line 180) break;
        EUDBreak()
        # (Line 181) case 5:
    _t3 = EUDSwitchCase()
    # (Line 182) heroID[cp] = 74;
    if _t3(5):
        _ARRW(heroID, cp) << (74)
        # (Line 183) break;
        EUDBreak()
        # (Line 184) case 6:
    _t4 = EUDSwitchCase()
    # (Line 185) heroID[cp] = 16;
    if _t4(6):
        _ARRW(heroID, cp) << (16)
        # (Line 186) break;
        EUDBreak()
        # (Line 187) case 7:
    _t5 = EUDSwitchCase()
    # (Line 188) heroID[cp] = 77;
    if _t5(7):
        _ARRW(heroID, cp) << (77)
        # (Line 189) break;
        EUDBreak()
        # (Line 190) case 8:
    _t6 = EUDSwitchCase()
    # (Line 191) heroID[cp] = 51;
    if _t6(8):
        _ARRW(heroID, cp) << (51)
        # (Line 192) break;
        EUDBreak()
        # (Line 193) case 9:
    _t7 = EUDSwitchCase()
    # (Line 194) heroID[cp] = 51;
    if _t7(9):
        _ARRW(heroID, cp) << (51)
        # (Line 195) break;
        EUDBreak()
        # (Line 196) case 10:
    _t8 = EUDSwitchCase()
    # (Line 197) heroID[cp] = 77;
    if _t8(10):
        _ARRW(heroID, cp) << (77)
        # (Line 198) break;
        EUDBreak()
        # (Line 199) case 11:
    _t9 = EUDSwitchCase()
    # (Line 200) heroID[cp] = 16;
    if _t9(11):
        _ARRW(heroID, cp) << (16)
        # (Line 201) break;
        EUDBreak()
        # (Line 202) case 12:
    _t10 = EUDSwitchCase()
    # (Line 203) heroID[cp] = 54;
    if _t10(12):
        _ARRW(heroID, cp) << (54)
        # (Line 204) break;
        EUDBreak()
        # (Line 205) case 13:
    _t11 = EUDSwitchCase()
    # (Line 206) heroID[cp] = 16;
    if _t11(13):
        _ARRW(heroID, cp) << (16)
        # (Line 207) break;
        EUDBreak()
        # (Line 208) case 14:
    _t12 = EUDSwitchCase()
    # (Line 209) heroID[cp] = 99;
    if _t12(14):
        _ARRW(heroID, cp) << (99)
        # (Line 210) break;
        EUDBreak()
        # (Line 211) case 15:
    _t13 = EUDSwitchCase()
    # (Line 212) heroID[cp] = 54;
    if _t13(15):
        _ARRW(heroID, cp) << (54)
        # (Line 213) break;
        EUDBreak()
        # (Line 214) case 16:
    _t14 = EUDSwitchCase()
    # (Line 215) heroID[cp] = 16;
    if _t14(16):
        _ARRW(heroID, cp) << (16)
        # (Line 216) break;
        EUDBreak()
        # (Line 217) case 17:
    _t15 = EUDSwitchCase()
    # (Line 218) heroID[cp] = 77;
    if _t15(17):
        _ARRW(heroID, cp) << (77)
        # (Line 219) break;
        EUDBreak()
        # (Line 220) case 18:
    _t16 = EUDSwitchCase()
    # (Line 221) heroID[cp] = 99;
    if _t16(18):
        _ARRW(heroID, cp) << (99)
        # (Line 222) break;
        EUDBreak()
        # (Line 223) case 19:
    _t17 = EUDSwitchCase()
    # (Line 224) heroID[cp] = 51;
    if _t17(19):
        _ARRW(heroID, cp) << (51)
        # (Line 225) break;
        EUDBreak()
        # (Line 226) case 20:
    _t18 = EUDSwitchCase()
    # (Line 227) heroID[cp] = 99;
    if _t18(20):
        _ARRW(heroID, cp) << (99)
        # (Line 228) break;
        EUDBreak()
        # (Line 229) case 21:
    _t19 = EUDSwitchCase()
    # (Line 230) heroID[cp] = 64;
    if _t19(21):
        _ARRW(heroID, cp) << (64)
        # (Line 231) break;
        EUDBreak()
        # (Line 232) }
    # (Line 233) }
    EUDEndSwitch()
    # (Line 235) function ShieldFix(cp : TrgPlayer, amount)

# (Line 236) {
@EUDTypedFunc([TrgPlayer, None])
def ShieldFix(cp, amount):
    # (Line 237) SetDeaths(cp, SetTo, 1, " `ShieldRecharge");
    # (Line 238) v.Shield[cp] = amount;
    DoActions(SetDeaths(cp, SetTo, 1, " `ShieldRecharge"))
    _ARRW(v.Shield, cp) << (amount)
    # (Line 239) }
    # (Line 241) function ComputerAlly(cp : TrgPlayer)

# (Line 242) {
@EUDTypedFunc([TrgPlayer])
def ComputerAlly(cp):
    # (Line 243) ally[cp] = 1;
    _ARRW(ally, cp) << (1)
    # (Line 244) SetSwitch("ComputerAlliy", Set);
    # (Line 245) SetAllianceStatus(P7, Ally);
    DoActions(SetSwitch("ComputerAlliy", Set))
    # (Line 246) SetAllianceStatus(P8, Ally);
    DoActions(SetAllianceStatus(P7, Ally))
    # (Line 247) }
    DoActions(SetAllianceStatus(P8, Ally))
    # (Line 249) function MoveLoc(Unit : TrgUnit, cp : TrgPlayer, x, y)

# (Line 250) {
@EUDTypedFunc([TrgUnit, TrgPlayer, None, None])
def MoveLoc(Unit, cp, x, y):
    # (Line 251) MoveLocation(location[cp], Unit, cp, "Anywhere");
    # (Line 252) addloc(location[cp], x, y);
    DoActions(MoveLocation(location[cp], Unit, cp, "Anywhere"))
    f_addloc(location[cp], x, y)
    # (Line 253) }
    # (Line 255) function SkillUnitWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, property)

# (Line 256) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None])
def SkillUnitWithProperty(cp, count_1, Unit, property):
    # (Line 257) switch (property)
    EUDSwitch(property)
    # (Line 258) {
    # (Line 259) case 0:
    _t1 = EUDSwitchCase()
    # (Line 260) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(burrowed = true));
    if _t1(0):
        # (Line 261) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(burrowed=True)))
        EUDBreak()
        # (Line 262) case 1:
    _t2 = EUDSwitchCase()
    # (Line 263) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(hallucinated = true));
    if _t2(1):
        # (Line 264) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(hallucinated=True)))
        EUDBreak()
        # (Line 265) case 2:
    _t3 = EUDSwitchCase()
    # (Line 266) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(hallucinated = true, burrowed = true));
    if _t3(2):
        # (Line 267) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(hallucinated=True, burrowed=True)))
        EUDBreak()
        # (Line 269) }
    # (Line 271) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    EUDEndSwitch()
    # (Line 272) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 273) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]))
    # (Line 275) function SkillUnit(cp : TrgPlayer, count, Unit : TrgUnit)

# (Line 276) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit])
def SkillUnit(cp, count_1, Unit):
    # (Line 277) CreateUnit(count, Unit, dwrand() % 8 + 33, cp);
    # (Line 278) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(count_1, Unit, f_dwrand() % 8 + 33, cp))
    # (Line 279) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 280) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]))
    # (Line 282) function BanReturn(cp)

# (Line 283) {
@EUDFunc
def BanReturn(cp):
    # (Line 284) KillUnit("Protoss Observer", cp);
    # (Line 285) }
    DoActions(KillUnit("Protoss Observer", cp))
    # (Line 287) function Slow(cp)

# (Line 288) {
@EUDFunc
def Slow(cp):
    # (Line 289) MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere");
    # (Line 290) CreateUnit(1, "Zerg Devourer", "[Skill]Unit_Wait_8", cp);
    DoActions(MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere"))
    # (Line 291) SetInvincibility(Enable, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(1, "Zerg Devourer", "[Skill]Unit_Wait_8", cp))
    # (Line 292) MoveUnit(1, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL", HoldLocation[cp]);
    DoActions(SetInvincibility(Enable, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL"))
    # (Line 293) RemoveUnitAt(1, "Zerg Devourer", "Anywhere", cp);
    DoActions(MoveUnit(1, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL", HoldLocation[cp]))
    # (Line 294) }
    DoActions(RemoveUnitAt(1, "Zerg Devourer", "Anywhere", cp))
    # (Line 296) function HoldPosition(cp)

# (Line 297) {
@EUDFunc
def HoldPosition(cp):
    # (Line 298) Slow(cp);
    Slow(cp)
    # (Line 299) MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere");
    # (Line 300) Order(heroID[cp], cp, "Anywhere", Move, HoldLocation[cp]);
    DoActions(MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere"))
    # (Line 301) }
    DoActions(Order(heroID[cp], cp, "Anywhere", Move, HoldLocation[cp]))
    # (Line 303) function DotShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 304) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def DotShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 305) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 306) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 307) }
    # (Line 309) function DotShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 310) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def DotShape(cp, count_1, Unit, x, y):
    # (Line 311) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 312) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 313) }
    # (Line 315) function SkillWait(cp, count)

# (Line 316) {
@EUDFunc
def SkillWait(cp, count_1):
    # (Line 317) delay[cp] = count/83 + 1;
    _ARRW(delay, cp) << (count_1 // 83 + 1)
    # (Line 318) }
    # (Line 320) function SkillWaitB(cp, count)

# (Line 321) {
@EUDFunc
def SkillWaitB(cp, count_1):
    # (Line 322) delayB[cp] = count/83 + 1;
    _ARRW(delayB, cp) << (count_1 // 83 + 1)
    # (Line 323) }
    # (Line 325) function SkillWaitC(cp, count)

# (Line 326) {
@EUDFunc
def SkillWaitC(cp, count_1):
    # (Line 327) delayC[cp] = count/83 + 1;
    _ARRW(delayC, cp) << (count_1 // 83 + 1)
    # (Line 328) }
    # (Line 330) function SkillEnd(cp)

# (Line 331) {
@EUDFunc
def SkillEnd(cp):
    # (Line 332) count[cp] = 999;
    _ARRW(count, cp) << (999)
    # (Line 334) v.Shield[cp] = 0;
    _ARRW(v.Shield, cp) << (0)
    # (Line 335) SetDeaths(cp, SetTo, 0, " `ShieldRecharge");
    # (Line 337) if (ally[cp] == 1)
    DoActions(SetDeaths(cp, SetTo, 0, " `ShieldRecharge"))
    if EUDIf()(ally[cp] == 1):
        # (Line 338) {
        # (Line 339) SetSwitch("ComputerAlliy", Clear);
        # (Line 340) if (cp < 3)
        DoActions(SetSwitch("ComputerAlliy", Clear))
        if EUDIf()(cp >= 3, neg=True):
            # (Line 341) {
            # (Line 342) SetAllianceStatus(P8, Enemy);
            # (Line 343) }
            DoActions(SetAllianceStatus(P8, Enemy))
            # (Line 344) else if (cp >= 3)
        if EUDElseIf()(cp >= 3):
            # (Line 345) {
            # (Line 346) SetAllianceStatus(P7, Enemy);
            # (Line 347) }
            DoActions(SetAllianceStatus(P7, Enemy))
            # (Line 348) ally[cp] = 0;
        EUDEndIf()
        _ARRW(ally, cp) << (0)
        # (Line 349) }
        # (Line 350) }
    EUDEndIf()
    # (Line 352) function DoubleShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 353) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def DoubleShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 354) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 355) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 356) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 357) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 358) }
    # (Line 360) function DoubleShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 361) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def DoubleShape(cp, count_1, Unit, x, y):
    # (Line 362) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 363) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 364) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 365) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 366) }
    # (Line 368) function SquareShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 369) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def SquareShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 370) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 371) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 372) MoveLoc(heroID[cp], cp, -y, x);
    MoveLoc(heroID[cp], cp, -y, x)
    # (Line 373) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 374) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 375) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 376) MoveLoc(heroID[cp], cp, y, -x);
    MoveLoc(heroID[cp], cp, y, -x)
    # (Line 377) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 378) }
    # (Line 380) function SquareShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 381) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def SquareShape(cp, count_1, Unit, x, y):
    # (Line 382) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 383) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 384) MoveLoc(heroID[cp], cp, -y, x);
    MoveLoc(heroID[cp], cp, -y, x)
    # (Line 385) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 386) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 387) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 388) MoveLoc(heroID[cp], cp, y, -x);
    MoveLoc(heroID[cp], cp, y, -x)
    # (Line 389) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 390) }
    # (Line 392) function SquareShapeAtWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, distanceX, distanceY, property)

# (Line 393) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def SquareShapeAtWithProperty(cp, count_1, Unit, x, y, distanceX, distanceY, property):
    # (Line 394) MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY);
    MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY)
    # (Line 395) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 396) MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY);
    MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY)
    # (Line 397) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 398) MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY);
    MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY)
    # (Line 399) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 400) MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY);
    MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY)
    # (Line 401) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 402) }
    # (Line 405) function SquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, x, y, distanceX, distanceY)

# (Line 406) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def SquareShapeAt(cp, count_1, Unit, x, y, distanceX, distanceY):
    # (Line 407) MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY);
    MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY)
    # (Line 408) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 409) MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY);
    MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY)
    # (Line 410) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 411) MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY);
    MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY)
    # (Line 412) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 413) MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY);
    MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY)
    # (Line 414) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 415) }
    # (Line 417) function NxNSquareShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, n, interval, property)

# (Line 418) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def NxNSquareShapeWithProperty(cp, count_1, Unit, n, interval, property):
    # (Line 419) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 420) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 421) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 422) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 424) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 425) {
        # (Line 426) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 427) {
            # (Line 428) MoveLoc(heroID[cp] ,cp, 0, 0);
            MoveLoc(heroID[cp], cp, 0, 0)
            # (Line 429) SkillUnitWithProperty(cp, count, Unit, property);
            SkillUnitWithProperty(cp, count_1, Unit, property)
            # (Line 430) }
            # (Line 431) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 432) {
            # (Line 433) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 434) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 435) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 436) {
                # (Line 437) SquareShapeWithProperty(cp, count, Unit, distance, destY * interval - distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, distance_1, destY * interval - distance_1, property)
                # (Line 438) }
                # (Line 439) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 440) {
                # (Line 441) SquareShapeWithProperty(cp, count, Unit, destX * interval - distance, distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval - distance_1, distance_1, property)
                # (Line 442) }
                # (Line 443) else
                # (Line 444) {
            if EUDElse()():
                # (Line 445) SquareShapeWithProperty(cp, count, Unit, destX * interval - distance, destY * interval - distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1, property)
                # (Line 446) }
                # (Line 447) }
            EUDEndIf()
            # (Line 448) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 449) {
            # (Line 450) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 451) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 453) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 454) {
                # (Line 455) MoveLoc(heroID[cp] ,cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 456) SkillUnitWithProperty(cp, count, Unit, property);
                SkillUnitWithProperty(cp, count_1, Unit, property)
                # (Line 457) }
                # (Line 458) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 459) {
                # (Line 460) SquareShapeWithProperty(cp, count, Unit, destX * interval, 0, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval, 0, property)
                # (Line 461) }
                # (Line 462) else
                # (Line 463) {
            if EUDElse()():
                # (Line 464) SquareShapeWithProperty(cp, count, Unit, destX * interval + interval, destY * interval, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval + interval, destY * interval, property)
                # (Line 465) }
                # (Line 467) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 468) {
                # (Line 469) SquareShapeWithProperty(cp, count, Unit, (n / 2) * interval, 0, property);
                SquareShapeWithProperty(cp, count_1, Unit, (n // 2) * interval, 0, property)
                # (Line 470) }
                # (Line 471) }
            EUDEndIf()
            # (Line 472) }
        EUDEndIf()
        # (Line 473) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 475) function NxNSquareShape(cp : TrgPlayer, count, Unit : TrgUnit, n, interval)

# (Line 476) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def NxNSquareShape(cp, count_1, Unit, n, interval):
    # (Line 477) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 478) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 479) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 480) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 482) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 483) {
        # (Line 484) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 485) {
            # (Line 486) MoveLoc(heroID[cp] ,cp, 0, 0);
            MoveLoc(heroID[cp], cp, 0, 0)
            # (Line 487) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 488) }
            # (Line 489) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 490) {
            # (Line 491) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 492) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 493) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 494) {
                # (Line 495) SquareShape(cp, count, Unit, distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, distance_1, destY * interval - distance_1)
                # (Line 496) }
                # (Line 497) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 498) {
                # (Line 499) SquareShape(cp, count, Unit, destX * interval - distance, distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, distance_1)
                # (Line 500) }
                # (Line 501) else
                # (Line 502) {
            if EUDElse()():
                # (Line 503) SquareShape(cp, count, Unit, destX * interval - distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1)
                # (Line 504) }
                # (Line 505) }
            EUDEndIf()
            # (Line 506) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 507) {
            # (Line 508) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 509) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 511) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 512) {
                # (Line 513) MoveLoc(heroID[cp] ,cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 514) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 515) }
                # (Line 516) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 517) {
                # (Line 518) SquareShape(cp, count, Unit, destX * interval, 0);
                SquareShape(cp, count_1, Unit, destX * interval, 0)
                # (Line 519) }
                # (Line 520) else
                # (Line 521) {
            if EUDElse()():
                # (Line 522) SquareShape(cp, count, Unit, destX * interval + interval, destY * interval);
                SquareShape(cp, count_1, Unit, destX * interval + interval, destY * interval)
                # (Line 523) }
                # (Line 525) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 526) {
                # (Line 527) SquareShape(cp, count, Unit, (n / 2) * interval, 0);
                SquareShape(cp, count_1, Unit, (n // 2) * interval, 0)
                # (Line 528) }
                # (Line 529) }
            EUDEndIf()
            # (Line 530) }
        EUDEndIf()
        # (Line 531) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 534) function NxNSquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, n, interval, distanceX, distanceY)

# (Line 535) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def NxNSquareShapeAt(cp, count_1, Unit, n, interval, distanceX, distanceY):
    # (Line 536) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 537) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 538) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 539) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 541) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 542) {
        # (Line 543) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 544) {
            # (Line 545) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
            MoveLoc(heroID[cp], cp, distanceX, distanceY)
            # (Line 546) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 547) }
            # (Line 548) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 549) {
            # (Line 550) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 551) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 553) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 554) {
                # (Line 555) SquareShapeAt(cp, count, Unit, distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 556) }
                # (Line 557) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 558) {
                # (Line 559) SquareShapeAt(cp, count, Unit, destX * interval - distance, distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, distance_1, distanceX, distanceY)
                # (Line 560) }
                # (Line 561) else
                # (Line 562) {
            if EUDElse()():
                # (Line 563) SquareShapeAt(cp, count, Unit, destX * interval - distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 564) }
                # (Line 565) }
            EUDEndIf()
            # (Line 566) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 567) {
            # (Line 568) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 569) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 571) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 572) {
                # (Line 573) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
                MoveLoc(heroID[cp], cp, distanceX, distanceY)
                # (Line 574) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 575) }
                # (Line 576) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 577) {
                # (Line 578) SquareShapeAt(cp, count, Unit, destX * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval, 0, distanceX, distanceY)
                # (Line 579) }
                # (Line 580) else
                # (Line 581) {
            if EUDElse()():
                # (Line 582) SquareShapeAt(cp, count, Unit, destX * interval + interval, destY * interval, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval + interval, destY * interval, distanceX, distanceY)
                # (Line 583) }
                # (Line 585) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 586) {
                # (Line 587) SquareShapeAt(cp, count, Unit, (n / 2) * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, (n // 2) * interval, 0, distanceX, distanceY)
                # (Line 588) }
                # (Line 589) }
            EUDEndIf()
            # (Line 590) }
        EUDEndIf()
        # (Line 591) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 593) function LineShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distance)

# (Line 594) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def LineShape(cp, count_1, Unit, degree, n, interval, distance_1):
    # (Line 595) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 597) Table_Sin(cp, degree + 90, distance);
    Table_Sin(cp, degree + 90, distance_1)
    # (Line 598) Table_Cos(cp, degree + 90, distance);
    Table_Cos(cp, degree + 90, distance_1)
    # (Line 599) var x_initial = CosAngle[cp];
    x_initial = EUDVariable()
    x_initial << (CosAngle[cp])
    # (Line 600) var y_initial = SinAngle[cp];
    y_initial = EUDVariable()
    y_initial << (SinAngle[cp])
    # (Line 602) if (n % 2 == 0)
    if EUDIf()(n % 2 == 0):
        # (Line 603) {
        # (Line 604) Table_Sin(cp, degree, interval * (n / 2) - interval / 2);
        Table_Sin(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 605) Table_Cos(cp, degree, interval * (n / 2) - interval / 2);
        Table_Cos(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 606) }
        # (Line 607) else
        # (Line 608) {
    if EUDElse()():
        # (Line 609) Table_Sin(cp, degree, interval * (n / 2));
        Table_Sin(cp, degree, interval * (n // 2))
        # (Line 610) Table_Cos(cp, degree, interval * (n / 2));
        Table_Cos(cp, degree, interval * (n // 2))
        # (Line 611) }
        # (Line 613) x_initial = x_initial + CosAngle[cp];
    EUDEndIf()
    x_initial << (x_initial + CosAngle[cp])
    # (Line 614) y_initial = y_initial + SinAngle[cp];
    y_initial << (y_initial + SinAngle[cp])
    # (Line 616) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 617) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 619) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t3():
            i.__iadd__(1)
        # (Line 620) {
        # (Line 621) MoveLoc(heroID[cp], cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp]);
        MoveLoc(heroID[cp], cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp])
        # (Line 622) SkillUnit(cp, count, Unit);
        SkillUnit(cp, count_1, Unit)
        # (Line 623) }
        # (Line 625) }
        EUDSetContinuePoint()
        _t3()
    EUDEndWhile()
    # (Line 628) function LineShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distanceX, distanceY)

# (Line 629) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def LineShapeAt(cp, count_1, Unit, degree, n, interval, distanceX, distanceY):
    # (Line 630) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 631) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 633) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 634) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 636) var gradientX = CosAngle[cp];
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp])
    # (Line 637) var gradientY = SinAngle[cp] ;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp])
    # (Line 639) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 641) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 642) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 644) for (; i <= n / 2; i++)
    if EUDWhile()(i <= n // 2):
        def _t2():
            i.__iadd__(1)
        # (Line 645) {
        # (Line 646) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 647) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 649) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 650) {
            # (Line 651) MoveLoc(heroID[cp], cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY);
            MoveLoc(heroID[cp], cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY)
            # (Line 652) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 653) MoveLoc(heroID[cp], cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY);
            MoveLoc(heroID[cp], cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY)
            # (Line 654) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 655) }
            # (Line 656) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 657) {
            # (Line 658) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 659) {
                # (Line 660) MoveLoc(heroID[cp], cp, distanceX, distanceY);
                MoveLoc(heroID[cp], cp, distanceX, distanceY)
                # (Line 661) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 662) }
                # (Line 663) else
                # (Line 664) {
            if EUDElse()():
                # (Line 665) MoveLoc(heroID[cp], cp, -Angle_X + distanceX, Angle_Y + distanceY);
                MoveLoc(heroID[cp], cp, -Angle_X + distanceX, Angle_Y + distanceY)
                # (Line 666) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 667) MoveLoc(heroID[cp], cp, Angle_X + distanceX, -Angle_Y + distanceY);
                MoveLoc(heroID[cp], cp, Angle_X + distanceX, -Angle_Y + distanceY)
                # (Line 668) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 669) }
                # (Line 671) }
            EUDEndIf()
            # (Line 672) }
        EUDEndIf()
        # (Line 674) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 676) function CrossShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval)

# (Line 677) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def CrossShape(cp, count_1, Unit, degree, n, interval):
    # (Line 678) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 679) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 681) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 682) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 684) var gradientX = CosAngle[cp] / 2;
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp] // 2)
    # (Line 685) var gradientY = SinAngle[cp] / 2;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp] // 2)
    # (Line 687) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 689) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 690) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 692) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 693) {
        # (Line 694) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 695) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 697) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 698) {
            # (Line 699) SquareShape(cp, count, Unit, Angle_X + gradientX, Angle_Y + gradientY);
            SquareShape(cp, count_1, Unit, Angle_X + gradientX, Angle_Y + gradientY)
            # (Line 700) }
            # (Line 701) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 702) {
            # (Line 703) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 704) {
                # (Line 705) MoveLoc(heroID[cp], cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 706) }
                # (Line 707) else
                # (Line 708) {
            if EUDElse()():
                # (Line 709) SquareShape(cp, count, Unit, Angle_X, Angle_Y);
                SquareShape(cp, count_1, Unit, Angle_X, Angle_Y)
                # (Line 710) }
                # (Line 711) }
            EUDEndIf()
            # (Line 712) }
        EUDEndIf()
        # (Line 713) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 715) function Table_Sin(cp, degree, distance)

# (Line 716) {
@EUDFunc
def Table_Sin(cp, degree, distance_1):
    # (Line 717) degree = degree % 360;
    degree << (degree % 360)
    # (Line 718) SinAngle[cp] = SinTableArray[degree];
    _ARRW(SinAngle, cp) << (SinTableArray[degree])
    # (Line 720) if (SinAngle[cp] > 2147483648)
    if EUDIf()(SinAngle[cp] <= 2147483648, neg=True):
        # (Line 721) {
        # (Line 722) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 723) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 724) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 725) }
        # (Line 726) else
        # (Line 727) {
    if EUDElse()():
        # (Line 728) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 729) }
        # (Line 730) }
    EUDEndIf()
    # (Line 732) function Table_Cos(cp, degree, distance)

# (Line 733) {
@EUDFunc
def Table_Cos(cp, degree, distance_1):
    # (Line 734) degree = degree % 360;
    degree << (degree % 360)
    # (Line 735) CosAngle[cp] = CosTableArray[degree];
    _ARRW(CosAngle, cp) << (CosTableArray[degree])
    # (Line 737) if (CosAngle[cp] > 2147483648)
    if EUDIf()(CosAngle[cp] <= 2147483648, neg=True):
        # (Line 738) {
        # (Line 739) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 740) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 741) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 742) }
        # (Line 743) else
        # (Line 744) {
    if EUDElse()():
        # (Line 745) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 746) }
        # (Line 747) }
    EUDEndIf()
    # (Line 749) function EdgeShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, property)

# (Line 750) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def EdgeShapeWithProperty(cp, count_1, Unit, degree, n, interval, property):
    # (Line 751) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 753) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 754) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 756) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 757) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 759) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 761) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 762) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 764) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 765) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 767) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 768) {
        # (Line 769) DotShapeWithProperty(cp, 1, Unit, 0, 0, property);
        DotShapeWithProperty(cp, 1, Unit, 0, 0, property)
        # (Line 770) }
        # (Line 771) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 772) {
        # (Line 773) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 774) {
            # (Line 775) SquareShapeWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property);
            SquareShapeWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property)
            # (Line 776) }
            # (Line 777) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 778) }
    EUDEndIf()
    # (Line 780) function EdgeShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval)

# (Line 781) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def EdgeShape(cp, count_1, Unit, degree, n, interval):
    # (Line 782) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 784) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 785) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 787) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 788) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 790) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 792) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 793) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 795) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 796) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 798) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 799) {
        # (Line 800) DotShape(cp, 1, Unit, 0, 0);
        DotShape(cp, 1, Unit, 0, 0)
        # (Line 801) }
        # (Line 802) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 803) {
        # (Line 804) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 805) {
            # (Line 806) SquareShape(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i));
            SquareShape(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i))
            # (Line 807) }
            # (Line 808) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 809) }
    EUDEndIf()
    # (Line 810) function EdgeShapeAtWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y, property)

# (Line 811) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None, None])
def EdgeShapeAtWithProperty(cp, count_1, Unit, degree, n, interval, x, y, property):
    # (Line 812) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 814) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 815) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 817) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 818) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 820) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 822) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 823) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 825) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 826) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 828) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 829) {
        # (Line 830) DotShapeWithProperty(cp, 1, Unit, x, y, property);
        DotShapeWithProperty(cp, 1, Unit, x, y, property)
        # (Line 831) }
        # (Line 832) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 833) {
        # (Line 834) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 835) {
            # (Line 836) SquareShapeAtWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y, property);
            SquareShapeAtWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y, property)
            # (Line 837) }
            # (Line 838) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 839) }
    EUDEndIf()
    # (Line 841) function EdgeShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y)

# (Line 842) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def EdgeShapeAt(cp, count_1, Unit, degree, n, interval, x, y):
    # (Line 843) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 845) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 846) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 848) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 849) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 851) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 853) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 854) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 856) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 857) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 859) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 860) {
        # (Line 861) DotShape(cp, 1, Unit, x, y);
        DotShape(cp, 1, Unit, x, y)
        # (Line 862) }
        # (Line 863) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 864) {
        # (Line 865) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 866) {
            # (Line 867) SquareShapeAt(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y);
            SquareShapeAt(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y)
            # (Line 868) }
            # (Line 869) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 870) }
    EUDEndIf()
