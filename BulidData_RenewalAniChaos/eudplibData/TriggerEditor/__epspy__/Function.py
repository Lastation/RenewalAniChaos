## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import py_math;
import math
# (Line 3) import Variable as v;
import Variable as v
# (Line 5) const step		= PVariable();			// SkillStep
step = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 6) const count 		= PVariable();			// SkillCount
count = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 7) const loop 		= PVariable();			// SkillLoop
loop = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 8) const delay		= PVariable();			// wait(value)
delay = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 9) const wait		= PVariable();			// SkillWait
wait = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 11) const INSERT_KEY = PVariable();
INSERT_KEY = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 13) const loopB 		= PVariable();			// SkillLoop2
loopB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 14) const loopC 	= PVariable();			// SkillLoop3
loopC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 15) const delayB	= PVariable();			// wait(value)2
delayB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 16) const delayC	= PVariable();			// wait(value)3
delayC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 18) const location 	= PVariable();			// 로케이션 번호
location = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 19) const heroID 	= PVariable();			// 영웅 유닛 번호
heroID = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 21) const UltimateA	= PVariable();			// 궁극기 1번 소모량
UltimateA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 22) const UltimateB	= PVariable();			// 궁극기 2번 소모량
UltimateB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 23) const UltimateC	= PVariable();			// 궁극기 3번 소모량
UltimateC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 25) const UniqueA	= PVariable();			// 유니크 1번 쿨타임
UniqueA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 27) const distance	= PVariable();			// 거리 관련 변수
distance = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 28) const SinAngle 	= PVariable();			// Sin 변수
SinAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 29) const CosAngle 	= PVariable();			// Cos 변수
CosAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 31) const stb		= StringBuffer();
stb = _CGFW(lambda: [StringBuffer()], 1)[0]
# (Line 33) const HoldLocation = [97, 119, 122, 126, 134, 143, 0, 0];
HoldLocation = _CGFW(lambda: [_ARR(FlattenList([97, 119, 122, 126, 134, 143, 0, 0]))], 1)[0]
# (Line 35) const CosTableArray = EUDArray(py_eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"));
CosTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 36) const SinTableArray 	= EUDArray(py_eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"));
SinTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 38) const Rusaruka_Voice 	= PVariable();	// 1
Rusaruka_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 39) const Chtholly_Voice 	= PVariable();	// 2
Chtholly_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 40) const Kiana_Voice 		= PVariable();	// 17
Kiana_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 41) const Yuuna_Voice 		= PVariable();	// 22
Yuuna_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 42) const Mayuri_Voice 		= PVariable();	// 23
Mayuri_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 43) const Nanami_Voice 		= PVariable();	// 24
Nanami_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 44) const Milim_Voice 		= PVariable();	// 25
Milim_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 45) const Yume_Voice 		= PVariable();	// 26
Yume_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 46) const Magane_Voice 	= PVariable();	// 27
Magane_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 47) const Oda_Voice 		= PVariable();	// 28
Oda_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 48) const Ekidona_Voice 	= PVariable();	// 29
Ekidona_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 49) const Niwa_Voice 		= PVariable();	// 30
Niwa_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 50) const Park_Voice 		= PVariable();	// 31
Park_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 52) const p_burrowed = UnitProperty(burrowed = true);
p_burrowed = _CGFW(lambda: [UnitProperty(burrowed=True)], 1)[0]
# (Line 53) const p_hallucinated = UnitProperty(hallucinated = true);
p_hallucinated = _CGFW(lambda: [UnitProperty(hallucinated=True)], 1)[0]
# (Line 55) const ally				= PVariable();
ally = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 57) function Table_Sin(cp, degree, distance);
# (Line 58) function Table_Cos(cp, degree, distance);
# (Line 60) function Voice_Routine(cp, num)
# (Line 61) {
@EUDFunc
def Voice_Routine(cp, num):
    # (Line 62) switch(v.Hero_Num[cp])
    EUDSwitch(v.Hero_Num[cp])
    # (Line 63) {
    # (Line 64) case 1:
    _t1 = EUDSwitchCase()
    # (Line 65) Rusaruka_Voice[0] = num;
    if _t1(1):
        _ARRW(Rusaruka_Voice, 0) << (num)
        # (Line 66) Rusaruka_Voice[1] = num;
        _ARRW(Rusaruka_Voice, 1) << (num)
        # (Line 67) Rusaruka_Voice[2] = num;
        _ARRW(Rusaruka_Voice, 2) << (num)
        # (Line 68) Rusaruka_Voice[3] = num;
        _ARRW(Rusaruka_Voice, 3) << (num)
        # (Line 69) Rusaruka_Voice[4] = num;
        _ARRW(Rusaruka_Voice, 4) << (num)
        # (Line 70) Rusaruka_Voice[5] = num;
        _ARRW(Rusaruka_Voice, 5) << (num)
        # (Line 71) break;
        EUDBreak()
        # (Line 72) case 2:
    _t2 = EUDSwitchCase()
    # (Line 73) Chtholly_Voice[0] = num;
    if _t2(2):
        _ARRW(Chtholly_Voice, 0) << (num)
        # (Line 74) Chtholly_Voice[1] = num;
        _ARRW(Chtholly_Voice, 1) << (num)
        # (Line 75) Chtholly_Voice[2] = num;
        _ARRW(Chtholly_Voice, 2) << (num)
        # (Line 76) Chtholly_Voice[3] = num;
        _ARRW(Chtholly_Voice, 3) << (num)
        # (Line 77) Chtholly_Voice[4] = num;
        _ARRW(Chtholly_Voice, 4) << (num)
        # (Line 78) Chtholly_Voice[5] = num;
        _ARRW(Chtholly_Voice, 5) << (num)
        # (Line 79) break;
        EUDBreak()
        # (Line 80) case 17:
    _t3 = EUDSwitchCase()
    # (Line 81) Kiana_Voice[0] = num;
    if _t3(17):
        _ARRW(Kiana_Voice, 0) << (num)
        # (Line 82) Kiana_Voice[1] = num;
        _ARRW(Kiana_Voice, 1) << (num)
        # (Line 83) Kiana_Voice[2] = num;
        _ARRW(Kiana_Voice, 2) << (num)
        # (Line 84) Kiana_Voice[3] = num;
        _ARRW(Kiana_Voice, 3) << (num)
        # (Line 85) Kiana_Voice[4] = num;
        _ARRW(Kiana_Voice, 4) << (num)
        # (Line 86) Kiana_Voice[5] = num;
        _ARRW(Kiana_Voice, 5) << (num)
        # (Line 87) break;
        EUDBreak()
        # (Line 88) case 22:
    _t4 = EUDSwitchCase()
    # (Line 89) Yuuna_Voice[0] = num;
    if _t4(22):
        _ARRW(Yuuna_Voice, 0) << (num)
        # (Line 90) Yuuna_Voice[1] = num;
        _ARRW(Yuuna_Voice, 1) << (num)
        # (Line 91) Yuuna_Voice[2] = num;
        _ARRW(Yuuna_Voice, 2) << (num)
        # (Line 92) Yuuna_Voice[3] = num;
        _ARRW(Yuuna_Voice, 3) << (num)
        # (Line 93) Yuuna_Voice[4] = num;
        _ARRW(Yuuna_Voice, 4) << (num)
        # (Line 94) Yuuna_Voice[5] = num;
        _ARRW(Yuuna_Voice, 5) << (num)
        # (Line 95) break;
        EUDBreak()
        # (Line 96) case 23:
    _t5 = EUDSwitchCase()
    # (Line 97) Mayuri_Voice[0] = num;
    if _t5(23):
        _ARRW(Mayuri_Voice, 0) << (num)
        # (Line 98) Mayuri_Voice[1] = num;
        _ARRW(Mayuri_Voice, 1) << (num)
        # (Line 99) Mayuri_Voice[2] = num;
        _ARRW(Mayuri_Voice, 2) << (num)
        # (Line 100) Mayuri_Voice[3] = num;
        _ARRW(Mayuri_Voice, 3) << (num)
        # (Line 101) Mayuri_Voice[4] = num;
        _ARRW(Mayuri_Voice, 4) << (num)
        # (Line 102) Mayuri_Voice[5] = num;
        _ARRW(Mayuri_Voice, 5) << (num)
        # (Line 103) break;
        EUDBreak()
        # (Line 104) case 24:
    _t6 = EUDSwitchCase()
    # (Line 105) Nanami_Voice[0] = num;
    if _t6(24):
        _ARRW(Nanami_Voice, 0) << (num)
        # (Line 106) Nanami_Voice[1] = num;
        _ARRW(Nanami_Voice, 1) << (num)
        # (Line 107) Nanami_Voice[2] = num;
        _ARRW(Nanami_Voice, 2) << (num)
        # (Line 108) Nanami_Voice[3] = num;
        _ARRW(Nanami_Voice, 3) << (num)
        # (Line 109) Nanami_Voice[4] = num;
        _ARRW(Nanami_Voice, 4) << (num)
        # (Line 110) Nanami_Voice[5] = num;
        _ARRW(Nanami_Voice, 5) << (num)
        # (Line 111) break;
        EUDBreak()
        # (Line 112) case 25:
    _t7 = EUDSwitchCase()
    # (Line 113) Milim_Voice[0] = num;
    if _t7(25):
        _ARRW(Milim_Voice, 0) << (num)
        # (Line 114) Milim_Voice[1] = num;
        _ARRW(Milim_Voice, 1) << (num)
        # (Line 115) Milim_Voice[2] = num;
        _ARRW(Milim_Voice, 2) << (num)
        # (Line 116) Milim_Voice[3] = num;
        _ARRW(Milim_Voice, 3) << (num)
        # (Line 117) Milim_Voice[4] = num;
        _ARRW(Milim_Voice, 4) << (num)
        # (Line 118) Milim_Voice[5] = num;
        _ARRW(Milim_Voice, 5) << (num)
        # (Line 119) break;
        EUDBreak()
        # (Line 120) case 26:
    _t8 = EUDSwitchCase()
    # (Line 121) Yume_Voice[0] = num;
    if _t8(26):
        _ARRW(Yume_Voice, 0) << (num)
        # (Line 122) Yume_Voice[1] = num;
        _ARRW(Yume_Voice, 1) << (num)
        # (Line 123) Yume_Voice[2] = num;
        _ARRW(Yume_Voice, 2) << (num)
        # (Line 124) Yume_Voice[3] = num;
        _ARRW(Yume_Voice, 3) << (num)
        # (Line 125) Yume_Voice[4] = num;
        _ARRW(Yume_Voice, 4) << (num)
        # (Line 126) Yume_Voice[5] = num;
        _ARRW(Yume_Voice, 5) << (num)
        # (Line 127) break;
        EUDBreak()
        # (Line 128) case 27:
    _t9 = EUDSwitchCase()
    # (Line 129) Magane_Voice[0] = num;
    if _t9(27):
        _ARRW(Magane_Voice, 0) << (num)
        # (Line 130) Magane_Voice[1] = num;
        _ARRW(Magane_Voice, 1) << (num)
        # (Line 131) Magane_Voice[2] = num;
        _ARRW(Magane_Voice, 2) << (num)
        # (Line 132) Magane_Voice[3] = num;
        _ARRW(Magane_Voice, 3) << (num)
        # (Line 133) Magane_Voice[4] = num;
        _ARRW(Magane_Voice, 4) << (num)
        # (Line 134) Magane_Voice[5] = num;
        _ARRW(Magane_Voice, 5) << (num)
        # (Line 135) break;
        EUDBreak()
        # (Line 136) case 28:
    _t10 = EUDSwitchCase()
    # (Line 137) Oda_Voice[0] = num;
    if _t10(28):
        _ARRW(Oda_Voice, 0) << (num)
        # (Line 138) Oda_Voice[1] = num;
        _ARRW(Oda_Voice, 1) << (num)
        # (Line 139) Oda_Voice[2] = num;
        _ARRW(Oda_Voice, 2) << (num)
        # (Line 140) Oda_Voice[3] = num;
        _ARRW(Oda_Voice, 3) << (num)
        # (Line 141) Oda_Voice[4] = num;
        _ARRW(Oda_Voice, 4) << (num)
        # (Line 142) Oda_Voice[5] = num;
        _ARRW(Oda_Voice, 5) << (num)
        # (Line 143) break;
        EUDBreak()
        # (Line 144) case 29:
    _t11 = EUDSwitchCase()
    # (Line 145) Ekidona_Voice[0] = num;
    if _t11(29):
        _ARRW(Ekidona_Voice, 0) << (num)
        # (Line 146) Ekidona_Voice[1] = num;
        _ARRW(Ekidona_Voice, 1) << (num)
        # (Line 147) Ekidona_Voice[2] = num;
        _ARRW(Ekidona_Voice, 2) << (num)
        # (Line 148) Ekidona_Voice[3] = num;
        _ARRW(Ekidona_Voice, 3) << (num)
        # (Line 149) Ekidona_Voice[4] = num;
        _ARRW(Ekidona_Voice, 4) << (num)
        # (Line 150) Ekidona_Voice[5] = num;
        _ARRW(Ekidona_Voice, 5) << (num)
        # (Line 151) break;
        EUDBreak()
        # (Line 152) case 30:
    _t12 = EUDSwitchCase()
    # (Line 153) Niwa_Voice[0] = num;
    if _t12(30):
        _ARRW(Niwa_Voice, 0) << (num)
        # (Line 154) Niwa_Voice[1] = num;
        _ARRW(Niwa_Voice, 1) << (num)
        # (Line 155) Niwa_Voice[2] = num;
        _ARRW(Niwa_Voice, 2) << (num)
        # (Line 156) Niwa_Voice[3] = num;
        _ARRW(Niwa_Voice, 3) << (num)
        # (Line 157) Niwa_Voice[4] = num;
        _ARRW(Niwa_Voice, 4) << (num)
        # (Line 158) Niwa_Voice[5] = num;
        _ARRW(Niwa_Voice, 5) << (num)
        # (Line 159) break;
        EUDBreak()
        # (Line 160) case 31:
    _t13 = EUDSwitchCase()
    # (Line 161) Park_Voice[0] = num;
    if _t13(31):
        _ARRW(Park_Voice, 0) << (num)
        # (Line 162) Park_Voice[1] = num;
        _ARRW(Park_Voice, 1) << (num)
        # (Line 163) Park_Voice[2] = num;
        _ARRW(Park_Voice, 2) << (num)
        # (Line 164) Park_Voice[3] = num;
        _ARRW(Park_Voice, 3) << (num)
        # (Line 165) Park_Voice[4] = num;
        _ARRW(Park_Voice, 4) << (num)
        # (Line 166) Park_Voice[5] = num;
        _ARRW(Park_Voice, 5) << (num)
        # (Line 167) break;
        EUDBreak()
        # (Line 169) }
    # (Line 170) }
    EUDEndSwitch()
    # (Line 172) function SetHeroID(cp)

# (Line 173) {
@EUDFunc
def SetHeroID(cp):
    # (Line 174) switch(v.Hero_Num[cp])
    EUDSwitch(v.Hero_Num[cp])
    # (Line 175) {
    # (Line 176) case 3:
    _t1 = EUDSwitchCase()
    # (Line 177) heroID[cp] = 74;
    if _t1(3):
        _ARRW(heroID, cp) << (74)
        # (Line 178) break;
        EUDBreak()
        # (Line 179) case 4:
    _t2 = EUDSwitchCase()
    # (Line 180) heroID[cp] = 74;
    if _t2(4):
        _ARRW(heroID, cp) << (74)
        # (Line 181) break;
        EUDBreak()
        # (Line 182) case 5:
    _t3 = EUDSwitchCase()
    # (Line 183) heroID[cp] = 74;
    if _t3(5):
        _ARRW(heroID, cp) << (74)
        # (Line 184) break;
        EUDBreak()
        # (Line 185) case 6:
    _t4 = EUDSwitchCase()
    # (Line 186) heroID[cp] = 16;
    if _t4(6):
        _ARRW(heroID, cp) << (16)
        # (Line 187) break;
        EUDBreak()
        # (Line 188) case 7:
    _t5 = EUDSwitchCase()
    # (Line 189) heroID[cp] = 77;
    if _t5(7):
        _ARRW(heroID, cp) << (77)
        # (Line 190) break;
        EUDBreak()
        # (Line 191) case 8:
    _t6 = EUDSwitchCase()
    # (Line 192) heroID[cp] = 51;
    if _t6(8):
        _ARRW(heroID, cp) << (51)
        # (Line 193) break;
        EUDBreak()
        # (Line 194) case 9:
    _t7 = EUDSwitchCase()
    # (Line 195) heroID[cp] = 51;
    if _t7(9):
        _ARRW(heroID, cp) << (51)
        # (Line 196) break;
        EUDBreak()
        # (Line 197) case 10:
    _t8 = EUDSwitchCase()
    # (Line 198) heroID[cp] = 77;
    if _t8(10):
        _ARRW(heroID, cp) << (77)
        # (Line 199) break;
        EUDBreak()
        # (Line 200) case 11:
    _t9 = EUDSwitchCase()
    # (Line 201) heroID[cp] = 16;
    if _t9(11):
        _ARRW(heroID, cp) << (16)
        # (Line 202) break;
        EUDBreak()
        # (Line 203) case 12:
    _t10 = EUDSwitchCase()
    # (Line 204) heroID[cp] = 54;
    if _t10(12):
        _ARRW(heroID, cp) << (54)
        # (Line 205) break;
        EUDBreak()
        # (Line 206) case 13:
    _t11 = EUDSwitchCase()
    # (Line 207) heroID[cp] = 16;
    if _t11(13):
        _ARRW(heroID, cp) << (16)
        # (Line 208) break;
        EUDBreak()
        # (Line 209) case 14:
    _t12 = EUDSwitchCase()
    # (Line 210) heroID[cp] = 99;
    if _t12(14):
        _ARRW(heroID, cp) << (99)
        # (Line 211) break;
        EUDBreak()
        # (Line 212) case 15:
    _t13 = EUDSwitchCase()
    # (Line 213) heroID[cp] = 54;
    if _t13(15):
        _ARRW(heroID, cp) << (54)
        # (Line 214) break;
        EUDBreak()
        # (Line 215) case 16:
    _t14 = EUDSwitchCase()
    # (Line 216) heroID[cp] = 16;
    if _t14(16):
        _ARRW(heroID, cp) << (16)
        # (Line 217) break;
        EUDBreak()
        # (Line 218) case 17:
    _t15 = EUDSwitchCase()
    # (Line 219) heroID[cp] = 77;
    if _t15(17):
        _ARRW(heroID, cp) << (77)
        # (Line 220) break;
        EUDBreak()
        # (Line 221) case 18:
    _t16 = EUDSwitchCase()
    # (Line 222) heroID[cp] = 99;
    if _t16(18):
        _ARRW(heroID, cp) << (99)
        # (Line 223) break;
        EUDBreak()
        # (Line 224) case 19:
    _t17 = EUDSwitchCase()
    # (Line 225) heroID[cp] = 51;
    if _t17(19):
        _ARRW(heroID, cp) << (51)
        # (Line 226) break;
        EUDBreak()
        # (Line 227) case 20:
    _t18 = EUDSwitchCase()
    # (Line 228) heroID[cp] = 99;
    if _t18(20):
        _ARRW(heroID, cp) << (99)
        # (Line 229) break;
        EUDBreak()
        # (Line 230) case 21:
    _t19 = EUDSwitchCase()
    # (Line 231) heroID[cp] = 64;
    if _t19(21):
        _ARRW(heroID, cp) << (64)
        # (Line 232) break;
        EUDBreak()
        # (Line 233) }
    # (Line 234) }
    EUDEndSwitch()
    # (Line 236) function ShieldFix(cp : TrgPlayer, amount)

# (Line 237) {
@EUDTypedFunc([TrgPlayer, None])
def ShieldFix(cp, amount):
    # (Line 238) SetDeaths(cp, SetTo, 1, " `ShieldRecharge");
    # (Line 239) v.Shield[cp] = amount;
    DoActions(SetDeaths(cp, SetTo, 1, " `ShieldRecharge"))
    _ARRW(v.Shield, cp) << (amount)
    # (Line 240) }
    # (Line 242) function ComputerAlly(cp : TrgPlayer)

# (Line 243) {
@EUDTypedFunc([TrgPlayer])
def ComputerAlly(cp):
    # (Line 244) ally[cp] = 1;
    _ARRW(ally, cp) << (1)
    # (Line 245) SetSwitch("ComputerAlliy", Set);
    # (Line 246) SetAllianceStatus(P7, Ally);
    DoActions(SetSwitch("ComputerAlliy", Set))
    # (Line 247) SetAllianceStatus(P8, Ally);
    DoActions(SetAllianceStatus(P7, Ally))
    # (Line 248) }
    DoActions(SetAllianceStatus(P8, Ally))
    # (Line 250) function MoveLoc(Unit : TrgUnit, cp : TrgPlayer, x, y)

# (Line 251) {
@EUDTypedFunc([TrgUnit, TrgPlayer, None, None])
def MoveLoc(Unit, cp, x, y):
    # (Line 252) MoveLocation(location[cp], Unit, cp, "Anywhere");
    # (Line 253) addloc(location[cp], x, y);
    DoActions(MoveLocation(location[cp], Unit, cp, "Anywhere"))
    f_addloc(location[cp], x, y)
    # (Line 254) }
    # (Line 256) function SkillUnitWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, property)

# (Line 257) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None])
def SkillUnitWithProperty(cp, count_1, Unit, property):
    # (Line 258) switch (property)
    EUDSwitch(property)
    # (Line 259) {
    # (Line 260) case 0:
    _t1 = EUDSwitchCase()
    # (Line 261) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(burrowed = true));
    if _t1(0):
        # (Line 262) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(burrowed=True)))
        EUDBreak()
        # (Line 263) case 1:
    _t2 = EUDSwitchCase()
    # (Line 264) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(hallucinated = true));
    if _t2(1):
        # (Line 265) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(hallucinated=True)))
        EUDBreak()
        # (Line 266) case 2:
    _t3 = EUDSwitchCase()
    # (Line 267) CreateUnitWithProperties(count, Unit, dwrand() % 8 + 33, cp, UnitProperty(hallucinated = true, burrowed = true));
    if _t3(2):
        # (Line 268) break;
        DoActions(CreateUnitWithProperties(count_1, Unit, f_dwrand() % 8 + 33, cp, UnitProperty(hallucinated=True, burrowed=True)))
        EUDBreak()
        # (Line 270) }
    # (Line 272) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    EUDEndSwitch()
    # (Line 273) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 274) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]))
    # (Line 276) function SkillUnit(cp : TrgPlayer, count, Unit : TrgUnit)

# (Line 277) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit])
def SkillUnit(cp, count_1, Unit):
    # (Line 278) CreateUnit(count, Unit, dwrand() % 8 + 33, cp);
    # (Line 279) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(count_1, Unit, f_dwrand() % 8 + 33, cp))
    # (Line 280) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 281) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]))
    # (Line 283) function BanReturn(cp)

# (Line 284) {
@EUDFunc
def BanReturn(cp):
    # (Line 285) KillUnit("Protoss Observer", cp);
    # (Line 286) }
    DoActions(KillUnit("Protoss Observer", cp))
    # (Line 288) function Slow(cp)

# (Line 289) {
@EUDFunc
def Slow(cp):
    # (Line 290) MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere");
    # (Line 291) CreateUnit(1, "Zerg Devourer", "[Skill]Unit_Wait_8", cp);
    DoActions(MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere"))
    # (Line 292) SetInvincibility(Enable, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(1, "Zerg Devourer", "[Skill]Unit_Wait_8", cp))
    # (Line 293) MoveUnit(1, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL", HoldLocation[cp]);
    DoActions(SetInvincibility(Enable, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL"))
    # (Line 294) RemoveUnitAt(1, "Zerg Devourer", "Anywhere", cp);
    DoActions(MoveUnit(1, "Zerg Devourer", cp, "[Skill]Unit_Wait_ALL", HoldLocation[cp]))
    # (Line 295) }
    DoActions(RemoveUnitAt(1, "Zerg Devourer", "Anywhere", cp))
    # (Line 297) function HoldPosition(cp)

# (Line 298) {
@EUDFunc
def HoldPosition(cp):
    # (Line 299) Slow(cp);
    Slow(cp)
    # (Line 300) MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere");
    # (Line 301) Order(heroID[cp], cp, "Anywhere", Move, HoldLocation[cp]);
    DoActions(MoveLocation(HoldLocation[cp], heroID[cp], cp, "Anywhere"))
    # (Line 302) }
    DoActions(Order(heroID[cp], cp, "Anywhere", Move, HoldLocation[cp]))
    # (Line 304) function DotShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 305) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def DotShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 306) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 307) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 308) }
    # (Line 310) function DotShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 311) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def DotShape(cp, count_1, Unit, x, y):
    # (Line 312) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 313) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 314) }
    # (Line 316) function SkillWait(cp, count)

# (Line 317) {
@EUDFunc
def SkillWait(cp, count_1):
    # (Line 318) delay[cp] = count/83 + 1;
    _ARRW(delay, cp) << (count_1 // 83 + 1)
    # (Line 319) }
    # (Line 321) function SkillWaitB(cp, count)

# (Line 322) {
@EUDFunc
def SkillWaitB(cp, count_1):
    # (Line 323) delayB[cp] = count/83 + 1;
    _ARRW(delayB, cp) << (count_1 // 83 + 1)
    # (Line 324) }
    # (Line 326) function SkillWaitC(cp, count)

# (Line 327) {
@EUDFunc
def SkillWaitC(cp, count_1):
    # (Line 328) delayC[cp] = count/83 + 1;
    _ARRW(delayC, cp) << (count_1 // 83 + 1)
    # (Line 329) }
    # (Line 331) function SkillEnd(cp)

# (Line 332) {
@EUDFunc
def SkillEnd(cp):
    # (Line 333) count[cp] = 999;
    _ARRW(count, cp) << (999)
    # (Line 335) v.Shield[cp] = 0;
    _ARRW(v.Shield, cp) << (0)
    # (Line 336) SetDeaths(cp, SetTo, 0, " `ShieldRecharge");
    # (Line 338) if (ally[cp] == 1)
    DoActions(SetDeaths(cp, SetTo, 0, " `ShieldRecharge"))
    if EUDIf()(ally[cp] == 1):
        # (Line 339) {
        # (Line 340) SetSwitch("ComputerAlliy", Clear);
        # (Line 341) if (cp < 3)
        DoActions(SetSwitch("ComputerAlliy", Clear))
        if EUDIf()(cp >= 3, neg=True):
            # (Line 342) {
            # (Line 343) SetAllianceStatus(P8, Enemy);
            # (Line 344) }
            DoActions(SetAllianceStatus(P8, Enemy))
            # (Line 345) else if (cp >= 3)
        if EUDElseIf()(cp >= 3):
            # (Line 346) {
            # (Line 347) SetAllianceStatus(P7, Enemy);
            # (Line 348) }
            DoActions(SetAllianceStatus(P7, Enemy))
            # (Line 349) ally[cp] = 0;
        EUDEndIf()
        _ARRW(ally, cp) << (0)
        # (Line 350) }
        # (Line 351) }
    EUDEndIf()
    # (Line 353) function DoubleShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 354) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def DoubleShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 355) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 356) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 357) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 358) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 359) }
    # (Line 361) function DoubleShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 362) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def DoubleShape(cp, count_1, Unit, x, y):
    # (Line 363) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 364) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 365) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 366) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 367) }
    # (Line 369) function SquareShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, property)

# (Line 370) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def SquareShapeWithProperty(cp, count_1, Unit, x, y, property):
    # (Line 371) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 372) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 373) MoveLoc(heroID[cp], cp, -y, x);
    MoveLoc(heroID[cp], cp, -y, x)
    # (Line 374) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 375) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 376) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 377) MoveLoc(heroID[cp], cp, y, -x);
    MoveLoc(heroID[cp], cp, y, -x)
    # (Line 378) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 379) }
    # (Line 381) function SquareShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 382) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def SquareShape(cp, count_1, Unit, x, y):
    # (Line 383) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 384) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 385) MoveLoc(heroID[cp], cp, -y, x);
    MoveLoc(heroID[cp], cp, -y, x)
    # (Line 386) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 387) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 388) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 389) MoveLoc(heroID[cp], cp, y, -x);
    MoveLoc(heroID[cp], cp, y, -x)
    # (Line 390) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 391) }
    # (Line 393) function SquareShapeAtWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, x, y, distanceX, distanceY, property)

# (Line 394) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def SquareShapeAtWithProperty(cp, count_1, Unit, x, y, distanceX, distanceY, property):
    # (Line 395) MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY);
    MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY)
    # (Line 396) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 397) MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY);
    MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY)
    # (Line 398) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 399) MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY);
    MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY)
    # (Line 400) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 401) MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY);
    MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY)
    # (Line 402) SkillUnitWithProperty(cp, count, Unit, property);
    SkillUnitWithProperty(cp, count_1, Unit, property)
    # (Line 403) }
    # (Line 406) function SquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, x, y, distanceX, distanceY)

# (Line 407) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def SquareShapeAt(cp, count_1, Unit, x, y, distanceX, distanceY):
    # (Line 408) MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY);
    MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY)
    # (Line 409) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 410) MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY);
    MoveLoc(heroID[cp], cp, -y + distanceX, x + distanceY)
    # (Line 411) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 412) MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY);
    MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY)
    # (Line 413) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 414) MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY);
    MoveLoc(heroID[cp], cp, y + distanceX, -x + distanceY)
    # (Line 415) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 416) }
    # (Line 418) function NxNSquareShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, n, interval, property)

# (Line 419) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def NxNSquareShapeWithProperty(cp, count_1, Unit, n, interval, property):
    # (Line 420) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 421) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 422) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 423) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 425) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 426) {
        # (Line 427) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 428) {
            # (Line 429) MoveLoc(heroID[cp] ,cp, 0, 0);
            MoveLoc(heroID[cp], cp, 0, 0)
            # (Line 430) SkillUnitWithProperty(cp, count, Unit, property);
            SkillUnitWithProperty(cp, count_1, Unit, property)
            # (Line 431) }
            # (Line 432) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 433) {
            # (Line 434) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 435) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 436) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 437) {
                # (Line 438) SquareShapeWithProperty(cp, count, Unit, distance, destY * interval - distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, distance_1, destY * interval - distance_1, property)
                # (Line 439) }
                # (Line 440) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 441) {
                # (Line 442) SquareShapeWithProperty(cp, count, Unit, destX * interval - distance, distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval - distance_1, distance_1, property)
                # (Line 443) }
                # (Line 444) else
                # (Line 445) {
            if EUDElse()():
                # (Line 446) SquareShapeWithProperty(cp, count, Unit, destX * interval - distance, destY * interval - distance, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1, property)
                # (Line 447) }
                # (Line 448) }
            EUDEndIf()
            # (Line 449) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 450) {
            # (Line 451) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 452) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 454) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 455) {
                # (Line 456) MoveLoc(heroID[cp] ,cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 457) SkillUnitWithProperty(cp, count, Unit, property);
                SkillUnitWithProperty(cp, count_1, Unit, property)
                # (Line 458) }
                # (Line 459) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 460) {
                # (Line 461) SquareShapeWithProperty(cp, count, Unit, destX * interval, 0, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval, 0, property)
                # (Line 462) }
                # (Line 463) else
                # (Line 464) {
            if EUDElse()():
                # (Line 465) SquareShapeWithProperty(cp, count, Unit, destX * interval + interval, destY * interval, property);
                SquareShapeWithProperty(cp, count_1, Unit, destX * interval + interval, destY * interval, property)
                # (Line 466) }
                # (Line 468) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 469) {
                # (Line 470) SquareShapeWithProperty(cp, count, Unit, (n / 2) * interval, 0, property);
                SquareShapeWithProperty(cp, count_1, Unit, (n // 2) * interval, 0, property)
                # (Line 471) }
                # (Line 472) }
            EUDEndIf()
            # (Line 473) }
        EUDEndIf()
        # (Line 474) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 476) function NxNSquareShape(cp : TrgPlayer, count, Unit : TrgUnit, n, interval)

# (Line 477) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def NxNSquareShape(cp, count_1, Unit, n, interval):
    # (Line 478) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 479) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 480) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 481) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 483) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 484) {
        # (Line 485) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 486) {
            # (Line 487) MoveLoc(heroID[cp] ,cp, 0, 0);
            MoveLoc(heroID[cp], cp, 0, 0)
            # (Line 488) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 489) }
            # (Line 490) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 491) {
            # (Line 492) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 493) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 494) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 495) {
                # (Line 496) SquareShape(cp, count, Unit, distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, distance_1, destY * interval - distance_1)
                # (Line 497) }
                # (Line 498) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 499) {
                # (Line 500) SquareShape(cp, count, Unit, destX * interval - distance, distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, distance_1)
                # (Line 501) }
                # (Line 502) else
                # (Line 503) {
            if EUDElse()():
                # (Line 504) SquareShape(cp, count, Unit, destX * interval - distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1)
                # (Line 505) }
                # (Line 506) }
            EUDEndIf()
            # (Line 507) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 508) {
            # (Line 509) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 510) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 512) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 513) {
                # (Line 514) MoveLoc(heroID[cp] ,cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 515) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 516) }
                # (Line 517) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 518) {
                # (Line 519) SquareShape(cp, count, Unit, destX * interval, 0);
                SquareShape(cp, count_1, Unit, destX * interval, 0)
                # (Line 520) }
                # (Line 521) else
                # (Line 522) {
            if EUDElse()():
                # (Line 523) SquareShape(cp, count, Unit, destX * interval + interval, destY * interval);
                SquareShape(cp, count_1, Unit, destX * interval + interval, destY * interval)
                # (Line 524) }
                # (Line 526) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 527) {
                # (Line 528) SquareShape(cp, count, Unit, (n / 2) * interval, 0);
                SquareShape(cp, count_1, Unit, (n // 2) * interval, 0)
                # (Line 529) }
                # (Line 530) }
            EUDEndIf()
            # (Line 531) }
        EUDEndIf()
        # (Line 532) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 535) function NxNSquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, n, interval, distanceX, distanceY)

# (Line 536) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def NxNSquareShapeAt(cp, count_1, Unit, n, interval, distanceX, distanceY):
    # (Line 537) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 538) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 539) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 540) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 542) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 543) {
        # (Line 544) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 545) {
            # (Line 546) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
            MoveLoc(heroID[cp], cp, distanceX, distanceY)
            # (Line 547) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 548) }
            # (Line 549) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 550) {
            # (Line 551) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 552) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 554) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 555) {
                # (Line 556) SquareShapeAt(cp, count, Unit, distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 557) }
                # (Line 558) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 559) {
                # (Line 560) SquareShapeAt(cp, count, Unit, destX * interval - distance, distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, distance_1, distanceX, distanceY)
                # (Line 561) }
                # (Line 562) else
                # (Line 563) {
            if EUDElse()():
                # (Line 564) SquareShapeAt(cp, count, Unit, destX * interval - distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 565) }
                # (Line 566) }
            EUDEndIf()
            # (Line 567) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 568) {
            # (Line 569) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 570) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 572) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 573) {
                # (Line 574) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
                MoveLoc(heroID[cp], cp, distanceX, distanceY)
                # (Line 575) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 576) }
                # (Line 577) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 578) {
                # (Line 579) SquareShapeAt(cp, count, Unit, destX * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval, 0, distanceX, distanceY)
                # (Line 580) }
                # (Line 581) else
                # (Line 582) {
            if EUDElse()():
                # (Line 583) SquareShapeAt(cp, count, Unit, destX * interval + interval, destY * interval, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval + interval, destY * interval, distanceX, distanceY)
                # (Line 584) }
                # (Line 586) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 587) {
                # (Line 588) SquareShapeAt(cp, count, Unit, (n / 2) * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, (n // 2) * interval, 0, distanceX, distanceY)
                # (Line 589) }
                # (Line 590) }
            EUDEndIf()
            # (Line 591) }
        EUDEndIf()
        # (Line 592) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 594) function LineShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distance)

# (Line 595) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def LineShape(cp, count_1, Unit, degree, n, interval, distance_1):
    # (Line 596) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 598) Table_Sin(cp, degree + 90, distance);
    Table_Sin(cp, degree + 90, distance_1)
    # (Line 599) Table_Cos(cp, degree + 90, distance);
    Table_Cos(cp, degree + 90, distance_1)
    # (Line 600) var x_initial = CosAngle[cp];
    x_initial = EUDVariable()
    x_initial << (CosAngle[cp])
    # (Line 601) var y_initial = SinAngle[cp];
    y_initial = EUDVariable()
    y_initial << (SinAngle[cp])
    # (Line 603) if (n % 2 == 0)
    if EUDIf()(n % 2 == 0):
        # (Line 604) {
        # (Line 605) Table_Sin(cp, degree, interval * (n / 2) - interval / 2);
        Table_Sin(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 606) Table_Cos(cp, degree, interval * (n / 2) - interval / 2);
        Table_Cos(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 607) }
        # (Line 608) else
        # (Line 609) {
    if EUDElse()():
        # (Line 610) Table_Sin(cp, degree, interval * (n / 2));
        Table_Sin(cp, degree, interval * (n // 2))
        # (Line 611) Table_Cos(cp, degree, interval * (n / 2));
        Table_Cos(cp, degree, interval * (n // 2))
        # (Line 612) }
        # (Line 614) x_initial = x_initial + CosAngle[cp];
    EUDEndIf()
    x_initial << (x_initial + CosAngle[cp])
    # (Line 615) y_initial = y_initial + SinAngle[cp];
    y_initial << (y_initial + SinAngle[cp])
    # (Line 617) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 618) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 620) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t3():
            i.__iadd__(1)
        # (Line 621) {
        # (Line 622) MoveLoc(heroID[cp], cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp]);
        MoveLoc(heroID[cp], cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp])
        # (Line 623) SkillUnit(cp, count, Unit);
        SkillUnit(cp, count_1, Unit)
        # (Line 624) }
        # (Line 626) }
        EUDSetContinuePoint()
        _t3()
    EUDEndWhile()
    # (Line 629) function LineShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distanceX, distanceY)

# (Line 630) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def LineShapeAt(cp, count_1, Unit, degree, n, interval, distanceX, distanceY):
    # (Line 631) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 632) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 634) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 635) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 637) var gradientX = CosAngle[cp];
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp])
    # (Line 638) var gradientY = SinAngle[cp] ;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp])
    # (Line 640) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 642) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 643) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 645) for (; i <= n / 2; i++)
    if EUDWhile()(i <= n // 2):
        def _t2():
            i.__iadd__(1)
        # (Line 646) {
        # (Line 647) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 648) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 650) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 651) {
            # (Line 652) MoveLoc(heroID[cp], cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY);
            MoveLoc(heroID[cp], cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY)
            # (Line 653) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 654) MoveLoc(heroID[cp], cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY);
            MoveLoc(heroID[cp], cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY)
            # (Line 655) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 656) }
            # (Line 657) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 658) {
            # (Line 659) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 660) {
                # (Line 661) MoveLoc(heroID[cp], cp, distanceX, distanceY);
                MoveLoc(heroID[cp], cp, distanceX, distanceY)
                # (Line 662) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 663) }
                # (Line 664) else
                # (Line 665) {
            if EUDElse()():
                # (Line 666) MoveLoc(heroID[cp], cp, -Angle_X + distanceX, Angle_Y + distanceY);
                MoveLoc(heroID[cp], cp, -Angle_X + distanceX, Angle_Y + distanceY)
                # (Line 667) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 668) MoveLoc(heroID[cp], cp, Angle_X + distanceX, -Angle_Y + distanceY);
                MoveLoc(heroID[cp], cp, Angle_X + distanceX, -Angle_Y + distanceY)
                # (Line 669) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 670) }
                # (Line 672) }
            EUDEndIf()
            # (Line 673) }
        EUDEndIf()
        # (Line 675) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 677) function CrossShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval)

# (Line 678) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def CrossShape(cp, count_1, Unit, degree, n, interval):
    # (Line 679) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 680) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 682) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 683) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 685) var gradientX = CosAngle[cp] / 2;
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp] // 2)
    # (Line 686) var gradientY = SinAngle[cp] / 2;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp] // 2)
    # (Line 688) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 690) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 691) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 693) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 694) {
        # (Line 695) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 696) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 698) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 699) {
            # (Line 700) SquareShape(cp, count, Unit, Angle_X + gradientX, Angle_Y + gradientY);
            SquareShape(cp, count_1, Unit, Angle_X + gradientX, Angle_Y + gradientY)
            # (Line 701) }
            # (Line 702) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 703) {
            # (Line 704) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 705) {
                # (Line 706) MoveLoc(heroID[cp], cp, 0, 0);
                MoveLoc(heroID[cp], cp, 0, 0)
                # (Line 707) }
                # (Line 708) else
                # (Line 709) {
            if EUDElse()():
                # (Line 710) SquareShape(cp, count, Unit, Angle_X, Angle_Y);
                SquareShape(cp, count_1, Unit, Angle_X, Angle_Y)
                # (Line 711) }
                # (Line 712) }
            EUDEndIf()
            # (Line 713) }
        EUDEndIf()
        # (Line 714) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 716) function Table_Sin(cp, degree, distance)

# (Line 717) {
@EUDFunc
def Table_Sin(cp, degree, distance_1):
    # (Line 718) degree = degree % 360;
    degree << (degree % 360)
    # (Line 719) SinAngle[cp] = SinTableArray[degree];
    _ARRW(SinAngle, cp) << (SinTableArray[degree])
    # (Line 721) if (SinAngle[cp] > 2147483648)
    if EUDIf()(SinAngle[cp] <= 2147483648, neg=True):
        # (Line 722) {
        # (Line 723) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 724) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 725) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 726) }
        # (Line 727) else
        # (Line 728) {
    if EUDElse()():
        # (Line 729) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 730) }
        # (Line 731) }
    EUDEndIf()
    # (Line 733) function Table_Cos(cp, degree, distance)

# (Line 734) {
@EUDFunc
def Table_Cos(cp, degree, distance_1):
    # (Line 735) degree = degree % 360;
    degree << (degree % 360)
    # (Line 736) CosAngle[cp] = CosTableArray[degree];
    _ARRW(CosAngle, cp) << (CosTableArray[degree])
    # (Line 738) if (CosAngle[cp] > 2147483648)
    if EUDIf()(CosAngle[cp] <= 2147483648, neg=True):
        # (Line 739) {
        # (Line 740) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 741) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 742) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 743) }
        # (Line 744) else
        # (Line 745) {
    if EUDElse()():
        # (Line 746) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 747) }
        # (Line 748) }
    EUDEndIf()
    # (Line 750) function EdgeShapeWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, property)

# (Line 751) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def EdgeShapeWithProperty(cp, count_1, Unit, degree, n, interval, property):
    # (Line 752) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 754) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 755) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 757) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 758) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 760) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 762) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 763) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 765) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 766) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 768) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 769) {
        # (Line 770) DotShapeWithProperty(cp, 1, Unit, 0, 0, property);
        DotShapeWithProperty(cp, 1, Unit, 0, 0, property)
        # (Line 771) }
        # (Line 772) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 773) {
        # (Line 774) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 775) {
            # (Line 776) SquareShapeWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property);
            SquareShapeWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property)
            # (Line 777) }
            # (Line 778) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 779) }
    EUDEndIf()
    # (Line 781) function EdgeShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval)

# (Line 782) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def EdgeShape(cp, count_1, Unit, degree, n, interval):
    # (Line 783) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 785) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 786) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 788) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 789) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 791) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 793) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 794) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 796) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 797) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 799) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 800) {
        # (Line 801) DotShape(cp, 1, Unit, 0, 0);
        DotShape(cp, 1, Unit, 0, 0)
        # (Line 802) }
        # (Line 803) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 804) {
        # (Line 805) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 806) {
            # (Line 807) SquareShape(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i));
            SquareShape(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i))
            # (Line 808) }
            # (Line 809) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 810) }
    EUDEndIf()
    # (Line 811) function EdgeShapeAtWithProperty(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y, property)

# (Line 812) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None, None])
def EdgeShapeAtWithProperty(cp, count_1, Unit, degree, n, interval, x, y, property):
    # (Line 813) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 815) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 816) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 818) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 819) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 821) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 823) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 824) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 826) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 827) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 829) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 830) {
        # (Line 831) DotShapeWithProperty(cp, 1, Unit, x, y, property);
        DotShapeWithProperty(cp, 1, Unit, x, y, property)
        # (Line 832) }
        # (Line 833) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 834) {
        # (Line 835) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 836) {
            # (Line 837) SquareShapeAtWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y, property);
            SquareShapeAtWithProperty(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y, property)
            # (Line 838) }
            # (Line 839) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 840) }
    EUDEndIf()
    # (Line 842) function EdgeShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, x, y)

# (Line 843) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def EdgeShapeAt(cp, count_1, Unit, degree, n, interval, x, y):
    # (Line 844) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 846) Table_Sin(cp, degree, interval * 14 / 10);
    Table_Sin(cp, degree, interval * 14 // 10)
    # (Line 847) Table_Cos(cp, degree, interval * 14 / 10);
    Table_Cos(cp, degree, interval * 14 // 10)
    # (Line 849) var x_o = CosAngle[cp];
    x_o = EUDVariable()
    x_o << (CosAngle[cp])
    # (Line 850) var y_o = SinAngle[cp];
    y_o = EUDVariable()
    y_o << (SinAngle[cp])
    # (Line 852) var distance = (interval * 2) / (n - 1);
    distance_1 = EUDVariable()
    distance_1 << ((interval * 2) // (n - 1))
    # (Line 854) Table_Sin(cp, degree + 45, distance);
    Table_Sin(cp, degree + 45, distance_1)
    # (Line 855) Table_Cos(cp, degree + 45, distance);
    Table_Cos(cp, degree + 45, distance_1)
    # (Line 857) var distance_x = CosAngle[cp];
    distance_x = EUDVariable()
    distance_x << (CosAngle[cp])
    # (Line 858) var distance_y = SinAngle[cp];
    distance_y = EUDVariable()
    distance_y << (SinAngle[cp])
    # (Line 860) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 861) {
        # (Line 862) DotShape(cp, 1, Unit, x, y);
        DotShape(cp, 1, Unit, x, y)
        # (Line 863) }
        # (Line 864) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 865) {
        # (Line 866) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 867) {
            # (Line 868) SquareShapeAt(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y);
            SquareShapeAt(cp, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), x, y)
            # (Line 869) }
            # (Line 870) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 871) }
    EUDEndIf()
