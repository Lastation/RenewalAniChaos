// SCA Variable
const SCAStatus 		= PVariable();

const Exp_Total		= PVariable();		// Exp 토탈
const CS_Total		= PVariable();		// CS 토탈
const Kill_All			= PVariable();		// Kill 토탈
const Kill_Total		= EUDArray(160);	// Kill 캐릭터 토탈
const Death_Total	= EUDArray(160);	// Death 토탈

const Exp_Group 	= EUDArray(160);	// Exp
const CS_Group 		= EUDArray(160);	// CS

const Exp_Player		= PVariable();		// 현재 캐릭터 경험치
const CS_Player		= PVariable();		// 현재 캐릭터 CS

const Kill_Score		= PVariable();		// 킬
const Death_Score	= PVariable();		// 사망

const AnnounceList = PVariable();

// Common Variable
const Unit_Death 	= PVariable();
const Hero_Text 		= PVariable();

const Unit_ID 		= PVariable();
const PlayerID 		= PVariable();

const Hero_Index = PVariable();
const Unit_PrevIndex = PVariable();
const Unit_NowIndex = PVariable();

const Level = PVariable();
const Unique_Cool = PVariable();

const EXP_Need = PVariable();
const EXP_Now = PVariable();
const Potion = PVariable();
const DeathCount = PVariable();

const Sound_Text1 		= EUDArray(16);
const Sound_Text2 		= EUDArray(16);
const Sound_Text3 		= EUDArray(16);
const Sound_Text4 		= EUDArray(16);

const Sound_Text_Uilti 	= EUDArray(16);
const Sound_Text_Uniq 	= EUDArray(16);

const Hero_Num 		= PVariable();

const Mana_Property 	= PVariable();
const Upgrade_Armor 	= PVariable();
const DeathOre_Text 	= PVariable();

const Unit_HP 		= PVariable();		// 건물 체력
const BuildingText 	= PVariable();		// 건물 텍스트
const BuildingIndex  = PVariable();		// 건물 인덱스

const DeathOre 		= PVariable();
const Team_Num 	= PVariable();

const Atk		 	= PVariable();		//영웅 공격력
const Dfs		 	= PVariable();		//영웅 방어력
const Shield		 	= PVariable();		//영웅 쉴드

const Hero_Name = 
[Db("#REF"),
Db("루살카 슈베게린"),
Db("크톨리 노타 세니오리스"),
Db("텐카와 마이히메"),
Db("히나나위 텐시"),
Db("소네 미유키"),
Db("네게브"),
Db("세실리아"),
Db("후타바 안즈"),
Db("호토 모카"),
Db("수수께끼의 얼터에고 Λ"),
Db("토키사키 쿠루미"),
Db("류즈"),
Db("사쿠라"),
Db("야시로 가쿠"),
Db("린"),
Db("세라핌"),
Db("키아나 카스라나"),
Db("이와후네 텐케이"),
Db("마르고트 나이트 & 마르가 나르제"),
Db("에메트셀크"),
Db("마젤란"),
Db("유우키 유우나"),
Db("시이나 마유리"),
Db("나나미 치아키"),
Db("밀림 나바"),
Db("츠바쿠로 유메"),
Db("치쿠조인 마가네"),
Db("오다 사쿠노스케"),
Db("에키드나"),
Db("니와 료카"),
Db("박 일표"),
Db("#REF")];

