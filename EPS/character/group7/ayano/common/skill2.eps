import PluginVariables as msqcvar;
import variable as v;
import func.trig as trg;

function main(playerID)
{
	if (v.P_WaitMain[playerID] == 0)
	{
		if (v.P_CountMain[playerID] == 0)
		{
			RemoveUnitAt(All, "40 + 1n Goliath", "Anywhere", playerID);
			RemoveUnitAt(All, "40 + 1n Wraith", "Anywhere", playerID);
			if (v.P_LoopMain[playerID] <= 2)
			{
				trg.Shape_Line(playerID,1,"40 + 1n Goliath",60+30*v.P_LoopMain[playerID],2,224,0);
				trg.Shape_Line(playerID,1,"40 + 1n Goliath",150+30*v.P_LoopMain[playerID],2,224,0);
				trg.Shape_Line(playerID,1,"40 + 1n Wraith",30+30*v.P_LoopMain[playerID],2,224,0);
				trg.Shape_Line(playerID,1,"40 + 1n Wraith",120+30*v.P_LoopMain[playerID],2,224,0);
				trg.MoveLoc(v.P_UnitID[playerID],playerID,0,0);
				MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
				MoveUnit(All,"40 + 1n Goliath", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]);
				Order("40 + 1n Goliath",playerID,"Anywhere",Attack,v.P_LocationID[playerID]);
				Order("40 + 1n Wraith",playerID,"Anywhere",Attack,v.P_LocationID[playerID]);
				trg.Main_Wait(320);
			}
			else if (v.P_LoopMain[playerID] == 3)
			{
				trg.SkillEnd();
			}
			v.P_LoopMain[playerID] += 1;
		}
	}
}