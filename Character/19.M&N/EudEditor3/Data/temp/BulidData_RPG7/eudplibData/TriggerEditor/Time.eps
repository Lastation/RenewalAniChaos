import timeline as tL;
import Main as m;
import Variables as v;

const s = StringBuffer(1000);

function beforeTriggerExec()
{
	foreach(t : tL.Timeline(24 * 35 / 10))
	{
		if(t == (24 * 35 / 10) - 1)
		{
			SetVariables(v.second, 1, list(Add));
		}
	}
	
	EUDPlayerLoop()();
	if(getcurpl() < 7)
	{
		//if(v.second > 5) SetMemoryEPD(EPD(0x657A9C), SetTo, 25);
	}
	EUDEndPlayerLoop();
}