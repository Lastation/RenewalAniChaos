import setting.unit as cu;
import setting.announce as ac;
import setting.random as rand;
import text as at;

function onPluginStart() 
{
	randomize();
	
	cu.CreateUnitA();		// P8 유닛 생성 1 ~ 30
	cu.CreateUnitB();			// P8 유닛 생성 31 ~ 60
	//cu.CreateUnitC();		// P8 유닛 생성 61 ~ 90
	//cu.CreateUnitD();		// P8 유닛 생성 91 ~ 120
	//cu.CreateUnitE();			// P8 유닛 생성 121 ~ 150
}

function beforeTriggerExec()
{
	EUDPlayerLoop()();
	
	rand.Random();			// 스위치 랜덤 트리거
	ac.Announce();			// 점수판 , 시작 텍스트 출력
	at.Answer_Text();			// 정답 텍스트 출력
	
	EUDEndPlayerLoop();
}