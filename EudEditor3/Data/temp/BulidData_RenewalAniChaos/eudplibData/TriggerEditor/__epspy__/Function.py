## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import py_math;
import math
# (Line 2) import Variable as v;
import Variable as v
# (Line 4) const step		= PVariable();			// SkillStep
step = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 5) const count 		= PVariable();			// SkillCount
count = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 6) const loop 		= PVariable();			// SkillLoop
loop = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 7) const delay		= PVariable();			// wait(value)
delay = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 8) const wait		= PVariable();			// SkillWait
wait = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 11) const loopB 		= PVariable();			// SkillLoop2
loopB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 12) const loopC 	= PVariable();			// SkillLoop3
loopC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 13) const delayB	= PVariable();			// wait(value)2
delayB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 14) const delayC	= PVariable();			// wait(value)3
delayC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 16) const location 	= PVariable();			// 로케이션 번호
location = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 17) const heroID 	= PVariable();			// 영웅 유닛 번호
heroID = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 19) const UltimateA	= PVariable();			// 궁극기 1번 소모량
UltimateA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 20) const UltimateB	= PVariable();			// 궁극기 2번 소모량
UltimateB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 21) const UltimateC	= PVariable();			// 궁극기 3번 소모량
UltimateC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 23) const UniqueA	= PVariable();			// 유니크 1번 쿨타임
UniqueA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 25) const distance	= PVariable();			// 거리 관련 변수
distance = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 26) const SinAngle 	= PVariable();			// Sin 변수
SinAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 27) const CosAngle 	= PVariable();			// Cos 변수
CosAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 29) const CosTableArray = EUDArray(py_eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"));
CosTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 30) const SinTableArray 	= EUDArray(py_eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"));
SinTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 32) const Rusaruka_Voice 	= PVariable();
Rusaruka_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 33) const Yuuna_Voice 		= PVariable();
Yuuna_Voice = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 35) function Table_Sin(cp, degree, distance);
# (Line 36) function Table_Cos(cp, degree, distance);
# (Line 38) function Voice_Routine(cp, num)
# (Line 39) {
@EUDFunc
def Voice_Routine(cp, num):
    # (Line 40) switch(v.Hero_Num[cp])
    EUDSwitch(v.Hero_Num[cp])
    # (Line 41) {
    # (Line 42) case 1:
    _t1 = EUDSwitchCase()
    # (Line 43) Rusaruka_Voice[0] = num;
    if _t1(1):
        _ARRW(Rusaruka_Voice, 0) << (num)
        # (Line 44) Rusaruka_Voice[1] = num;
        _ARRW(Rusaruka_Voice, 1) << (num)
        # (Line 45) Rusaruka_Voice[2] = num;
        _ARRW(Rusaruka_Voice, 2) << (num)
        # (Line 46) Rusaruka_Voice[3] = num;
        _ARRW(Rusaruka_Voice, 3) << (num)
        # (Line 47) Rusaruka_Voice[4] = num;
        _ARRW(Rusaruka_Voice, 4) << (num)
        # (Line 48) Rusaruka_Voice[5] = num;
        _ARRW(Rusaruka_Voice, 5) << (num)
        # (Line 49) break;
        EUDBreak()
        # (Line 50) case 22:
    _t2 = EUDSwitchCase()
    # (Line 51) Yuuna_Voice[0] = num;
    if _t2(22):
        _ARRW(Yuuna_Voice, 0) << (num)
        # (Line 52) Yuuna_Voice[1] = num;
        _ARRW(Yuuna_Voice, 1) << (num)
        # (Line 53) Yuuna_Voice[2] = num;
        _ARRW(Yuuna_Voice, 2) << (num)
        # (Line 54) Yuuna_Voice[3] = num;
        _ARRW(Yuuna_Voice, 3) << (num)
        # (Line 55) Yuuna_Voice[4] = num;
        _ARRW(Yuuna_Voice, 4) << (num)
        # (Line 56) Yuuna_Voice[5] = num;
        _ARRW(Yuuna_Voice, 5) << (num)
        # (Line 57) break;
        EUDBreak()
        # (Line 58) }
    # (Line 59) }
    EUDEndSwitch()
    # (Line 62) function MoveLoc(Unit : TrgUnit, cp : TrgPlayer, x, y)

# (Line 63) {
@EUDTypedFunc([TrgUnit, TrgPlayer, None, None])
def MoveLoc(Unit, cp, x, y):
    # (Line 64) MoveLocation(location[cp], Unit, cp, "Anywhere");
    # (Line 65) addloc(location[cp] - 1, x, y);
    DoActions(MoveLocation(location[cp], Unit, cp, "Anywhere"))
    f_addloc(location[cp] - 1, x, y)
    # (Line 66) }
    # (Line 68) function SkillUnit(cp : TrgPlayer, count, Unit : TrgUnit)

# (Line 69) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit])
def SkillUnit(cp, count_1, Unit):
    # (Line 70) CreateUnit(count, Unit, "[Skill]Unit_Wait_1", cp);
    # (Line 71) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(count_1, Unit, "[Skill]Unit_Wait_1", cp))
    # (Line 72) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 73) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location[cp]))
    # (Line 75) function SkillWait(cp, count)

# (Line 76) {
@EUDFunc
def SkillWait(cp, count_1):
    # (Line 77) delay[cp] = count/83 + 1;
    _ARRW(delay, cp) << (count_1 // 83 + 1)
    # (Line 78) }
    # (Line 80) function SkillWaitB(cp, count)

# (Line 81) {
@EUDFunc
def SkillWaitB(cp, count_1):
    # (Line 82) delayB[cp] = count/83 + 1;
    _ARRW(delayB, cp) << (count_1 // 83 + 1)
    # (Line 83) }
    # (Line 85) function SkillWaitC(cp, count)

# (Line 86) {
@EUDFunc
def SkillWaitC(cp, count_1):
    # (Line 87) delayB[cp] = count/83 + 1;
    _ARRW(delayB, cp) << (count_1 // 83 + 1)
    # (Line 88) }
    # (Line 90) function SkillEnd(cp)

# (Line 91) {
@EUDFunc
def SkillEnd(cp):
    # (Line 92) wait[cp] = 12;
    _ARRW(wait, cp) << (12)
    # (Line 93) }
    # (Line 95) function SquareShape(cp : TrgPlayer, count, Unit : TrgUnit, x, y)

# (Line 96) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def SquareShape(cp, count_1, Unit, x, y):
    # (Line 97) MoveLoc(heroID[cp], cp, x, y);
    MoveLoc(heroID[cp], cp, x, y)
    # (Line 98) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 99) MoveLoc(heroID[cp], cp, -y, x);
    MoveLoc(heroID[cp], cp, -y, x)
    # (Line 100) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 101) MoveLoc(heroID[cp], cp, -x, -y);
    MoveLoc(heroID[cp], cp, -x, -y)
    # (Line 102) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 103) MoveLoc(heroID[cp], cp, y, -x);
    MoveLoc(heroID[cp], cp, y, -x)
    # (Line 104) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 105) }
    # (Line 107) function SquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, x, y, distanceX, distanceY)

# (Line 108) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def SquareShapeAt(cp, count_1, Unit, x, y, distanceX, distanceY):
    # (Line 109) MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY);
    MoveLoc(heroID[cp], cp, x + distanceX, y + distanceY)
    # (Line 110) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 111) MoveLoc(heroID[cp], cp, -y + distanceY, x + distanceX);
    MoveLoc(heroID[cp], cp, -y + distanceY, x + distanceX)
    # (Line 112) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 113) MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY);
    MoveLoc(heroID[cp], cp, -x + distanceX, -y + distanceY)
    # (Line 114) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 115) MoveLoc(heroID[cp], cp, y + distanceY, -x + distanceX);
    MoveLoc(heroID[cp], cp, y + distanceY, -x + distanceX)
    # (Line 116) SkillUnit(cp, count, Unit);
    SkillUnit(cp, count_1, Unit)
    # (Line 117) }
    # (Line 119) function NxNSquareShape(cp : TrgPlayer, count, Unit : TrgUnit, n, interval)

# (Line 120) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def NxNSquareShape(cp, count_1, Unit, n, interval):
    # (Line 121) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 122) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 123) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 124) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 126) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 127) {
        # (Line 128) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 129) {
            # (Line 130) MoveLoc(heroID[cp] ,cp, 0, 0);
            MoveLoc(heroID[cp], cp, 0, 0)
            # (Line 131) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 132) }
            # (Line 133) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 134) {
            # (Line 135) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 136) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 137) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 138) {
                # (Line 139) SquareShape(cp, count, Unit, distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, distance_1, destY * interval - distance_1)
                # (Line 140) }
                # (Line 141) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 142) {
                # (Line 143) SquareShape(cp, count, Unit, destX * interval - distance, distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, distance_1)
                # (Line 144) }
                # (Line 145) else
                # (Line 146) {
            if EUDElse()():
                # (Line 147) SquareShape(cp, count, Unit, destX * interval - distance, destY * interval - distance);
                SquareShape(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1)
                # (Line 148) }
                # (Line 149) }
            EUDEndIf()
            # (Line 150) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 151) {
            # (Line 152) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 153) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 155) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 156) {
                # (Line 157) MoveLoc(Unit ,cp, 0, 0);
                MoveLoc(Unit, cp, 0, 0)
                # (Line 158) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 159) }
                # (Line 160) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 161) {
                # (Line 162) SquareShape(cp, count, Unit, destX * interval, 0);
                SquareShape(cp, count_1, Unit, destX * interval, 0)
                # (Line 163) }
                # (Line 164) else
                # (Line 165) {
            if EUDElse()():
                # (Line 166) SquareShape(cp, count, Unit, destX * interval + interval, destY * interval);
                SquareShape(cp, count_1, Unit, destX * interval + interval, destY * interval)
                # (Line 167) }
                # (Line 169) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 170) {
                # (Line 171) SquareShape(cp, count, Unit, (n / 2) * interval, 0);
                SquareShape(cp, count_1, Unit, (n // 2) * interval, 0)
                # (Line 172) }
                # (Line 173) }
            EUDEndIf()
            # (Line 174) }
        EUDEndIf()
        # (Line 175) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 178) function NxNSquareShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, n, interval, distanceX, distanceY)

# (Line 179) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def NxNSquareShapeAt(cp, count_1, Unit, n, interval, distanceX, distanceY):
    # (Line 180) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 181) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 182) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 183) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 185) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 186) {
        # (Line 187) if (n == 1)
        if EUDIf()(n == 1):
            # (Line 188) {
            # (Line 189) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
            MoveLoc(heroID[cp], cp, distanceX, distanceY)
            # (Line 190) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 191) }
            # (Line 192) else if (n % 2 == 0)
        if EUDElseIf()(n % 2 == 0):
            # (Line 193) {
            # (Line 194) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 195) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 197) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 198) {
                # (Line 199) SquareShapeAt(cp, count, Unit, distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 200) }
                # (Line 201) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 202) {
                # (Line 203) SquareShapeAt(cp, count, Unit, destX * interval - distance, distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, distance_1, distanceX, distanceY)
                # (Line 204) }
                # (Line 205) else
                # (Line 206) {
            if EUDElse()():
                # (Line 207) SquareShapeAt(cp, count, Unit, destX * interval - distance, destY * interval - distance, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval - distance_1, destY * interval - distance_1, distanceX, distanceY)
                # (Line 208) }
                # (Line 209) }
            EUDEndIf()
            # (Line 210) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 211) {
            # (Line 212) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 213) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 215) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 216) {
                # (Line 217) MoveLoc(heroID[cp] ,cp, distanceX, distanceY);
                MoveLoc(heroID[cp], cp, distanceX, distanceY)
                # (Line 218) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 219) }
                # (Line 220) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 221) {
                # (Line 222) SquareShapeAt(cp, count, Unit, destX * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval, 0, distanceX, distanceY)
                # (Line 223) }
                # (Line 224) else
                # (Line 225) {
            if EUDElse()():
                # (Line 226) SquareShapeAt(cp, count, Unit, destX * interval + interval, destY * interval, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, destX * interval + interval, destY * interval, distanceX, distanceY)
                # (Line 227) }
                # (Line 229) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 230) {
                # (Line 231) SquareShapeAt(cp, count, Unit, (n / 2) * interval, 0, distanceX, distanceY);
                SquareShapeAt(cp, count_1, Unit, (n // 2) * interval, 0, distanceX, distanceY)
                # (Line 232) }
                # (Line 233) }
            EUDEndIf()
            # (Line 234) }
        EUDEndIf()
        # (Line 235) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 237) function LineShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distance)

# (Line 238) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def LineShape(cp, count_1, Unit, degree, n, interval, distance_1):
    # (Line 239) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 241) Table_Sin(cp, degree + 90, distance);
    Table_Sin(cp, degree + 90, distance_1)
    # (Line 242) Table_Cos(cp, degree + 90, distance);
    Table_Cos(cp, degree + 90, distance_1)
    # (Line 243) var x_initial = CosAngle[cp];
    x_initial = EUDVariable()
    x_initial << (CosAngle[cp])
    # (Line 244) var y_initial = SinAngle[cp];
    y_initial = EUDVariable()
    y_initial << (SinAngle[cp])
    # (Line 246) if (n / 2 == 0)
    if EUDIf()(n // 2 == 0):
        # (Line 247) {
        # (Line 248) Table_Sin(cp, degree, interval * (n / 2) - interval / 2);
        Table_Sin(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 249) Table_Cos(cp, degree, interval * (n / 2) - interval / 2);
        Table_Cos(cp, degree, interval * (n // 2) - interval // 2)
        # (Line 250) }
        # (Line 251) else
        # (Line 252) {
    if EUDElse()():
        # (Line 253) Table_Sin(cp, degree, interval * (n / 2));
        Table_Sin(cp, degree, interval * (n // 2))
        # (Line 254) Table_Cos(cp, degree, interval * (n / 2));
        Table_Cos(cp, degree, interval * (n // 2))
        # (Line 255) }
        # (Line 257) x_initial = x_initial + CosAngle[cp];
    EUDEndIf()
    x_initial << (x_initial + CosAngle[cp])
    # (Line 258) y_initial = y_initial + SinAngle[cp];
    y_initial << (y_initial + SinAngle[cp])
    # (Line 260) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 261) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 263) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t3():
            i.__iadd__(1)
        # (Line 264) {
        # (Line 265) MoveLoc(Unit, cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp]);
        MoveLoc(Unit, cp, x_initial - i * CosAngle[cp], y_initial - i * SinAngle[cp])
        # (Line 266) SkillUnit(cp, count, Unit);
        SkillUnit(cp, count_1, Unit)
        # (Line 267) }
        # (Line 269) }
        EUDSetContinuePoint()
        _t3()
    EUDEndWhile()
    # (Line 272) function LineShapeAt(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, distanceX, distanceY)

# (Line 273) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def LineShapeAt(cp, count_1, Unit, degree, n, interval, distanceX, distanceY):
    # (Line 274) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 275) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 277) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 278) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 280) var gradientX = CosAngle[cp] / 2;
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp] // 2)
    # (Line 281) var gradientY = SinAngle[cp] / 2;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp] // 2)
    # (Line 283) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 285) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 286) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 288) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 289) {
        # (Line 290) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 291) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 293) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 294) {
            # (Line 295) MoveLoc(Unit, cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY);
            MoveLoc(Unit, cp, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY)
            # (Line 296) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 297) MoveLoc(Unit, cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY);
            MoveLoc(Unit, cp, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY)
            # (Line 298) SkillUnit(cp, count, Unit);
            SkillUnit(cp, count_1, Unit)
            # (Line 299) }
            # (Line 300) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 301) {
            # (Line 302) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 303) {
                # (Line 304) MoveLoc(Unit, cp, distanceX, distanceY);
                MoveLoc(Unit, cp, distanceX, distanceY)
                # (Line 305) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 306) }
                # (Line 307) else
                # (Line 308) {
            if EUDElse()():
                # (Line 309) MoveLoc(Unit, cp, -Angle_X + distanceX, Angle_Y + distanceY);
                MoveLoc(Unit, cp, -Angle_X + distanceX, Angle_Y + distanceY)
                # (Line 310) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 311) MoveLoc(Unit, cp, Angle_X + distanceX, -Angle_Y + distanceY);
                MoveLoc(Unit, cp, Angle_X + distanceX, -Angle_Y + distanceY)
                # (Line 312) SkillUnit(cp, count, Unit);
                SkillUnit(cp, count_1, Unit)
                # (Line 313) }
                # (Line 314) }
            EUDEndIf()
            # (Line 315) }
        EUDEndIf()
        # (Line 317) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 319) function CrossShape(cp : TrgPlayer, count, Unit : TrgUnit, degree, n, interval)

# (Line 320) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def CrossShape(cp, count_1, Unit, degree, n, interval):
    # (Line 321) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 322) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 324) Table_Sin(cp, degree, distance);
    Table_Sin(cp, degree, distance_1)
    # (Line 325) Table_Cos(cp, degree, distance);
    Table_Cos(cp, degree, distance_1)
    # (Line 327) var gradientX = CosAngle[cp] / 2;
    gradientX = EUDVariable()
    gradientX << (CosAngle[cp] // 2)
    # (Line 328) var gradientY = SinAngle[cp] / 2;
    gradientY = EUDVariable()
    gradientY << (SinAngle[cp] // 2)
    # (Line 330) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 332) Table_Sin(cp, degree, interval);
    Table_Sin(cp, degree, interval)
    # (Line 333) Table_Cos(cp, degree, interval);
    Table_Cos(cp, degree, interval)
    # (Line 335) for (; i < n; i++)
    if EUDWhile()(i >= n, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 336) {
        # (Line 337) Angle_X = CosAngle[cp] * i;
        Angle_X << (CosAngle[cp] * i)
        # (Line 338) Angle_Y = SinAngle[cp] * i;
        Angle_Y << (SinAngle[cp] * i)
        # (Line 340) if (n % 2 == 0 && i != n / 2)
        if EUDIf()(EUDSCAnd()(n % 2 == 0)(i == n // 2, neg=True)()):
            # (Line 341) {
            # (Line 342) SquareShape(cp, count, Unit, Angle_X + gradientX, Angle_Y + gradientY);
            SquareShape(cp, count_1, Unit, Angle_X + gradientX, Angle_Y + gradientY)
            # (Line 343) }
            # (Line 344) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 345) {
            # (Line 346) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 347) {
                # (Line 348) MoveLoc(Unit, cp, 0, 0);
                MoveLoc(Unit, cp, 0, 0)
                # (Line 349) }
                # (Line 350) else
                # (Line 351) {
            if EUDElse()():
                # (Line 352) SquareShape(cp, count, Unit, Angle_X, Angle_Y);
                SquareShape(cp, count_1, Unit, Angle_X, Angle_Y)
                # (Line 353) }
                # (Line 354) }
            EUDEndIf()
            # (Line 355) }
        EUDEndIf()
        # (Line 356) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 358) function Table_Sin(cp, degree, distance)

# (Line 359) {
@EUDFunc
def Table_Sin(cp, degree, distance_1):
    # (Line 360) degree = degree % 360;
    degree << (degree % 360)
    # (Line 361) SinAngle[cp] = SinTableArray[degree];
    _ARRW(SinAngle, cp) << (SinTableArray[degree])
    # (Line 363) if (SinAngle[cp] > 2147483648)
    if EUDIf()(SinAngle[cp] <= 2147483648, neg=True):
        # (Line 364) {
        # (Line 365) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 366) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 367) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 368) }
        # (Line 369) else
        # (Line 370) {
    if EUDElse()():
        # (Line 371) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 372) }
        # (Line 373) }
    EUDEndIf()
    # (Line 375) function Table_Cos(cp, degree, distance)

# (Line 376) {
@EUDFunc
def Table_Cos(cp, degree, distance_1):
    # (Line 377) degree = degree % 360;
    degree << (degree % 360)
    # (Line 378) CosAngle[cp] = CosTableArray[degree];
    _ARRW(CosAngle, cp) << (CosTableArray[degree])
    # (Line 380) if (CosAngle[cp] > 2147483648)
    if EUDIf()(CosAngle[cp] <= 2147483648, neg=True):
        # (Line 381) {
        # (Line 382) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 383) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 384) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 385) }
        # (Line 386) else
        # (Line 387) {
    if EUDElse()():
        # (Line 388) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 389) }
        # (Line 390) }
    EUDEndIf()
