## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import py_math;
import math
# (Line 3) const step		= PVariable();			// 179번 유닛 `SkillStep
step = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 4) const count 		= PVariable();			// 181번 유닛 `SkillCount
count = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 5) const loop 		= PVariable();			// 212번 유닛 `SkillLoop
loop = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 7) const location 	= PVariable();			// 로케이션 번호
location = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 8) const heroID 	= PVariable();			// 영웅 유닛 번호
heroID = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 10) const UltimateA	= PVariable();			// 궁극기 1번 소모량
UltimateA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 11) const UltimateB	= PVariable();			// 궁극기 2번 소모량
UltimateB = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 12) const UltimateC	= PVariable();			// 궁극기 3번 소모량
UltimateC = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 14) const UniqueA	= PVariable();			// 유니크 1번 쿨타임
UniqueA = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 16) const distance	= PVariable();			// 거리 관련 변수
distance = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 17) const SinAngle 	= PVariable();			// Sin 변수
SinAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 18) const CosAngle 	= PVariable();			// Cos 변수
CosAngle = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 20) const CosTableArray = EUDArray(py_eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"));
CosTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.cos(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 21) const SinTableArray 	= EUDArray(py_eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"));
SinTableArray = _CGFW(lambda: [EUDArray(eval("[int(round(100*math.sin(math.radians(t)))) for t in range(361)]"))], 1)[0]
# (Line 23) function MoveLoc(Unit : TrgUnit, location : TrgLocation, cp : TrgPlayer, x, y)
# (Line 24) {
@EUDTypedFunc([TrgUnit, TrgLocation, TrgPlayer, None, None])
def MoveLoc(Unit, location_1, cp, x, y):
    # (Line 25) MoveLocation(location, Unit, cp, "Anywhere");
    # (Line 26) addloc(location - 1, x, y);
    DoActions(MoveLocation(location_1, Unit, cp, "Anywhere"))
    f_addloc(location_1 - 1, x, y)
    # (Line 27) }
    # (Line 29) function SkillUnit(count, Unit : TrgUnit, location : TrgLocation, cp : TrgPlayer)

# (Line 30) {
@EUDTypedFunc([None, TrgUnit, TrgLocation, TrgPlayer])
def SkillUnit(count_1, Unit, location_1, cp):
    # (Line 31) CreateUnit(count, Unit, "[Skill]Unit_Wait_1", cp);
    # (Line 32) SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL");
    DoActions(CreateUnit(count_1, Unit, "[Skill]Unit_Wait_1", cp))
    # (Line 33) MoveUnit(count, Unit, cp, "[Skill]Unit_Wait_ALL", location);
    DoActions(SetInvincibility(Enable, Unit, cp, "[Skill]Unit_Wait_ALL"))
    # (Line 34) }
    DoActions(MoveUnit(count_1, Unit, cp, "[Skill]Unit_Wait_ALL", location_1))
    # (Line 36) function SkillWait(cp, count)

# (Line 37) {
@EUDFunc
def SkillWait(cp, count_1):
    # (Line 38) SetDeaths(cp, SetTo, count/83 + 1, " `WaitTime");
    # (Line 39) }
    DoActions(SetDeaths(cp, SetTo, count_1 // 83 + 1, " `WaitTime"))
    # (Line 41) function SkillEnd(cp)

# (Line 42) {
@EUDFunc
def SkillEnd(cp):
    # (Line 43) SetDeaths(cp, SetTo, 12, " `SkillWait");
    # (Line 44) }
    DoActions(SetDeaths(cp, SetTo, 12, " `SkillWait"))
    # (Line 46) function SquareShape(heroID : TrgUnit, count, Unit : TrgUnit, location : TrgLocation, cp : TrgPlayer, x, y)

# (Line 47) {
@EUDTypedFunc([TrgUnit, None, TrgUnit, TrgLocation, TrgPlayer, None, None])
def SquareShape(heroID_1, count_1, Unit, location_1, cp, x, y):
    # (Line 48) MoveLoc(heroID, location, cp, x, y);
    MoveLoc(heroID_1, location_1, cp, x, y)
    # (Line 49) SkillUnit(count, Unit, location, cp);
    SkillUnit(count_1, Unit, location_1, cp)
    # (Line 50) MoveLoc(heroID, location, cp, -y, x);
    MoveLoc(heroID_1, location_1, cp, -y, x)
    # (Line 51) SkillUnit(count, Unit, location, cp);
    SkillUnit(count_1, Unit, location_1, cp)
    # (Line 52) MoveLoc(heroID, location, cp, -x, -y);
    MoveLoc(heroID_1, location_1, cp, -x, -y)
    # (Line 53) SkillUnit(count, Unit, location, cp);
    SkillUnit(count_1, Unit, location_1, cp)
    # (Line 54) MoveLoc(heroID, location, cp, y, -x);
    MoveLoc(heroID_1, location_1, cp, y, -x)
    # (Line 55) SkillUnit(count, Unit, location, cp);
    SkillUnit(count_1, Unit, location_1, cp)
    # (Line 56) }
    # (Line 58) function NxNSquareShape(heroID : TrgUnit, count, Unit : TrgUnit, location : TrgLocation, cp : TrgPlayer, n, interval)

# (Line 59) {
@EUDTypedFunc([TrgUnit, None, TrgUnit, TrgLocation, TrgPlayer, None, None])
def NxNSquareShape(heroID_1, count_1, Unit, location_1, cp, n, interval):
    # (Line 60) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 61) var destX, destY;
    destX, destY = EUDCreateVariables(2)
    # (Line 62) var distance = interval / 2;
    distance_1 = EUDVariable()
    distance_1 << (interval // 2)
    # (Line 63) var size = n * n;
    size = EUDVariable()
    size << (n * n)
    # (Line 65) for (; i < size / 4; i++)
    if EUDWhile()(i >= size // 4, neg=True):
        def _t2():
            i.__iadd__(1)
        # (Line 66) {
        # (Line 67) if (n % 2 == 0)
        if EUDIf()(n % 2 == 0):
            # (Line 68) {
            # (Line 69) destX = i % (n / 2) + 1;
            destX << (i % (n // 2) + 1)
            # (Line 70) destY = i / (n / 2) + 1;
            destY << (i // (n // 2) + 1)
            # (Line 71) if (destX == 1)
            if EUDIf()(destX == 1):
                # (Line 72) {
                # (Line 73) SquareShape(heroID, count, Unit, location, cp, distance, destY * interval - distance);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, distance_1, destY * interval - distance_1)
                # (Line 74) }
                # (Line 75) else if (destY == 1)
            if EUDElseIf()(destY == 1):
                # (Line 76) {
                # (Line 77) SquareShape(heroID, count, Unit, location, cp, destX * interval - distance, distance);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, destX * interval - distance_1, distance_1)
                # (Line 78) }
                # (Line 79) else
                # (Line 80) {
            if EUDElse()():
                # (Line 81) SquareShape(heroID, count, Unit, location, cp, destX * interval - distance, destY * interval - distance);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, destX * interval - distance_1, destY * interval - distance_1)
                # (Line 82) }
                # (Line 83) }
            EUDEndIf()
            # (Line 84) else if (n % 2 == 1)
        if EUDElseIf()(n % 2 == 1):
            # (Line 85) {
            # (Line 86) destX = i % (n / 2);
            destX << (i % (n // 2))
            # (Line 87) destY = i / (n / 2);
            destY << (i // (n // 2))
            # (Line 89) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 90) {
                # (Line 91) MoveLoc(heroID, location, cp, 0, 0);
                MoveLoc(heroID_1, location_1, cp, 0, 0)
                # (Line 92) SkillUnit(count, Unit, location, cp);
                SkillUnit(count_1, Unit, location_1, cp)
                # (Line 93) }
                # (Line 94) else if (destY == 0)
            if EUDElseIf()(destY == 0):
                # (Line 95) {
                # (Line 96) SquareShape(heroID, count, Unit, location, cp, destX * interval, 0);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, destX * interval, 0)
                # (Line 97) }
                # (Line 98) else
                # (Line 99) {
            if EUDElse()():
                # (Line 100) SquareShape(heroID, count, Unit, location, cp, destX * interval + interval, destY * interval);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, destX * interval + interval, destY * interval)
                # (Line 101) }
                # (Line 103) if (i == size / 4 - 1)
            EUDEndIf()
            if EUDIf()(i == size // 4 - 1):
                # (Line 104) {
                # (Line 105) SquareShape(heroID, count, Unit, location, cp, (n / 2) * interval, 0);
                SquareShape(heroID_1, count_1, Unit, location_1, cp, (n // 2) * interval, 0)
                # (Line 106) }
                # (Line 107) }
            EUDEndIf()
            # (Line 108) }
        EUDEndIf()
        # (Line 109) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 111) function Table_Sin(cp, degree, distance)

# (Line 112) {
@EUDFunc
def Table_Sin(cp, degree, distance_1):
    # (Line 113) degree = degree % 360;
    degree << (degree % 360)
    # (Line 114) SinAngle[cp] = SinTableArray[degree];
    _ARRW(SinAngle, cp) << (SinTableArray[degree])
    # (Line 116) if (SinAngle[cp] > 2147483648)
    if EUDIf()(SinAngle[cp] <= 2147483648, neg=True):
        # (Line 117) {
        # (Line 118) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 119) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 120) SinAngle[cp] = -SinAngle[cp];
        _ARRW(SinAngle, cp) << (-SinAngle[cp])
        # (Line 121) }
        # (Line 122) else
        # (Line 123) {
    if EUDElse()():
        # (Line 124) SinAngle[cp] = (SinAngle[cp] * distance) / 100;
        _ARRW(SinAngle, cp) << ((SinAngle[cp] * distance_1) // 100)
        # (Line 125) }
        # (Line 126) }
    EUDEndIf()
    # (Line 128) function Table_Cos(cp, degree, distance)

# (Line 129) {
@EUDFunc
def Table_Cos(cp, degree, distance_1):
    # (Line 130) degree = degree % 360;
    degree << (degree % 360)
    # (Line 131) CosAngle[cp] = CosTableArray[degree];
    _ARRW(CosAngle, cp) << (CosTableArray[degree])
    # (Line 133) if (CosAngle[cp] > 2147483648)
    if EUDIf()(CosAngle[cp] <= 2147483648, neg=True):
        # (Line 134) {
        # (Line 135) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 136) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 137) CosAngle[cp] = -CosAngle[cp];
        _ARRW(CosAngle, cp) << (-CosAngle[cp])
        # (Line 138) }
        # (Line 139) else
        # (Line 140) {
    if EUDElse()():
        # (Line 141) CosAngle[cp] = (CosAngle[cp] * distance) / 100;
        _ARRW(CosAngle, cp) << ((CosAngle[cp] * distance_1) // 100)
        # (Line 142) }
        # (Line 143) }
    EUDEndIf()
