import Player.shop as sp;
import Player.sound as sd;
import Player.title as te;
import Player.lesson as ln;
import Player.event as et;

function main(cp)
{
	sp.Marge(cp);
	ln.Marge(cp);
	
	sd.PlaySound(cp);
	te.Title_Effect(cp);
	et.event_score(cp);
}