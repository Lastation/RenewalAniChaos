import System.Round as r;
import System.Announce as a;
import System.Gatcha as g;
import System.Combine as c;
import System.Mission as m;

function Init();
function FixedUpdate();
function PlayerUpdate(cp);

function Boss_reward();

function Init()
{

}

function FixedUpdate()
{
	r.FixedUpdate();
	m.FixedUpdate();
	Boss_reward();
}

function PlayerUpdate(cp)
{	
	r.PlayerUpdate(cp);
	a.FixedUpdate(cp);
	g.FixedUpdate(cp);
	c.FixedUpdate(cp);
	m.PlayerUpdate(cp);
}

function Boss_reward()
{
	if (Deaths(P8, AtLeast, 1, 133))
	{
		CreateUnit(1, 7, "MedalGacha", Force1);
		SetResources(Force1, Add, 100, Ore);
		SetDeaths(P8, Subtract, 1, 133);
	}
}