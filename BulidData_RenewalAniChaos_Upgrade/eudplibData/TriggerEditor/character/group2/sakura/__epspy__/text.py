## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _RELIMP(path, mod_name):
    import inspect, pathlib, importlib.util
    p = pathlib.Path(inspect.getabsfile(inspect.currentframe())).parent
    for s in path.split("."):
        if s == "":  p = p.parent
        else:  p = p / s
    try:
        spec = importlib.util.spec_from_file_location(mod_name, p / (mod_name + ".py"))
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
    except FileNotFoundError:
        loader = EPSLoader(mod_name, str(p / (mod_name + ".eps")))
        spec = importlib.util.spec_from_loader(mod_name, loader)
        module = loader.create_module(spec)
        loader.exec_module(module)
    return module

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import variable as v;
import variable as v
# (Line 3) import func.trig as trg;
from func import trig as trg
# (Line 4) import func.sound as s;
from func import sound as s
# (Line 6) const P_player			= PVariable();
P_player = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 7) const P_observer 		= PVariable();
P_observer = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 9) const P_Next = PVariable();
P_Next = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 10) const P_Wait = PVariable();
P_Wait = _CGFW(lambda: [PVariable()], 1)[0]
# (Line 12) function Text(num, playerID);
# (Line 14) function player(playerID)
# (Line 15) {
@EUDFunc
def f_player(playerID):
    # (Line 16) Text(P_player[playerID], playerID);
    Text(P_player[playerID], playerID)
    # (Line 17) P_player[playerID] = 0;
    _ARRW(P_player, playerID) << (0)
    # (Line 18) }
    # (Line 20) function observer(playerID)

# (Line 21) {
@EUDFunc
def f_observer(playerID):
    # (Line 22) Text(P_observer[playerID - 128], playerID);
    Text(P_observer[playerID - 128], playerID)
    # (Line 23) P_observer[playerID - 128] = 0;
    _ARRW(P_observer, playerID - 128) << (0)
    # (Line 24) }
    # (Line 26) function main(playerID)

# (Line 27) {
@EUDFunc
def f_main(playerID):
    # (Line 28) if (playerID < 6) 	{ player(playerID); 	}
    if EUDIf()(playerID >= 6, neg=True):
        f_player(playerID)
        # (Line 29) else  			{ observer(playerID); }
    if EUDElse()():
        f_observer(playerID)
        # (Line 31) if (playerID < 6)
    EUDEndIf()
    if EUDIf()(playerID >= 6, neg=True):
        # (Line 32) {
        # (Line 33) if (P_Wait[playerID] > 0)
        if EUDIf()(P_Wait[playerID] <= 0, neg=True):
            # (Line 34) { P_Wait[playerID] -= 1; }
            _ARRW(P_Wait, playerID).__isub__(1)
            # (Line 35) else if (P_Next[playerID] != 0)
        if EUDElseIf()(P_Next[playerID] == 0, neg=True):
            # (Line 36) {
            # (Line 37) P_player[playerID] 	= P_Next[playerID] + 1;
            _ARRW(P_player, playerID) << (P_Next[playerID] + 1)
            # (Line 38) P_observer[playerID] = P_Next[playerID] + 1;
            _ARRW(P_observer, playerID) << (P_Next[playerID] + 1)
            # (Line 39) P_Next[playerID] = 0;
            _ARRW(P_Next, playerID) << (0)
            # (Line 40) }
            # (Line 41) }
        EUDEndIf()
        # (Line 42) }
    EUDEndIf()
    # (Line 44) function TextDelay(playerID, wait)

# (Line 45) {
@EUDFunc
def TextDelay(playerID, wait):
    # (Line 46) if (playerID < 6)
    if EUDIf()(playerID >= 6, neg=True):
        # (Line 47) {
        # (Line 48) P_Next[playerID] = P_player[playerID];
        _ARRW(P_Next, playerID) << (P_player[playerID])
        # (Line 49) if (wait == 0)
        if EUDIf()(wait == 0):
            # (Line 50) { P_Next[playerID] = 0; }
            _ARRW(P_Next, playerID) << (0)
            # (Line 51) else
            # (Line 52) { P_Wait[playerID] = wait / 83 + 1; }
        if EUDElse()():
            _ARRW(P_Wait, playerID) << (wait // 83 + 1)
            # (Line 53) }
        EUDEndIf()
        # (Line 54) }
    EUDEndIf()
    # (Line 56) function Text(num, playerID)

# (Line 57) {
@EUDFunc
def Text(num, playerID):
    # (Line 58) switch(num)
    EUDSwitch(num)
    # (Line 59) {
    # (Line 60) case 1:
    _t1 = EUDSwitchCase()
    # (Line 61) PlayWAV("sakura_06");
    if _t1(1):
        # (Line 62) TextDelay(playerID, 0);
        DoActions(PlayWAV("sakura_06"))
        TextDelay(playerID, 0)
        # (Line 63) break;
        EUDBreak()
        # (Line 64) case 2:
    _t2 = EUDSwitchCase()
    # (Line 65) PlayWAV("sakura_01.ogg");
    if _t2(2):
        # (Line 66) v.stb.print("\x13\x1BMato Sakura\n");
        DoActions(PlayWAV("sakura_01.ogg"))
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 67) v.stb.print("\x13\x04와~~앙!!\n");
        v.stb.print("\x13\x04와~~앙!!\n")
        # (Line 68) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 69) break;
        EUDBreak()
        # (Line 70) case 3:
    _t3 = EUDSwitchCase()
    # (Line 71) PlayWAV("sakura_02.ogg");
    if _t3(3):
        # (Line 72) v.stb.print("\x13\x1BMato Sakura\n");
        DoActions(PlayWAV("sakura_02.ogg"))
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 73) v.stb.print("\x13\x04어째서... \x08항상 \x04이렇게 되어버리는 걸까...\n");
        v.stb.print("\x13\x04어째서... \x08항상 \x04이렇게 되어버리는 걸까...\n")
        # (Line 74) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 75) break;
        EUDBreak()
        # (Line 76) case 4:
    _t4 = EUDSwitchCase()
    # (Line 77) v.stb.print("\x13\x1BMato Sakura\n");
    if _t4(4):
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 78) v.stb.print("\x13\x04아주 예전부터 생각했다...\n\x13\x04아주 예전부터 \x08원망\x04하고 있었던 거야...\n");
        v.stb.print("\x13\x04아주 예전부터 생각했다...\n\x13\x04아주 예전부터 \x08원망\x04하고 있었던 거야...\n")
        # (Line 79) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 80) break;
        EUDBreak()
        # (Line 81) case 5:
    _t5 = EUDSwitchCase()
    # (Line 82) PlayWAV("sakura_03.ogg");
    if _t5(5):
        # (Line 83) v.stb.print("\x13\x1BMato Sakura\n");
        DoActions(PlayWAV("sakura_03.ogg"))
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 84) v.stb.print("\x13\x04왜 내 주변에 있는 \x08세계\x04는...\n");
        v.stb.print("\x13\x04왜 내 주변에 있는 \x08세계\x04는...\n")
        # (Line 85) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 86) break;
        EUDBreak()
        # (Line 87) case 6:
    _t6 = EUDSwitchCase()
    # (Line 88) v.stb.print("\x13\x1BMato Sakura\n");
    if _t6(6):
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 89) v.stb.print("\x13\x04이렇게나... 나를 \x08싫어하는 걸까...\n");
        v.stb.print("\x13\x04이렇게나... 나를 \x08싫어하는 걸까...\n")
        # (Line 90) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 91) break;
        EUDBreak()
        # (Line 92) case 7:
    _t7 = EUDSwitchCase()
    # (Line 93) PlayWAV("sakura_04.ogg");
    if _t7(7):
        # (Line 94) TextDelay(playerID, 5000);
        DoActions(PlayWAV("sakura_04.ogg"))
        TextDelay(playerID, 5000)
        # (Line 95) break;
        EUDBreak()
        # (Line 96) case 8:
    _t8 = EUDSwitchCase()
    # (Line 97) PlayWAV("sakura_05.ogg");
    if _t8(8):
        # (Line 98) v.stb.print("\x13\x1BMato Sakura\n");
        DoActions(PlayWAV("sakura_05.ogg"))
        v.stb.print("\x13\x1BMato Sakura\n")
        # (Line 99) v.stb.print("\x13\x08이런 사람... 없었으면 좋았을텐데...");
        v.stb.print("\x13\x08이런 사람... 없었으면 좋았을텐데...")
        # (Line 100) TextDelay(playerID, 0);
        TextDelay(playerID, 0)
        # (Line 101) break;
        EUDBreak()
        # (Line 102) }
    # (Line 103) }
    EUDEndSwitch()
