## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _RELIMP(path, mod_name):
    import inspect, pathlib, importlib.util
    p = pathlib.Path(inspect.getabsfile(inspect.currentframe())).parent
    for s in path.split("."):
        if s == "":  p = p.parent
        else:  p = p / s
    try:
        spec = importlib.util.spec_from_file_location(mod_name, p / (mod_name + ".py"))
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
    except FileNotFoundError:
        loader = EPSLoader(mod_name, str(p / (mod_name + ".eps")))
        spec = importlib.util.spec_from_loader(mod_name, loader)
        module = loader.create_module(spec)
        loader.exec_module(module)
    return module

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import variable as v;
import variable as v
# (Line 3) import func.trig as trg;
from func import trig as trg
# (Line 4) import func.sound as s;
from func import sound as s
# (Line 6) function main(playerID)
# (Line 7) {
@EUDFunc
def f_main(playerID):
    # (Line 8) if (v.P_WaitMain[playerID] == 0)
    if EUDIf()(v.P_WaitMain[playerID] == 0):
        # (Line 9) {
        # (Line 10) if (v.P_CountMain[playerID] == 0)
        if EUDIf()(v.P_CountMain[playerID] == 0):
            # (Line 11) {
            # (Line 12) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);
            # (Line 13) KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID);
            DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID))
            # (Line 15) if (v.P_LoopMain[playerID] < 4)
            DoActions(KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID))
            if EUDIf()(v.P_LoopMain[playerID] >= 4, neg=True):
                # (Line 16) {
                # (Line 17) var d = 30 + 30 * v.P_LoopMain[playerID];
                d = EUDVariable()
                d << (30 + 30 * v.P_LoopMain[playerID])
                # (Line 18) var n = 4;
                n = EUDVariable()
                n << (4)
                # (Line 19) var r = 125;
                r = EUDVariable()
                r << (125)
                # (Line 20) trg.Shape_Circle(playerID, 1, "50 + 1n Battlecruiser", d, n, r);
                trg.Shape_Circle(playerID, 1, "50 + 1n Battlecruiser", d, n, r)
                # (Line 22) if (Bring(playerID, AtLeast, 2, "Protoss Arbiter", "[Skill]UseSkill")
                _t4 = EUDIf()
                # (Line 23) && v.P_UltimateGauge[playerID] >= v.P_Ultimate1[playerID])
                if _t4(EUDSCAnd()(Bring(playerID, AtLeast, 2, "Protoss Arbiter", "[Skill]UseSkill"))(v.P_UltimateGauge[playerID] >= v.P_Ultimate1[playerID])()):
                    # (Line 24) {
                    # (Line 25) s.CharacterVoice(6);
                    s.CharacterVoice(6)
                    # (Line 26) v.P_SkillDelay[playerID] = 0;
                    _ARRW(v.P_SkillDelay, playerID) << (0)
                    # (Line 27) v.P_CountMain[playerID] = 0;
                    _ARRW(v.P_CountMain, playerID) << (0)
                    # (Line 28) v.P_LoopMain[playerID] = 0;
                    _ARRW(v.P_LoopMain, playerID) << (0)
                    # (Line 29) v.P_Step[playerID] = 320;
                    _ARRW(v.P_Step, playerID) << (320)
                    # (Line 30) SetDeaths(playerID, Subtract, v.P_Ultimate1[playerID], " `UltimateCoolTime");
                    # (Line 31) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);
                    DoActions(SetDeaths(playerID, Subtract, v.P_Ultimate1[playerID], " `UltimateCoolTime"))
                    # (Line 32) KillUnitAt(2, "Protoss Arbiter", "[Skill]UseSkill", playerID);
                    DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID))
                    # (Line 33) }
                    DoActions(KillUnitAt(2, "Protoss Arbiter", "[Skill]UseSkill", playerID))
                    # (Line 34) else {
                if EUDElse()():
                    # (Line 35) v.P_LoopMain[playerID] += 1;
                    _ARRW(v.P_LoopMain, playerID).__iadd__(1)
                    # (Line 36) }
                    # (Line 37) }
                EUDEndIf()
                # (Line 38) else if (v.P_LoopMain[playerID] < 8)
            if EUDElseIf()(v.P_LoopMain[playerID] >= 8, neg=True):
                # (Line 39) {
                # (Line 40) var d = 30 + 30 * v.P_LoopMain[playerID];
                d = EUDVariable()
                d << (30 + 30 * v.P_LoopMain[playerID])
                # (Line 41) var n = 4;
                n = EUDVariable()
                n << (4)
                # (Line 42) var r = 125;
                r = EUDVariable()
                r << (125)
                # (Line 43) trg.Shape_Circle(playerID, 1, "50 + 1n Battlecruiser", d, n, r);
                trg.Shape_Circle(playerID, 1, "50 + 1n Battlecruiser", d, n, r)
                # (Line 44) trg.Shape_Circle(playerID, 1, " Creep. Dunkelheit", d, n, r);
                trg.Shape_Circle(playerID, 1, " Creep. Dunkelheit", d, n, r)
                # (Line 46) MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
                # (Line 47) MoveUnit(All, " Creep. Dunkelheit", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]);
                DoActions(MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere"))
                # (Line 48) Order(" Creep. Dunkelheit", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
                DoActions(MoveUnit(All, " Creep. Dunkelheit", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]))
                # (Line 49) Order("50 + 1n Battlecruiser", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
                DoActions(Order(" Creep. Dunkelheit", playerID, "Anywhere", Attack, v.P_LocationID[playerID]))
                # (Line 51) KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID);
                DoActions(Order("50 + 1n Battlecruiser", playerID, "Anywhere", Attack, v.P_LocationID[playerID]))
                # (Line 52) v.P_LoopMain[playerID] += 1;
                DoActions(KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID))
                _ARRW(v.P_LoopMain, playerID).__iadd__(1)
                # (Line 53) }
                # (Line 55) trg.Main_Wait(160);
            EUDEndIf()
            trg.Main_Wait(160)
            # (Line 59) if (v.P_LoopMain[playerID] == 8)
            if EUDIf()(v.P_LoopMain[playerID] == 8):
                # (Line 60) {
                # (Line 61) v.P_CountMain[playerID] += 1;
                _ARRW(v.P_CountMain, playerID).__iadd__(1)
                # (Line 62) v.P_LoopMain[playerID] = 0;
                _ARRW(v.P_LoopMain, playerID) << (0)
                # (Line 63) }
                # (Line 64) }
            EUDEndIf()
            # (Line 65) else if (v.P_CountMain[playerID] == 1)
        if EUDElseIf()(v.P_CountMain[playerID] == 1):
            # (Line 66) {
            # (Line 67) KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID);
            # (Line 68) KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID);
            DoActions(KillUnitAt(All, "50 + 1n Battlecruiser", "Anywhere", playerID))
            # (Line 70) trg.SkillEnd();
            DoActions(KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID))
            trg.SkillEnd()
            # (Line 71) }
            # (Line 72) }
        EUDEndIf()
        # (Line 73) }
    EUDEndIf()
