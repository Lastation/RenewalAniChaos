## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _RELIMP(path, mod_name):
    import inspect, pathlib, importlib.util
    p = pathlib.Path(inspect.getabsfile(inspect.currentframe())).parent
    for s in path.split("."):
        if s == "":  p = p.parent
        else:  p = p / s
    try:
        spec = importlib.util.spec_from_file_location(mod_name, p / (mod_name + ".py"))
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
    except FileNotFoundError:
        loader = EPSLoader(mod_name, str(p / (mod_name + ".eps")))
        spec = importlib.util.spec_from_loader(mod_name, loader)
        module = loader.create_module(spec)
        loader.exec_module(module)
    return module

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import variable as v;
import variable as v
# (Line 3) import func.trig as trg;
from func import trig as trg
# (Line 5) function main(playerID)
# (Line 6) {
@EUDFunc
def f_main(playerID):
    # (Line 7) if (v.P_WaitMain[playerID] == 0)
    if EUDIf()(v.P_WaitMain[playerID] == 0):
        # (Line 8) {
        # (Line 9) if (v.P_CountMain[playerID] == 0)
        if EUDIf()(v.P_CountMain[playerID] == 0):
            # (Line 10) {
            # (Line 11) KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID);
            # (Line 13) if (v.P_LoopMain[playerID] < 2)
            DoActions(KillUnitAt(All, " Creep. Dunkelheit", "Anywhere", playerID))
            if EUDIf()(v.P_LoopMain[playerID] >= 2, neg=True):
                # (Line 14) {
                # (Line 15) var d = 0;
                d = EUDVariable()
                d << (0)
                # (Line 16) var n = 8;
                n = EUDVariable()
                n << (8)
                # (Line 17) var r = 75 + 75 * v.P_LoopMain[playerID];
                r = EUDVariable()
                r << (75 + 75 * v.P_LoopMain[playerID])
                # (Line 18) trg.Shape_Circle(playerID, 1, " Creep. Dunkelheit", d, n, r);
                trg.Shape_Circle(playerID, 1, " Creep. Dunkelheit", d, n, r)
                # (Line 19) trg.Shape_Circle(playerID, 1, "Kakaru (Twilight)", d, n, r);
                trg.Shape_Circle(playerID, 1, "Kakaru (Twilight)", d, n, r)
                # (Line 21) MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
                # (Line 22) MoveUnit(All, " Creep. Dunkelheit", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]);
                DoActions(MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere"))
                # (Line 23) Order(" Creep. Dunkelheit", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
                DoActions(MoveUnit(All, " Creep. Dunkelheit", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]))
                # (Line 25) KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID);
                DoActions(Order(" Creep. Dunkelheit", playerID, "Anywhere", Attack, v.P_LocationID[playerID]))
                # (Line 26) }
                DoActions(KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID))
                # (Line 27) else if (v.P_LoopMain[playerID] == 3)
            if EUDElseIf()(v.P_LoopMain[playerID] == 3):
                # (Line 28) {
                # (Line 29) var d = 0;
                d = EUDVariable()
                d << (0)
                # (Line 30) var n = 8;
                n = EUDVariable()
                n << (8)
                # (Line 31) var r = 150;
                r = EUDVariable()
                r << (150)
                # (Line 32) trg.Shape_Circle(playerID, 1, "40 + 1n Ghost", d, n, r);
                trg.Shape_Circle(playerID, 1, "40 + 1n Ghost", d, n, r)
                # (Line 34) MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
                # (Line 35) MoveUnit(All, "40 + 1n Ghost", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]);
                DoActions(MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere"))
                # (Line 36) Order("40 + 1n Ghost", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
                DoActions(MoveUnit(All, "40 + 1n Ghost", playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]))
                # (Line 37) }
                DoActions(Order("40 + 1n Ghost", playerID, "Anywhere", Attack, v.P_LocationID[playerID]))
                # (Line 38) else if (v.P_LoopMain[playerID] == 5)
            if EUDElseIf()(v.P_LoopMain[playerID] == 5):
                # (Line 39) {
                # (Line 40) KillUnitAt(All, "40 + 1n Ghost", "Anywhere", playerID);
                # (Line 42) for (var i = 0; i < 3; i++)
                DoActions(KillUnitAt(All, "40 + 1n Ghost", "Anywhere", playerID))
                i = EUDVariable()
                i << (0)
                if EUDWhile()(i >= 3, neg=True):
                    def _t7():
                        i.__iadd__(1)
                    # (Line 43) {
                    # (Line 44) var d = 0;
                    d = EUDVariable()
                    d << (0)
                    # (Line 45) var n = 8;
                    n = EUDVariable()
                    n << (8)
                    # (Line 46) var r = 50 + 50 * i;
                    r = EUDVariable()
                    r << (50 + 50 * i)
                    # (Line 47) trg.Shape_Circle(playerID, 1, "40 + 1n Zergling", d, n, r);
                    trg.Shape_Circle(playerID, 1, "40 + 1n Zergling", d, n, r)
                    # (Line 48) }
                    # (Line 50) KillUnitAt(All, "40 + 1n Zergling", "Anywhere", playerID);
                    EUDSetContinuePoint()
                    _t7()
                EUDEndWhile()
                # (Line 51) }
                DoActions(KillUnitAt(All, "40 + 1n Zergling", "Anywhere", playerID))
                # (Line 53) trg.Main_Wait(160);
            EUDEndIf()
            trg.Main_Wait(160)
            # (Line 55) v.P_LoopMain[playerID] += 1;
            _ARRW(v.P_LoopMain, playerID).__iadd__(1)
            # (Line 57) if (v.P_LoopMain[playerID] == 6)
            if EUDIf()(v.P_LoopMain[playerID] == 6):
                # (Line 58) {
                # (Line 59) v.P_CountMain[playerID] += 1;
                _ARRW(v.P_CountMain, playerID).__iadd__(1)
                # (Line 60) v.P_LoopMain[playerID] = 0;
                _ARRW(v.P_LoopMain, playerID) << (0)
                # (Line 61) }
                # (Line 62) }
            EUDEndIf()
            # (Line 63) else if (v.P_CountMain[playerID] == 1)
        if EUDElseIf()(v.P_CountMain[playerID] == 1):
            # (Line 64) {
            # (Line 65) trg.SkillEnd();
            trg.SkillEnd()
            # (Line 66) }
            # (Line 67) }
        EUDEndIf()
        # (Line 68) }
    EUDEndIf()
