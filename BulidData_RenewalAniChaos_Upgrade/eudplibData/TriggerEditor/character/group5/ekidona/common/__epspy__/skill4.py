## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _RELIMP(path, mod_name):
    import inspect, pathlib, importlib.util
    p = pathlib.Path(inspect.getabsfile(inspect.currentframe())).parent
    for s in path.split("."):
        if s == "":  p = p.parent
        else:  p = p / s
    try:
        spec = importlib.util.spec_from_file_location(mod_name, p / (mod_name + ".py"))
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
    except FileNotFoundError:
        loader = EPSLoader(mod_name, str(p / (mod_name + ".eps")))
        spec = importlib.util.spec_from_loader(mod_name, loader)
        module = loader.create_module(spec)
        loader.exec_module(module)
    return module

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import variable as v;
import variable as v
# (Line 3) import func.trig as trg;
from func import trig as trg
# (Line 4) import func.trigepic as epic;
from func import trigepic as epic
# (Line 6) function main(playerID)
# (Line 7) {
@EUDFunc
def f_main(playerID):
    # (Line 8) if (v.P_WaitMain[playerID] == 0)
    if EUDIf()(v.P_WaitMain[playerID] == 0):
        # (Line 9) {
        # (Line 10) if (v.P_CountMain[playerID] == 0)
        if EUDIf()(v.P_CountMain[playerID] == 0):
            # (Line 11) {
            # (Line 12) if (v.P_LoopMain[playerID] == 0)
            if EUDIf()(v.P_LoopMain[playerID] == 0):
                # (Line 13) {
                # (Line 14) trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50);
                trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50)
                # (Line 15) Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere");
                # (Line 17) epic.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 5, 100, 1);
                DoActions(Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere"))
                epic.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 5, 100, 1)
                # (Line 18) trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50);
                trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50)
                # (Line 19) KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID);
                # (Line 20) }
                DoActions(KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID))
                # (Line 21) if (v.P_LoopMain[playerID] == 4)
            EUDEndIf()
            if EUDIf()(v.P_LoopMain[playerID] == 4):
                # (Line 22) {
                # (Line 23) KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
                # (Line 24) }
                DoActions(KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID))
                # (Line 26) var i = v.P_LoopMain[playerID] / 2;
            EUDEndIf()
            i = EUDVariable()
            i << (v.P_LoopMain[playerID] // 2)
            # (Line 27) var x = 50 - 50 * i;
            x = EUDVariable()
            x << (50 - 50 * i)
            # (Line 28) var y = 100;
            y = EUDVariable()
            y << (100)
            # (Line 30) if (v.P_LoopMain[playerID] < 8 && v.P_LoopMain[playerID] % 2 == 0)
            if EUDIf()(EUDSCAnd()(v.P_LoopMain[playerID] >= 8, neg=True)(v.P_LoopMain[playerID] % 2 == 0)()):
                # (Line 31) {
                # (Line 32) RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID);
                # (Line 34) if (i % 2 == 0)
                DoActions(RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID))
                if EUDIf()(i % 2 == 0):
                    # (Line 35) {
                    # (Line 36) epic.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y, 1);
                    epic.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y, 1)
                    # (Line 37) trg.Shape_Square(playerID, 1, "Protoss Dark Templar", x, y);
                    trg.Shape_Square(playerID, 1, "Protoss Dark Templar", x, y)
                    # (Line 38) KillUnitAt(All, "Protoss Dark Templar", "Anywhere", playerID);
                    # (Line 39) }
                    DoActions(KillUnitAt(All, "Protoss Dark Templar", "Anywhere", playerID))
                    # (Line 40) else
                    # (Line 41) {
                if EUDElse()():
                    # (Line 42) trg.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y);
                    trg.Shape_Square(playerID, 1, "80 + 1n Artanis", x, y)
                    # (Line 43) trg.Shape_Square(playerID, 1, "60 + 1n High Templar", x, y);
                    trg.Shape_Square(playerID, 1, "60 + 1n High Templar", x, y)
                    # (Line 44) KillUnitAt(All, "60 + 1n High Templar", "Anywhere", playerID);
                    # (Line 45) }
                    DoActions(KillUnitAt(All, "60 + 1n High Templar", "Anywhere", playerID))
                    # (Line 47) MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere");
                EUDEndIf()
                # (Line 48) Order("80 + 1n Artanis", playerID, "Anywhere", Attack, v.P_LocationID[playerID]);
                DoActions(MoveLocation(v.P_LocationID[playerID], v.P_UnitID[playerID], playerID, "Anywhere"))
                # (Line 49) }
                DoActions(Order("80 + 1n Artanis", playerID, "Anywhere", Attack, v.P_LocationID[playerID]))
                # (Line 51) trg.Main_Wait(80);
            EUDEndIf()
            trg.Main_Wait(80)
            # (Line 53) v.P_LoopMain[playerID] += 1;
            _ARRW(v.P_LoopMain, playerID).__iadd__(1)
            # (Line 55) if (v.P_LoopMain[playerID] == 8)
            if EUDIf()(v.P_LoopMain[playerID] == 8):
                # (Line 56) {
                # (Line 57) v.P_CountMain[playerID] += 1;
                _ARRW(v.P_CountMain, playerID).__iadd__(1)
                # (Line 58) v.P_LoopMain[playerID] = 0;
                _ARRW(v.P_LoopMain, playerID) << (0)
                # (Line 59) }
                # (Line 60) }
            EUDEndIf()
            # (Line 61) else if (v.P_CountMain[playerID] == 1)
        if EUDElseIf()(v.P_CountMain[playerID] == 1):
            # (Line 62) {
            # (Line 63) if (v.P_LoopMain[playerID] == 0)
            if EUDIf()(v.P_LoopMain[playerID] == 0):
                # (Line 64) {
                # (Line 65) RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID);
                # (Line 67) trg.Shape_Edge(playerID, 1, "40 + 1n Mojo", 45, 5, 100);
                DoActions(RemoveUnitAt(All, "80 + 1n Artanis", "Anywhere", playerID))
                trg.Shape_Edge(playerID, 1, "40 + 1n Mojo", 45, 5, 100)
                # (Line 68) KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
                # (Line 69) }
                DoActions(KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID))
                # (Line 70) if (v.P_LoopMain[playerID] == 2)
            EUDEndIf()
            if EUDIf()(v.P_LoopMain[playerID] == 2):
                # (Line 71) {
                # (Line 72) trg.Shape_Edge(playerID, 1, "Kakaru (Twilight)", 45, 5, 100);
                trg.Shape_Edge(playerID, 1, "Kakaru (Twilight)", 45, 5, 100)
                # (Line 73) KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID);
                # (Line 75) trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50);
                DoActions(KillUnitAt(All, "Kakaru (Twilight)", "Anywhere", playerID))
                trg.Shape_NxNSquare(playerID, 1, "40 + 1n Mojo", 3, 50)
                # (Line 76) Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere");
                # (Line 78) trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50);
                DoActions(Order("40 + 1n Mojo", playerID, "Anywhere", Attack, "Anywhere"))
                trg.Shape_Edge(playerID, 1, "60 + 1n Archon", 45, 3, 50)
                # (Line 79) KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID);
                # (Line 80) }
                DoActions(KillUnitAt(All, "60 + 1n Archon", "Anywhere", playerID))
                # (Line 82) trg.Main_Wait(80);
            EUDEndIf()
            trg.Main_Wait(80)
            # (Line 84) v.P_LoopMain[playerID] += 1;
            _ARRW(v.P_LoopMain, playerID).__iadd__(1)
            # (Line 86) if (v.P_LoopMain[playerID] == 6)
            if EUDIf()(v.P_LoopMain[playerID] == 6):
                # (Line 87) {
                # (Line 88) v.P_CountMain[playerID] += 1;
                _ARRW(v.P_CountMain, playerID).__iadd__(1)
                # (Line 89) v.P_LoopMain[playerID] = 0;
                _ARRW(v.P_LoopMain, playerID) << (0)
                # (Line 90) }
                # (Line 91) }
            EUDEndIf()
            # (Line 92) else if (v.P_CountMain[playerID] == 2)
        if EUDElseIf()(v.P_CountMain[playerID] == 2):
            # (Line 93) {
            # (Line 94) KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID);
            # (Line 96) trg.SkillEnd();
            DoActions(KillUnitAt(All, "40 + 1n Mojo", "Anywhere", playerID))
            trg.SkillEnd()
            # (Line 97) }
            # (Line 98) }
        EUDEndIf()
        # (Line 99) }
    EUDEndIf()
