## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _RELIMP(path, mod_name):
    import inspect, pathlib, importlib.util
    p = pathlib.Path(inspect.getabsfile(inspect.currentframe())).parent
    for s in path.split("."):
        if s == "":  p = p.parent
        else:  p = p / s
    try:
        spec = importlib.util.spec_from_file_location(mod_name, p / (mod_name + ".py"))
        module = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(module)
    except FileNotFoundError:
        loader = EPSLoader(mod_name, str(p / (mod_name + ".eps")))
        spec = importlib.util.spec_from_loader(mod_name, loader)
        module = loader.create_module(spec)
        loader.exec_module(module)
    return module

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import PluginVariables as msqcvar;
import PluginVariables as msqcvar
# (Line 2) import variable as v;
import variable as v
# (Line 3) import func.trig as trg;
from func import trig as trg
# (Line 24) function Shape_DotAt(playerID : TrgPlayer, baseunit : TrgUnit, count, unit : TrgUnit, pos_x, pos_y)
# (Line 25) {
@EUDTypedFunc([TrgPlayer, TrgUnit, None, TrgUnit, None, None])
def Shape_DotAt(playerID, baseunit, count, unit, pos_x, pos_y):
    # (Line 26) trg.MoveLoc(baseunit ,playerID, pos_x, pos_y);
    trg.MoveLoc(baseunit, playerID, pos_x, pos_y)
    # (Line 27) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 28) }
    # (Line 34) function Shape_DotAt2(playerID : TrgPlayer, count, unit : TrgUnit, pos_x, pos_y)

# (Line 35) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def Shape_DotAt2(playerID, count, unit, pos_x, pos_y):
    # (Line 36) trg.MoveLoc(v.P_UnitID[playerID] ,playerID, pos_x, pos_y);
    trg.MoveLoc(v.P_UnitID[playerID], playerID, pos_x, pos_y)
    # (Line 37) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 38) }
    # (Line 60) function Shape_DoubleAt(playerID : TrgPlayer, baseunit : TrgUnit, count, unit : TrgUnit, pos_x, pos_y)

# (Line 61) {
@EUDTypedFunc([TrgPlayer, TrgUnit, None, TrgUnit, None, None])
def Shape_DoubleAt(playerID, baseunit, count, unit, pos_x, pos_y):
    # (Line 62) Shape_DotAt(playerID, baseunit, count, unit, pos_x, pos_y);
    Shape_DotAt(playerID, baseunit, count, unit, pos_x, pos_y)
    # (Line 63) Shape_DotAt(playerID, baseunit, count, unit, -pos_x, -pos_y);
    Shape_DotAt(playerID, baseunit, count, unit, -pos_x, -pos_y)
    # (Line 64) }
    # (Line 70) function Shape_DoubleAt2(playerID : TrgPlayer, count, unit : TrgUnit, pos_x, pos_y)

# (Line 71) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None])
def Shape_DoubleAt2(playerID, count, unit, pos_x, pos_y):
    # (Line 72) Shape_DotAt2(playerID, v.P_UnitID[playerID], count, unit, pos_x, pos_y);
    Shape_DotAt2(playerID, v.P_UnitID[playerID], count, unit, pos_x, pos_y)
    # (Line 73) Shape_DotAt2(playerID, v.P_UnitID[playerID], count, unit, -pos_x, -pos_y);
    Shape_DotAt2(playerID, v.P_UnitID[playerID], count, unit, -pos_x, -pos_y)
    # (Line 74) }
    # (Line 97) function Shape_SquareAt(playerID : TrgPlayer, baseunit : TrgUnit, count, unit : TrgUnit, pos_x, pos_y)

# (Line 98) {
@EUDTypedFunc([TrgPlayer, TrgUnit, None, TrgUnit, None, None])
def Shape_SquareAt(playerID, baseunit, count, unit, pos_x, pos_y):
    # (Line 99) trg.MoveLoc(baseunit, playerID, pos_x, pos_y);
    trg.MoveLoc(baseunit, playerID, pos_x, pos_y)
    # (Line 100) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 101) trg.MoveLoc(baseunit, playerID, -pos_y, pos_x);
    trg.MoveLoc(baseunit, playerID, -pos_y, pos_x)
    # (Line 102) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 103) trg.MoveLoc(baseunit, playerID, -pos_x, -pos_y);
    trg.MoveLoc(baseunit, playerID, -pos_x, -pos_y)
    # (Line 104) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 105) trg.MoveLoc(baseunit, playerID, pos_y, -pos_x);
    trg.MoveLoc(baseunit, playerID, pos_y, -pos_x)
    # (Line 106) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 107) }
    # (Line 114) function Shape_SquareAt2(playerID : TrgPlayer, count, unit : TrgUnit, pos_x, pos_y, distance_x, distance_y)

# (Line 115) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def Shape_SquareAt2(playerID, count, unit, pos_x, pos_y, distance_x, distance_y):
    # (Line 116) trg.MoveLoc(v.P_UnitID[playerID] ,playerID, distance_x + pos_x, distance_y + pos_y);
    trg.MoveLoc(v.P_UnitID[playerID], playerID, distance_x + pos_x, distance_y + pos_y)
    # (Line 117) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 118) trg.MoveLoc(v.P_UnitID[playerID] ,playerID, distance_x + -pos_y, distance_y + pos_x);
    trg.MoveLoc(v.P_UnitID[playerID], playerID, distance_x + -pos_y, distance_y + pos_x)
    # (Line 119) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 120) trg.MoveLoc(v.P_UnitID[playerID] ,playerID, distance_x + -pos_x, distance_y + -pos_y);
    trg.MoveLoc(v.P_UnitID[playerID], playerID, distance_x + -pos_x, distance_y + -pos_y)
    # (Line 121) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 122) trg.MoveLoc(v.P_UnitID[playerID] ,playerID, distance_x + pos_y, distance_y + -pos_x);
    trg.MoveLoc(v.P_UnitID[playerID], playerID, distance_x + pos_y, distance_y + -pos_x)
    # (Line 123) trg.SkillUnit(playerID, count, unit);
    trg.SkillUnit(playerID, count, unit)
    # (Line 124) }
    # (Line 146) function Shape_NxNSquareAt(playerID : TrgPlayer, baseunit : TrgUnit, count, unit : TrgUnit, size, interval)

# (Line 147) {
@EUDTypedFunc([TrgPlayer, TrgUnit, None, TrgUnit, None, None])
def Shape_NxNSquareAt(playerID, baseunit, count, unit, size, interval):
    # (Line 148) var loop = (size * size) / 4;
    loop = EUDVariable()
    loop << ((size * size) // 4)
    # (Line 149) var space = interval / 2;
    space = EUDVariable()
    space << (interval // 2)
    # (Line 150) var dis_x, dis_y;
    dis_x, dis_y = EUDCreateVariables(2)
    # (Line 152) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 153) {
        # (Line 154) Shape_DotAt(playerID, baseunit, count, unit, 0, 0);
        Shape_DotAt(playerID, baseunit, count, unit, 0, 0)
        # (Line 155) }
        # (Line 156) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 157) {
        # (Line 158) foreach(idx : EUDLoopRange(loop))
        for idx in EUDLoopRange(loop):
            # (Line 159) {
            # (Line 160) if (size % 2 == 0)
            if EUDIf()(size % 2 == 0):
                # (Line 161) {
                # (Line 162) dis_x = idx % (size / 2) + 1;
                dis_x << (idx % (size // 2) + 1)
                # (Line 163) dis_y = idx / (size / 2) + 1;
                dis_y << (idx // (size // 2) + 1)
                # (Line 164) if (dis_x == 1)
                if EUDIf()(dis_x == 1):
                    # (Line 165) { Shape_SquareAt(playerID, baseunit, count, unit, space, dis_y * interval - space); }
                    Shape_SquareAt(playerID, baseunit, count, unit, space, dis_y * interval - space)
                    # (Line 166) else if (dis_y == 1)
                if EUDElseIf()(dis_y == 1):
                    # (Line 167) { Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval - space, space); }
                    Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval - space, space)
                    # (Line 168) else
                    # (Line 169) { Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval - space, dis_y * interval - space); }
                if EUDElse()():
                    Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval - space, dis_y * interval - space)
                    # (Line 170) }
                EUDEndIf()
                # (Line 171) else if (size % 2 == 1)
            if EUDElseIf()(size % 2 == 1):
                # (Line 172) {
                # (Line 173) dis_x = idx % (size / 2);
                dis_x << (idx % (size // 2))
                # (Line 174) dis_y = idx / (size / 2);
                dis_y << (idx // (size // 2))
                # (Line 176) if (idx == 0)
                if EUDIf()(idx == 0):
                    # (Line 177) { Shape_DotAt(playerID, baseunit, count, unit, 0, 0); }
                    Shape_DotAt(playerID, baseunit, count, unit, 0, 0)
                    # (Line 178) else if (dis_y == 0)
                if EUDElseIf()(dis_y == 0):
                    # (Line 179) { Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval, 0); }
                    Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval, 0)
                    # (Line 180) else
                    # (Line 181) { Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval + interval, dis_y * interval); }
                if EUDElse()():
                    Shape_SquareAt(playerID, baseunit, count, unit, dis_x * interval + interval, dis_y * interval)
                    # (Line 183) if (idx == loop - 1)
                EUDEndIf()
                if EUDIf()(idx == loop - 1):
                    # (Line 184) { Shape_SquareAt(playerID, baseunit, count, unit, (size / 2) * interval, 0); }
                    Shape_SquareAt(playerID, baseunit, count, unit, (size // 2) * interval, 0)
                    # (Line 185) }
                EUDEndIf()
                # (Line 186) }
            EUDEndIf()
            # (Line 187) }

        # (Line 188) }
    EUDEndIf()
    # (Line 210) function Shape_NxNSquareAt2(playerID : TrgPlayer, count, unit : TrgUnit, size, interval, x, y)

# (Line 211) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def Shape_NxNSquareAt2(playerID, count, unit, size, interval, x, y):
    # (Line 212) var loop = (size * size) / 4;
    loop = EUDVariable()
    loop << ((size * size) // 4)
    # (Line 213) var space = interval / 2;
    space = EUDVariable()
    space << (interval // 2)
    # (Line 214) var dis_x, dis_y;
    dis_x, dis_y = EUDCreateVariables(2)
    # (Line 216) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 217) {
        # (Line 218) Shape_DotAt2(playerID, count, unit, x, y);
        Shape_DotAt2(playerID, count, unit, x, y)
        # (Line 219) }
        # (Line 220) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 221) {
        # (Line 222) foreach(idx : EUDLoopRange(loop))
        for idx in EUDLoopRange(loop):
            # (Line 223) {
            # (Line 224) if (size % 2 == 0)
            if EUDIf()(size % 2 == 0):
                # (Line 225) {
                # (Line 226) dis_x = idx % (size / 2) + 1;
                dis_x << (idx % (size // 2) + 1)
                # (Line 227) dis_y = idx / (size / 2) + 1;
                dis_y << (idx // (size // 2) + 1)
                # (Line 228) if (dis_x == 1)
                if EUDIf()(dis_x == 1):
                    # (Line 229) { Shape_SquareAt2(playerID, count, unit, space, dis_y * interval - space, x, y); }
                    Shape_SquareAt2(playerID, count, unit, space, dis_y * interval - space, x, y)
                    # (Line 230) else if (dis_y == 1)
                if EUDElseIf()(dis_y == 1):
                    # (Line 231) { Shape_SquareAt2(playerID, count, unit, dis_x * interval - space, space, x, y); }
                    Shape_SquareAt2(playerID, count, unit, dis_x * interval - space, space, x, y)
                    # (Line 232) else
                    # (Line 233) { Shape_SquareAt2(playerID, count, unit, dis_x * interval - space, dis_y * interval - space, x, y); }
                if EUDElse()():
                    Shape_SquareAt2(playerID, count, unit, dis_x * interval - space, dis_y * interval - space, x, y)
                    # (Line 234) }
                EUDEndIf()
                # (Line 235) else if (size % 2 == 1)
            if EUDElseIf()(size % 2 == 1):
                # (Line 236) {
                # (Line 237) dis_x = idx % (size / 2);
                dis_x << (idx % (size // 2))
                # (Line 238) dis_y = idx / (size / 2);
                dis_y << (idx // (size // 2))
                # (Line 240) if (idx == 0)
                if EUDIf()(idx == 0):
                    # (Line 241) { Shape_DotAt2(playerID, count, unit, x, y); }
                    Shape_DotAt2(playerID, count, unit, x, y)
                    # (Line 242) else if (dis_y == 0)
                if EUDElseIf()(dis_y == 0):
                    # (Line 243) { Shape_SquareAt2(playerID, count, unit, dis_x * interval, 0, x, y); }
                    Shape_SquareAt2(playerID, count, unit, dis_x * interval, 0, x, y)
                    # (Line 244) else
                    # (Line 245) { Shape_SquareAt2(playerID, count, unit, dis_x * interval + interval, dis_y * interval, x, y); }
                if EUDElse()():
                    Shape_SquareAt2(playerID, count, unit, dis_x * interval + interval, dis_y * interval, x, y)
                    # (Line 247) if (idx == loop - 1)
                EUDEndIf()
                if EUDIf()(idx == loop - 1):
                    # (Line 248) { Shape_SquareAt2(playerID, count, unit, (size / 2) * interval, 0, x, y); }
                    Shape_SquareAt2(playerID, count, unit, (size // 2) * interval, 0, x, y)
                    # (Line 249) }
                EUDEndIf()
                # (Line 250) }
            EUDEndIf()
            # (Line 251) }

        # (Line 252) }
    EUDEndIf()
    # (Line 274) function Shape_QuadNxNSquareAt(playerID : TrgPlayer, count, unit : TrgUnit, size, interval, x, y)

# (Line 275) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def Shape_QuadNxNSquareAt(playerID, count, unit, size, interval, x, y):
    # (Line 276) Shape_NxNSquareAt2(playerID, count, unit, size, interval, x, y);
    Shape_NxNSquareAt2(playerID, count, unit, size, interval, x, y)
    # (Line 277) Shape_NxNSquareAt2(playerID, count, unit, size, interval, -y, x);
    Shape_NxNSquareAt2(playerID, count, unit, size, interval, -y, x)
    # (Line 278) Shape_NxNSquareAt2(playerID, count, unit, size, interval, y, -x);
    Shape_NxNSquareAt2(playerID, count, unit, size, interval, y, -x)
    # (Line 279) Shape_NxNSquareAt2(playerID, count, unit, size, interval, -x, -y);
    Shape_NxNSquareAt2(playerID, count, unit, size, interval, -x, -y)
    # (Line 280) }
    # (Line 307) function Shape_LineAt(playerID : TrgPlayer, count, baseunit : TrgUnit, unit : TrgUnit, degree, size, interval, distance)

# (Line 308) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, TrgUnit, None, None, None, None])
def Shape_LineAt(playerID, count, baseunit, unit, degree, size, interval, distance):
    # (Line 309) trg.Table_Sin(playerID, degree + 90, distance);
    trg.Table_Sin(playerID, degree + 90, distance)
    # (Line 310) trg.Table_Cos(playerID, degree + 90, distance);
    trg.Table_Cos(playerID, degree + 90, distance)
    # (Line 312) var initial_x = v.P_AngleCos[playerID];
    initial_x = EUDVariable()
    initial_x << (v.P_AngleCos[playerID])
    # (Line 313) var initial_y = v.P_AngleSin[playerID];
    initial_y = EUDVariable()
    initial_y << (v.P_AngleSin[playerID])
    # (Line 315) if (size % 2 == 0)
    if EUDIf()(size % 2 == 0):
        # (Line 316) { trg.Table_Sin(playerID, degree, interval * (size / 2) - interval / 2); trg.Table_Cos(playerID, degree, interval * (size / 2) - interval / 2); }
        trg.Table_Sin(playerID, degree, interval * (size // 2) - interval // 2)
        trg.Table_Cos(playerID, degree, interval * (size // 2) - interval // 2)
        # (Line 317) else
        # (Line 318) { trg.Table_Sin(playerID, degree, interval * (size / 2)); trg.Table_Cos(playerID, degree, interval * (size / 2)); }
    if EUDElse()():
        trg.Table_Sin(playerID, degree, interval * (size // 2))
        trg.Table_Cos(playerID, degree, interval * (size // 2))
        # (Line 320) initial_x = initial_x + v.P_AngleCos[playerID];
    EUDEndIf()
    initial_x << (initial_x + v.P_AngleCos[playerID])
    # (Line 321) initial_y = initial_y + v.P_AngleSin[playerID];
    initial_y << (initial_y + v.P_AngleSin[playerID])
    # (Line 323) trg.Table_Sin(playerID, degree, interval);
    trg.Table_Sin(playerID, degree, interval)
    # (Line 324) trg.Table_Cos(playerID, degree, interval);
    trg.Table_Cos(playerID, degree, interval)
    # (Line 326) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 327) { Shape_DotAt(playerID, baseunit, count, unit, 0, 0); }
        Shape_DotAt(playerID, baseunit, count, unit, 0, 0)
        # (Line 328) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 329) { foreach(idx : EUDLoopRange(size)) { Shape_DotAt(playerID, baseunit, count, unit, initial_x - idx * v.P_AngleCos[playerID], initial_y - idx * v.P_AngleSin[playerID]); } }
        for idx in EUDLoopRange(size):
            Shape_DotAt(playerID, baseunit, count, unit, initial_x - idx * v.P_AngleCos[playerID], initial_y - idx * v.P_AngleSin[playerID])

        # (Line 330) }
    EUDEndIf()
    # (Line 336) function Shape_LineAt2(playerID : TrgPlayer, count, unit : TrgUnit, degree, size, interval, distanceX, distanceY)

# (Line 337) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def Shape_LineAt2(playerID, count, unit, degree, size, interval, distanceX, distanceY):
    # (Line 338) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 339) var distance = interval / 2;
    distance = EUDVariable()
    distance << (interval // 2)
    # (Line 341) trg.Table_Sin(playerID, degree, distance);
    trg.Table_Sin(playerID, degree, distance)
    # (Line 342) trg.Table_Cos(playerID, degree, distance);
    trg.Table_Cos(playerID, degree, distance)
    # (Line 344) var gradientX = v.P_AngleCos[playerID];
    gradientX = EUDVariable()
    gradientX << (v.P_AngleCos[playerID])
    # (Line 345) var gradientY = v.P_AngleSin[playerID] ;
    gradientY = EUDVariable()
    gradientY << (v.P_AngleSin[playerID])
    # (Line 346) var Angle_X, Angle_Y;
    Angle_X, Angle_Y = EUDCreateVariables(2)
    # (Line 348) trg.Table_Sin(playerID, degree, interval);
    trg.Table_Sin(playerID, degree, interval)
    # (Line 349) trg.Table_Cos(playerID, degree, interval);
    trg.Table_Cos(playerID, degree, interval)
    # (Line 351) for (; i <= size / 2; i++)
    if EUDWhile()(i <= size // 2):
        def _t2():
            i.__iadd__(1)
        # (Line 352) {
        # (Line 353) Angle_X = v.P_AngleCos[playerID] * i;
        Angle_X << (v.P_AngleCos[playerID] * i)
        # (Line 354) Angle_Y = v.P_AngleSin[playerID] * i;
        Angle_Y << (v.P_AngleSin[playerID] * i)
        # (Line 356) if (size % 2 == 0 && i != size / 2)
        if EUDIf()(EUDSCAnd()(size % 2 == 0)(i == size // 2, neg=True)()):
            # (Line 357) {
            # (Line 358) trg.MoveLoc(v.P_UnitID[playerID], playerID, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY);
            trg.MoveLoc(v.P_UnitID[playerID], playerID, -Angle_X - gradientX + distanceX, Angle_Y + gradientY + distanceY)
            # (Line 359) trg.SkillUnit(playerID, count, unit);
            trg.SkillUnit(playerID, count, unit)
            # (Line 360) trg.MoveLoc(v.P_UnitID[playerID], playerID, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY);
            trg.MoveLoc(v.P_UnitID[playerID], playerID, Angle_X + gradientX + distanceX, -Angle_Y - gradientY + distanceY)
            # (Line 361) trg.SkillUnit(playerID, count, unit);
            trg.SkillUnit(playerID, count, unit)
            # (Line 362) }
            # (Line 363) else if (size % 2 == 1)
        if EUDElseIf()(size % 2 == 1):
            # (Line 364) {
            # (Line 365) if (i == 0)
            if EUDIf()(i == 0):
                # (Line 366) {
                # (Line 367) trg.MoveLoc(v.P_UnitID[playerID], playerID, distanceX, distanceY);
                trg.MoveLoc(v.P_UnitID[playerID], playerID, distanceX, distanceY)
                # (Line 368) trg.SkillUnit(playerID, count, unit);
                trg.SkillUnit(playerID, count, unit)
                # (Line 369) }
                # (Line 370) else
                # (Line 371) {
            if EUDElse()():
                # (Line 372) trg.MoveLoc(v.P_UnitID[playerID], playerID, -Angle_X + distanceX, Angle_Y + distanceY);
                trg.MoveLoc(v.P_UnitID[playerID], playerID, -Angle_X + distanceX, Angle_Y + distanceY)
                # (Line 373) trg.SkillUnit(playerID, count, unit);
                trg.SkillUnit(playerID, count, unit)
                # (Line 374) trg.MoveLoc(v.P_UnitID[playerID], playerID, Angle_X + distanceX, -Angle_Y + distanceY);
                trg.MoveLoc(v.P_UnitID[playerID], playerID, Angle_X + distanceX, -Angle_Y + distanceY)
                # (Line 375) trg.SkillUnit(playerID, count, unit);
                trg.SkillUnit(playerID, count, unit)
                # (Line 376) }
                # (Line 377) }
            EUDEndIf()
            # (Line 378) }
        EUDEndIf()
        # (Line 379) }
        EUDSetContinuePoint()
        _t2()
    EUDEndWhile()
    # (Line 403) function Shape_CrossAt(playerID : TrgPlayer, count, baseunit : TrgUnit, unit : TrgUnit, degree, size, interval)

# (Line 404) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, TrgUnit, None, None, None])
def Shape_CrossAt(playerID, count, baseunit, unit, degree, size, interval):
    # (Line 405) var distance = interval / 2;
    distance = EUDVariable()
    distance << (interval // 2)
    # (Line 407) trg.Table_Sin(playerID, degree, distance);
    trg.Table_Sin(playerID, degree, distance)
    # (Line 408) trg.Table_Cos(playerID, degree, distance);
    trg.Table_Cos(playerID, degree, distance)
    # (Line 410) var gradient_x = v.P_AngleCos[playerID] / 2;
    gradient_x = EUDVariable()
    gradient_x << (v.P_AngleCos[playerID] // 2)
    # (Line 411) var gradient_y = v.P_AngleSin[playerID] / 2;
    gradient_y = EUDVariable()
    gradient_y << (v.P_AngleSin[playerID] // 2)
    # (Line 413) var angle_x, angle_y;
    angle_x, angle_y = EUDCreateVariables(2)
    # (Line 415) trg.Table_Sin(playerID, degree, interval);
    trg.Table_Sin(playerID, degree, interval)
    # (Line 416) trg.Table_Cos(playerID, degree, interval);
    trg.Table_Cos(playerID, degree, interval)
    # (Line 418) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 419) {
        # (Line 420) Shape_DotAt(playerID, baseunit, count, unit, 0, 0);
        Shape_DotAt(playerID, baseunit, count, unit, 0, 0)
        # (Line 421) }
        # (Line 422) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 423) {
        # (Line 424) foreach(idx : EUDLoopRange(size))
        for idx in EUDLoopRange(size):
            # (Line 425) {
            # (Line 426) angle_x = v.P_AngleCos[playerID] * idx;
            angle_x << (v.P_AngleCos[playerID] * idx)
            # (Line 427) angle_y = v.P_AngleSin[playerID] * idx;
            angle_y << (v.P_AngleSin[playerID] * idx)
            # (Line 429) if (size % 2 == 0 && idx != size / 2)
            if EUDIf()(EUDSCAnd()(size % 2 == 0)(idx == size // 2, neg=True)()):
                # (Line 430) { Shape_SquareAt(playerID, baseunit, count, unit, angle_x + gradient_x, angle_y + gradient_y); }
                Shape_SquareAt(playerID, baseunit, count, unit, angle_x + gradient_x, angle_y + gradient_y)
                # (Line 431) else if (size % 2 == 1)
            if EUDElseIf()(size % 2 == 1):
                # (Line 432) {
                # (Line 433) if (idx == 0)
                if EUDIf()(idx == 0):
                    # (Line 434) { Shape_DotAt(playerID, baseunit, count, unit, 0, 0); }
                    Shape_DotAt(playerID, baseunit, count, unit, 0, 0)
                    # (Line 435) else
                    # (Line 436) { Shape_SquareAt(playerID, baseunit, count, unit, angle_x, angle_y); }
                if EUDElse()():
                    Shape_SquareAt(playerID, baseunit, count, unit, angle_x, angle_y)
                    # (Line 437) }
                EUDEndIf()
                # (Line 438) }
            EUDEndIf()
            # (Line 439) }

        # (Line 440) }
    EUDEndIf()
    # (Line 464) function Shape_EdgeAt(playerID : TrgPlayer, count, unit : TrgUnit, degree, size, interval, x, y)

# (Line 465) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def Shape_EdgeAt(playerID, count, unit, degree, size, interval, x, y):
    # (Line 466) trg.Table_Sin(playerID, degree, interval * 14 / 10);
    trg.Table_Sin(playerID, degree, interval * 14 // 10)
    # (Line 467) trg.Table_Cos(playerID, degree, interval * 14 / 10);
    trg.Table_Cos(playerID, degree, interval * 14 // 10)
    # (Line 469) var x_o = v.P_AngleCos[playerID];
    x_o = EUDVariable()
    x_o << (v.P_AngleCos[playerID])
    # (Line 470) var y_o = v.P_AngleSin[playerID];
    y_o = EUDVariable()
    y_o << (v.P_AngleSin[playerID])
    # (Line 472) var distance = (interval * 2) / (size - 1);
    distance = EUDVariable()
    distance << ((interval * 2) // (size - 1))
    # (Line 474) trg.Table_Sin(playerID, degree + 45, distance);
    trg.Table_Sin(playerID, degree + 45, distance)
    # (Line 475) trg.Table_Cos(playerID, degree + 45, distance);
    trg.Table_Cos(playerID, degree + 45, distance)
    # (Line 477) var distance_x = v.P_AngleCos[playerID];
    distance_x = EUDVariable()
    distance_x << (v.P_AngleCos[playerID])
    # (Line 478) var distance_y = v.P_AngleSin[playerID];
    distance_y = EUDVariable()
    distance_y << (v.P_AngleSin[playerID])
    # (Line 480) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 481) { Shape_DotAt(playerID, v.P_UnitID[playerID], 1, unit, x, y); }
        Shape_DotAt(playerID, v.P_UnitID[playerID], 1, unit, x, y)
        # (Line 482) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 483) { foreach(idx : EUDLoopRange(size - 1)) { Shape_SquareAt(playerID, v.P_UnitID[playerID], 1, unit, x + x_o - (distance_x * idx), y + y_o - (distance_y * idx)); } }
        for idx in EUDLoopRange(size - 1):
            Shape_SquareAt(playerID, v.P_UnitID[playerID], 1, unit, x + x_o - (distance_x * idx), y + y_o - (distance_y * idx))

        # (Line 484) }
    EUDEndIf()
