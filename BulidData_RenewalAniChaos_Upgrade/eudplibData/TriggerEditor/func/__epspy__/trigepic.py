## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *

def _IGVA(vList, exprListGen):
    def _():
        exprList = exprListGen()
        SetVariables(vList, exprList)
    EUDOnStart(_)

def _CGFW(exprf, retn):
    rets = [ExprProxy(None) for _ in range(retn)]
    def _():
        vals = exprf()
        for ret, val in zip(rets, vals):
            ret._value = val
    EUDOnStart(_)
    return rets

def _ARR(items):
    k = EUDArray(len(items))
    for i, item in enumerate(items):
        k[i] = item
    return k

def _VARR(items):
    k = EUDVArray(len(items))()
    for i, item in enumerate(items):
        k[i] = item
    return k

def _SRET(v, klist):
    return List2Assignable([v[k] for k in klist])

def _SV(dL, sL):
    [d << s for d, s in zip(FlattenList(dL), FlattenList(sL))]

class _ATTW:
    def __init__(self, obj, attrName):
        self.obj = obj
        self.attrName = attrName

    def __lshift__(self, r):
        setattr(self.obj, self.attrName, r)

    def __iadd__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov + v)

    def __isub__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov - v)

    def __imul__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov * v)

    def __ifloordiv__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov // v)

    def __iand__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov & v)

    def __ior__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov | v)

    def __ixor__(self, v):
        ov = getattr(self.obj, self.attrName)
        setattr(self.obj, self.attrName, ov ^ v)

class _ARRW:
    def __init__(self, obj, index):
        self.obj = obj
        self.index = index

    def __lshift__(self, r):
        self.obj[self.index] = r

    def __iadd__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov + v

    def __isub__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov - v

    def __imul__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov * v

    def __ifloordiv__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov // v

    def __iand__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov & v

    def __ior__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov | v

    def __ixor__(self, v):
        ov = self.obj[self.index]
        self.obj[self.index] = ov ^ v

def _L2V(l):
    ret = EUDVariable()
    if EUDIf()(l):
        ret << 1
    if EUDElse()():
        ret << 0
    EUDEndIf()
    return ret

def _MVAR(vs):
    return List2Assignable([
        v.makeL() if IsEUDVariable(v) else EUDVariable() << v
        for v in FlattenList(vs)])

def _LSH(l, r):
    if IsEUDVariable(l):  return f_bitlshift(l, r)
    else: return l << r

## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY

# (Line 1) import variable as v;
import variable as v
# (Line 2) import func.trig as trg;
from func import trig as trg
# (Line 8) function MoveLoc(unit : TrgUnit, playerID : TrgPlayer, pos_x, pos_y)
# (Line 9) {
@EUDTypedFunc([TrgUnit, TrgPlayer, None, None])
def MoveLoc(unit, playerID, pos_x, pos_y):
    # (Line 10) MoveLocation(v.P_LocationID[playerID], unit, playerID, "Anywhere");
    # (Line 11) addloc(v.P_LocationID[playerID], pos_x, pos_y);
    DoActions(MoveLocation(v.P_LocationID[playerID], unit, playerID, "Anywhere"))
    f_addloc(v.P_LocationID[playerID], pos_x, pos_y)
    # (Line 12) }
    # (Line 32) function SkillUnit(playerID : TrgPlayer, count, unit : TrgUnit, type : TrgCount)

# (Line 33) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, TrgCount])
def SkillUnit(playerID, count, unit, type):
    # (Line 34) switch (type)
    EUDSwitch(type)
    # (Line 35) {
    # (Line 36) case 0:
    _t1 = EUDSwitchCase()
    # (Line 37) CreateUnitWithProperties(count, unit, dwrand() % 8 + 33, playerID, UnitProperty(burrowed = true));
    if _t1(0):
        # (Line 38) break;
        DoActions(CreateUnitWithProperties(count, unit, f_dwrand() % 8 + 33, playerID, UnitProperty(burrowed=True)))
        EUDBreak()
        # (Line 39) case 1:
    _t2 = EUDSwitchCase()
    # (Line 40) CreateUnitWithProperties(count, unit, dwrand() % 8 + 33, playerID, UnitProperty(hallucinated = true));
    if _t2(1):
        # (Line 41) break;
        DoActions(CreateUnitWithProperties(count, unit, f_dwrand() % 8 + 33, playerID, UnitProperty(hallucinated=True)))
        EUDBreak()
        # (Line 42) case 2:
    _t3 = EUDSwitchCase()
    # (Line 43) CreateUnitWithProperties(count, unit, dwrand() % 8 + 33, playerID, UnitProperty(hallucinated = true, burrowed = true));
    if _t3(2):
        # (Line 44) break;
        DoActions(CreateUnitWithProperties(count, unit, f_dwrand() % 8 + 33, playerID, UnitProperty(hallucinated=True, burrowed=True)))
        EUDBreak()
        # (Line 45) }
    # (Line 47) SetInvincibility(Enable, unit, playerID, "[Skill]Unit_Wait_ALL");
    EUDEndSwitch()
    # (Line 48) MoveUnit(count, unit, playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]);
    DoActions(SetInvincibility(Enable, unit, playerID, "[Skill]Unit_Wait_ALL"))
    # (Line 49) }
    DoActions(MoveUnit(count, unit, playerID, "[Skill]Unit_Wait_ALL", v.P_LocationID[playerID]))
    # (Line 73) function Shape_Dot(playerID : TrgPlayer, count, Unit : TrgUnit, x, y, type)

# (Line 74) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def Shape_Dot(playerID, count, Unit, x, y, type):
    # (Line 75) MoveLoc(v.P_UnitID[playerID], playerID, x, y);
    MoveLoc(v.P_UnitID[playerID], playerID, x, y)
    # (Line 76) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 77) }
    # (Line 102) function Shape_Double(playerID : TrgPlayer, count, unit : TrgUnit, pos_x, pos_y, type : TrgCount)

# (Line 103) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, TrgCount])
def Shape_Double(playerID, count, unit, pos_x, pos_y, type):
    # (Line 104) Shape_Dot(playerID, count, unit, pos_x, pos_y, type);
    Shape_Dot(playerID, count, unit, pos_x, pos_y, type)
    # (Line 105) Shape_Dot(playerID, count, unit, -pos_x, -pos_y, type);
    Shape_Dot(playerID, count, unit, -pos_x, -pos_y, type)
    # (Line 106) }
    # (Line 134) function Shape_DoubleAt(playerID : TrgPlayer, count, unit : TrgUnit, pos_x, pos_y, distance_x, distance_y, type : TrgCount)

# (Line 135) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, TrgCount])
def Shape_DoubleAt(playerID, count, unit, pos_x, pos_y, distance_x, distance_y, type):
    # (Line 136) Shape_Dot(playerID, count, unit, pos_x + distance_x, pos_y + distance_y, type);
    Shape_Dot(playerID, count, unit, pos_x + distance_x, pos_y + distance_y, type)
    # (Line 137) Shape_Dot(playerID, count, unit, -pos_x + distance_x, -pos_y + distance_y, type);
    Shape_Dot(playerID, count, unit, -pos_x + distance_x, -pos_y + distance_y, type)
    # (Line 138) }
    # (Line 164) function Shape_Square(playerID : TrgPlayer, count, Unit : TrgUnit, x, y, type)

# (Line 165) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def Shape_Square(playerID, count, Unit, x, y, type):
    # (Line 166) MoveLoc(v.P_UnitID[playerID], playerID, x, y);
    MoveLoc(v.P_UnitID[playerID], playerID, x, y)
    # (Line 167) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 168) MoveLoc(v.P_UnitID[playerID], playerID, -y, x);
    MoveLoc(v.P_UnitID[playerID], playerID, -y, x)
    # (Line 169) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 170) MoveLoc(v.P_UnitID[playerID], playerID, -x, -y);
    MoveLoc(v.P_UnitID[playerID], playerID, -x, -y)
    # (Line 171) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 172) MoveLoc(v.P_UnitID[playerID], playerID, y, -x);
    MoveLoc(v.P_UnitID[playerID], playerID, y, -x)
    # (Line 173) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 174) }
    # (Line 203) function Shape_SquareAt(playerID : TrgPlayer, count, Unit : TrgUnit, x, y, distance_x, distance_y, type)

# (Line 204) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None])
def Shape_SquareAt(playerID, count, Unit, x, y, distance_x, distance_y, type):
    # (Line 205) MoveLoc(v.P_UnitID[playerID], playerID, x + distance_x, y + distance_y);
    MoveLoc(v.P_UnitID[playerID], playerID, x + distance_x, y + distance_y)
    # (Line 206) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 207) MoveLoc(v.P_UnitID[playerID], playerID, -y + distance_x, x + distance_y);
    MoveLoc(v.P_UnitID[playerID], playerID, -y + distance_x, x + distance_y)
    # (Line 208) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 209) MoveLoc(v.P_UnitID[playerID], playerID, -x + distance_x, -y + distance_y);
    MoveLoc(v.P_UnitID[playerID], playerID, -x + distance_x, -y + distance_y)
    # (Line 210) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 211) MoveLoc(v.P_UnitID[playerID], playerID, y + distance_x, -x + distance_y);
    MoveLoc(v.P_UnitID[playerID], playerID, y + distance_x, -x + distance_y)
    # (Line 212) SkillUnit(playerID, count, Unit, type);
    SkillUnit(playerID, count, Unit, type)
    # (Line 213) }
    # (Line 238) function Shape_NxNSquare(playerID : TrgPlayer, count, unit : TrgUnit, size, interval, type)

# (Line 239) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None])
def Shape_NxNSquare(playerID, count, unit, size, interval, type):
    # (Line 240) var loop = (size * size) / 4;
    loop = EUDVariable()
    loop << ((size * size) // 4)
    # (Line 241) var space = interval / 2;
    space = EUDVariable()
    space << (interval // 2)
    # (Line 242) var dis_x, dis_y;
    dis_x, dis_y = EUDCreateVariables(2)
    # (Line 244) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 245) {
        # (Line 246) Shape_Dot(playerID, count, unit, 0, 0, type);
        Shape_Dot(playerID, count, unit, 0, 0, type)
        # (Line 247) }
        # (Line 248) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 249) {
        # (Line 250) foreach(idx : EUDLoopRange(loop))
        for idx in EUDLoopRange(loop):
            # (Line 251) {
            # (Line 252) if (size % 2 == 0)
            if EUDIf()(size % 2 == 0):
                # (Line 253) {
                # (Line 254) dis_x = idx % (size / 2) + 1;
                dis_x << (idx % (size // 2) + 1)
                # (Line 255) dis_y = idx / (size / 2) + 1;
                dis_y << (idx // (size // 2) + 1)
                # (Line 256) if (dis_x == 1)
                if EUDIf()(dis_x == 1):
                    # (Line 257) { Shape_Square(playerID, count, unit, space, dis_y * interval - space, type); }
                    Shape_Square(playerID, count, unit, space, dis_y * interval - space, type)
                    # (Line 258) else if (dis_y == 1)
                if EUDElseIf()(dis_y == 1):
                    # (Line 259) { Shape_Square(playerID, count, unit, dis_x * interval - space, space, type); }
                    Shape_Square(playerID, count, unit, dis_x * interval - space, space, type)
                    # (Line 260) else
                    # (Line 261) { Shape_Square(playerID, count, unit, dis_x * interval - space, dis_y * interval - space, type); }
                if EUDElse()():
                    Shape_Square(playerID, count, unit, dis_x * interval - space, dis_y * interval - space, type)
                    # (Line 262) }
                EUDEndIf()
                # (Line 263) else if (size % 2 == 1)
            if EUDElseIf()(size % 2 == 1):
                # (Line 264) {
                # (Line 265) dis_x = idx % (size / 2);
                dis_x << (idx % (size // 2))
                # (Line 266) dis_y = idx / (size / 2);
                dis_y << (idx // (size // 2))
                # (Line 268) if (idx == 0)
                if EUDIf()(idx == 0):
                    # (Line 269) { Shape_Dot(playerID, count, unit, 0, 0, type); }
                    Shape_Dot(playerID, count, unit, 0, 0, type)
                    # (Line 270) else if (dis_y == 0)
                if EUDElseIf()(dis_y == 0):
                    # (Line 271) { Shape_Square(playerID, count, unit, dis_x * interval, 0, type); }
                    Shape_Square(playerID, count, unit, dis_x * interval, 0, type)
                    # (Line 272) else
                    # (Line 273) { Shape_Square(playerID, count, unit, dis_x * interval + interval, dis_y * interval, type); }
                if EUDElse()():
                    Shape_Square(playerID, count, unit, dis_x * interval + interval, dis_y * interval, type)
                    # (Line 275) if (idx == loop - 1)
                EUDEndIf()
                if EUDIf()(idx == loop - 1):
                    # (Line 276) { Shape_Square(playerID, count, unit, (size / 2) * interval, 0, type); }
                    Shape_Square(playerID, count, unit, (size // 2) * interval, 0, type)
                    # (Line 277) }
                EUDEndIf()
                # (Line 278) }
            EUDEndIf()
            # (Line 279) }

        # (Line 280) }
    EUDEndIf()
    # (Line 310) function Shape_Edge(playerID : TrgPlayer, count, Unit : TrgUnit, degree, n, interval, property)

# (Line 311) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None])
def Shape_Edge(playerID, count, Unit, degree, n, interval, property):
    # (Line 312) var i = 0;
    i = EUDVariable()
    i << (0)
    # (Line 314) trg.Table_Sin(playerID, degree, interval * 14 / 10);
    trg.Table_Sin(playerID, degree, interval * 14 // 10)
    # (Line 315) trg.Table_Cos(playerID, degree, interval * 14 / 10);
    trg.Table_Cos(playerID, degree, interval * 14 // 10)
    # (Line 317) var x_o = v.P_AngleCos[playerID];
    x_o = EUDVariable()
    x_o << (v.P_AngleCos[playerID])
    # (Line 318) var y_o = v.P_AngleSin[playerID];
    y_o = EUDVariable()
    y_o << (v.P_AngleSin[playerID])
    # (Line 320) var distance = (interval * 2) / (n - 1);
    distance = EUDVariable()
    distance << ((interval * 2) // (n - 1))
    # (Line 322) trg.Table_Sin(playerID, degree + 45, distance);
    trg.Table_Sin(playerID, degree + 45, distance)
    # (Line 323) trg.Table_Cos(playerID, degree + 45, distance);
    trg.Table_Cos(playerID, degree + 45, distance)
    # (Line 325) var distance_x = v.P_AngleCos[playerID];
    distance_x = EUDVariable()
    distance_x << (v.P_AngleCos[playerID])
    # (Line 326) var distance_y = v.P_AngleSin[playerID];
    distance_y = EUDVariable()
    distance_y << (v.P_AngleSin[playerID])
    # (Line 328) if (n == 1)
    if EUDIf()(n == 1):
        # (Line 329) {
        # (Line 330) Shape_Dot(playerID, 1, Unit, 0, 0, property);
        Shape_Dot(playerID, 1, Unit, 0, 0, property)
        # (Line 331) }
        # (Line 332) else if (n > 1)
    if EUDElseIf()(n <= 1, neg=True):
        # (Line 333) {
        # (Line 334) for (; i < n - 1; i++)
        if EUDWhile()(i >= n - 1, neg=True):
            def _t4():
                i.__iadd__(1)
            # (Line 335) {
            # (Line 336) Shape_Square(playerID, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property);
            Shape_Square(playerID, 1, Unit, x_o - (distance_x * i), y_o - (distance_y * i), property)
            # (Line 337) }
            # (Line 338) }
            EUDSetContinuePoint()
            _t4()
        EUDEndWhile()
        # (Line 339) }
    EUDEndIf()
    # (Line 373) function Shape_EdgeAt(playerID : TrgPlayer, count, unit : TrgUnit, degree, size, interval, x, y, type)

# (Line 374) {
@EUDTypedFunc([TrgPlayer, None, TrgUnit, None, None, None, None, None, None])
def Shape_EdgeAt(playerID, count, unit, degree, size, interval, x, y, type):
    # (Line 375) trg.Table_Sin(playerID, degree, interval * 14 / 10);
    trg.Table_Sin(playerID, degree, interval * 14 // 10)
    # (Line 376) trg.Table_Cos(playerID, degree, interval * 14 / 10);
    trg.Table_Cos(playerID, degree, interval * 14 // 10)
    # (Line 378) var x_o = v.P_AngleCos[playerID];
    x_o = EUDVariable()
    x_o << (v.P_AngleCos[playerID])
    # (Line 379) var y_o = v.P_AngleSin[playerID];
    y_o = EUDVariable()
    y_o << (v.P_AngleSin[playerID])
    # (Line 381) var distance = (interval * 2) / (size - 1);
    distance = EUDVariable()
    distance << ((interval * 2) // (size - 1))
    # (Line 383) trg.Table_Sin(playerID, degree + 45, distance);
    trg.Table_Sin(playerID, degree + 45, distance)
    # (Line 384) trg.Table_Cos(playerID, degree + 45, distance);
    trg.Table_Cos(playerID, degree + 45, distance)
    # (Line 386) var distance_x = v.P_AngleCos[playerID];
    distance_x = EUDVariable()
    distance_x << (v.P_AngleCos[playerID])
    # (Line 387) var distance_y = v.P_AngleSin[playerID];
    distance_y = EUDVariable()
    distance_y << (v.P_AngleSin[playerID])
    # (Line 389) if (size == 1)
    if EUDIf()(size == 1):
        # (Line 390) { Shape_Dot(playerID, 1, unit, x, y, type); }
        Shape_Dot(playerID, 1, unit, x, y, type)
        # (Line 391) else if (size > 1)
    if EUDElseIf()(size <= 1, neg=True):
        # (Line 392) { foreach(idx : EUDLoopRange(size - 1)) { Shape_SquareAt(playerID, 1, unit,x_o - (distance_x * idx),y_o - (distance_y * idx), x, y, type); } }
        for idx in EUDLoopRange(size - 1):
            Shape_SquareAt(playerID, 1, unit, x_o - (distance_x * idx), y_o - (distance_y * idx), x, y, type)

        # (Line 393) }
    EUDEndIf()
