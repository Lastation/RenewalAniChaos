import PluginVariables as msqcvar;
import variable as v;

import character.group6.hodaka.text 	as hodaka;


function CharacterVoice(count : TrgCount)
{
	var playerID = getcurpl();
	
	foreach(idx : EUDLoopRange(6))
	{
		if 	(v.P_HeroID[playerID] == 32)	{ hodaka.	P_player[idx] = count; hodaka.	P_observer[idx] = count; 	}
	}
}

function main(playerID)
{
	hodaka.main(playerID);
}